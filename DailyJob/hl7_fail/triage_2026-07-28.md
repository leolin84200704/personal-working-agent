# HL7 File Input Daily Triage — 2026-07-28

## BLOCKED — VPN down, prod DB unreachable

- Pre-flight: `lisportalprod2.mysql.database.azure.com:3306` unreachable from this machine at Tue Jul 28 11:25:57 PDT 2026.
- Likely cause: Cisco Secure Client VPN not connected (it auto-disconnects overnight) and the current network path cannot reach the DB without it.
- A headless reconnect to `45.24.217.146` was attempted and did not restore connectivity (SSO login is interactive; the GUI agent holds the connect capability).
- **No DB queries were run. This is NOT a "no failed records" result** — re-run manually after reconnecting the VPN.

## [2026-07-28 dream back-check] Blind spot CLOSED — 0 failures

- Dream pipeline re-ran the check from the AKS prod pod (kubectl exec + mysql2 via pod DATABASE_URL, no VPN needed).
- Window: last 48h. Total hl7_file_input rows: 31. Failures (customer_not_found / emr_code_not_found / parse_finished=0 / error_detail): **0**.
