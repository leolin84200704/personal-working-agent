# HL7 File Input Daily Triage — 2026-07-30

## BLOCKED — VPN down, prod DB unreachable

- Pre-flight: `lisportalprod2.mysql.database.azure.com:3306` unreachable from this machine at Thu Jul 30 09:21:07 PDT 2026.
- Likely cause: Cisco Secure Client VPN not connected (it auto-disconnects overnight) and the current network path cannot reach the DB without it.
- A headless reconnect to `45.24.217.146` was attempted and did not restore connectivity (SSO login is interactive; the GUI agent holds the connect capability).
- **No DB queries were run. This is NOT a "no failed records" result** — re-run manually after reconnecting the VPN.

## Back-check by the 2026-07-31 dream run (via AKS prod pod, no VPN needed)

The blocked run above left today unexamined. Queried `hl7_file_input` from inside
`lis-emr-v2-deployment-prod-*` using the pod's own Prisma client / `DATABASE_URL`:

- **36h window: 21 rows received, 1 unfinished.**
- **UNTRIAGED FAILURE — row 6746**, received `2026-07-30T16:45:12Z`, `parse_finished=0`,
  `retry_num=0` (retries exhausted, so it will NOT self-heal on a rescan tick), `sample_id=NULL`,
  `last_error = "customer_not_found=VINCENT GROVE"`.
  - This is the standard `customer_not_found` class: the ordering provider has no `ehr_integrations`
    row. Playbook = mirror the practice's LIVE peer row (+ `order_clients`), then bump `retry_num` so
    the owning pod's retry-rescan re-parses from its retained local file.
  - **Not executed by the dream run** — adding integration rows is a prod INSERT and needs Leo's go
    (prod-change gate). Handing it over rather than acting.
  - Note `last_error` is doing its job on day 2 of VP-17533: the reason was legible from one query
    with no pod-log spelunking.

Lesson repeat: a blocked triage is not a clean triage. The pod-side DB read path works when the VPN
does not, and should be the fallback the daily job tries before declaring itself blocked.
