---
name: project_vp17065_daily_report
description: "VP-17065 Daily Appointments Report — MERGED (PR#525), prod template 45401442 live; External_url column follow-up PR#531 open to stage_test"
metadata: 
  node_type: memory
  type: project
  originSessionId: 25da76b7-c932-4728-aedf-05b96b1a128c
---

VP-17065 — Daily Appointments Report (Clinical Calendar Outage Backup) in **LIS-transformer-v2**. Scheduled `@Cron('1 0 * * 1-5', {timeZone:'America/Los_Angeles'})` builds a per clinical-team-member daily appointments `.xlsx` from the v2 calendar (practice 150105, `role != 'patient'`) and emails it as a VI My Calendar / UniMod outage backup. New module `src/calendar/models/daily-report/`. Each member gets their own single tab; ops (hung.l) gets the full workbook = delivery heartbeat; failure → email hung.l. Redis NX daily lock for multi-pod dedup. Added `exceljs`. 10 unit tests pass, build + start:dev boot verified.

**Status (2026-07-01): MERGED to stage_test via PR#525.** Postmark blocker resolved — prod template id `45401442` (VP-17155, server 8340335, subject "Daily Appointment Report {{report_date}}") filled into `email-templates-clinician.yaml` `daily_appointments_report.provider.prod` (staging still 0; job is prod-only, gated by `platform_type==='local' || isStagingEnvironment()` early-return in `runDailyReport`). Report emits via prod notification Kafka topic `notification-email-template` → LIS-notification-center → Postmark.

**External_url follow-up (2026-07-01, PR#531 open to stage_test, branch `feature/leo/VP-17065-external-url`)**: added a 20th column `External_url` = `v2_event.external_url` (buildRow + REPORT_COLUMNS). Note: for Call-type events `external_url` holds a PHONE NUMBER, not a URL — rendered as-is. 15 daily-report unit tests pass.

**TZ migration umbrella created (2026-06-23)**: Epic **VP-17190** "Migrate calendar timezone source: v2_calendar.timezone → provider timezone setting" + 4 child Tasks VP-17191 (A email/display), VP-17192 (B availability), VP-17193 (C external sync), VP-17194 (D read-API/writers/'UTC' sentinel). New source = provider-level `timezone` setting (VP-16943, gRPC `GetSettingByCustomerClinic`, default PST, normalize→IANA); reminder.service `resolveRecipientTimezone()` is the precedent. VP-17065 daily-report's `member.timezone` read is in scope (task A).

**TZ source migration started (2026-06-23, commit `cb9d70d` on VP-17065 branch, not pushed)**: added reusable `TimezoneSettingService` + `TimezoneSettingModule` at `src/calendar/models/shared/` (provider-level timezone via core SettingService gRPC `GetSettingByCustomerClinic`, normalize→IANA, fallback PST; mirrors reminder). daily-report now resolves each member's tz from this setting (per `calendar_owner_id`, clinic 150105), no longer reads `v2_calendar.timezone`. 9 unit tests pass, build + start:dev boot OK. **Note**: the reusable service lives on the VP-17065 branch — the VP-17190 child tasks (A event.service, B availability, C external sync) should reuse it (it lands in stage_test when VP-17065 merges, or replicate earlier).

**VP-17190 migration in progress (branch feature/leo/VP-17190, off VP-17065, commit `3d7f767`, NOT pushed)**: TZ source rule confirmed by Leo — provider always uses provider setting (all clinics); for recipients, clinic 150105 each party uses own setting (provider→clinician, patient→customer), non-150105 patient follows provider's tz (`TimezoneSettingService.resolveRecipientTimezone`). **ALL runtime v2_calendar.timezone readers migrated** (commits 3d7f767 + 1179760, branch feature/leo/VP-17190, NOT pushed; build+start:dev boot+daily-report tests green). Source decision Leo settled: the URL `GET /setting/getTimezoneSetting` is v2 (not v1) and JWT-bound to the caller → unusable server-side; it wraps the SAME gRPC `GetSettingByCustomerClinic`, so use the gRPC directly with explicit customer_id+clinic_id. Migrated: shared TimezoneSettingService (resolveProviderTimezone / resolveRecipientTimezone(150105 rule) / setProviderTimezone), schedule, public-booking, provider-availability, meeting-request, event.service (resolveEventTimezone+resolveRecipientTimezone async, 16 callers awaited, calendarTimezones map), zoom + outlook-inbound, user-settings (read+booking-window read setting; update writes setting + mirrors v2_calendar). LEFT intentionally: event-level v2_event.timezone passthroughs (google/outlook outbound, event DTO); WRITERS still mirror v2_calendar.timezone (provider-availability patient-calendar copy L667, user-settings dual-write); one-off migration scripts. REMAINING D cleanup: retire writers / drop v2_calendar.timezone column (separate; can't drop while mirror keeps mapper in sync). NOT covered behaviorally by tests beyond build/boot — needs runtime verification before merge.

**VP-17190 rebased to standalone (2026-06-24, commit `96c1f3b`, NOT pushed)**: per Leo, cut over decided (no gating on PST-default populate). Branch `feature/leo/VP-17190` reset onto origin/stage_test and rebuilt to contain ONLY the timezone migration (17 files, decoupled from daily-report/VP-17065). Added `timezone-setting.service.spec.ts` (9 tests green). Verified live: gRPC proto reachable from Mac (CORE_RPC_STAGE 192.168.60.6:30276), sampled 150105 clinicians all return setting='PST' (default; DB had dirty America/Anguilla/Boise). resolveIanaTimezone is total (abbrev→IANA w/ correct DST, junk→PST, never throws); 'UTC' taken literally. Leo confirmed: keep abbrev+IANA both (option a); IANA already pass-through. Backup of stacked version at branch `feature/leo/VP-17190-stacked`. NEXT: open PR to stage_test (not pushed yet — Leo's call). KNOWN future conflict: VP-17065 also creates timezone-setting.service.ts → when daily-report rebases onto post-VP-17190 stage_test, cb9d70d will conflict (resolve by keeping the fuller VP-17190 version).

**Resume TODO**: fill real template id → end-to-end live send test (real inbox + attachment) → Leo review → PR to stage_test. Two open decisions for Leo: excluding `is_canceled` events; whether members with no appointments still get an empty-tab email. Full record in lis-code-agent STM `VP-17065.md`; template-less test script at scratchpad `vp-17065-test-email.js`. See [[project_vp16945_reminder_provider_tz]] (same 150105 calendar / UTC-sentinel tz handling).
