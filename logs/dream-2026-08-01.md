# Dream Log — 2026-08-01

> Run started 18:30 PDT (2026-08-02 01:30Z). Working set is incremental since **2026-07-30** —
> two days, because the 2026-07-31 run never happened (see Automation health).

## 🟢 Last night's escalation is resolved — the cutover is live, not stuck in rollback

The 2026-07-30 log escalated that VP-17561's cloud-Kafka cutover had been reverted 71 min after
Jira Done. **Checked against prod tonight: it was re-applied and has been stable for 2 days.**

- `emr-v2` ns ConfigMap `lis-emr-v2-config-prod`: `KAFKA_CLOUD_ENABLED=true`, last `kubectl-patch`
  **2026-07-31T02:16:20Z** — 41 min after the 01:35:13Z revert.
- prod pod `...-55ffd5ffc5-dqc7f`: Running **47h, 0 restarts** (matches the 02:16Z rollout).
- Health: 0 `level:error` in 90 min, SFTP `189 successful / 0 failed`, `Scanning … [cloud]` polling
  normally, 25 result pushes in 30h **all `TRANSMITTED`**.
- **The switch-back caused no second duplicate wave**: `updated_at ∈ [02:16Z,04:00Z] AND created_at
  < 02:16Z` = **3 rows**, and the background rate for that signature is already ~1/hr. Those 3 are
  background, not replay.

**Why it was clean matters more than that it was clean.** The STM's own closeout section (written by
the session that did it) records that cloud was re-enabled **after advancing the cloud group's offset
past 8,501 queued messages** — i.e. the offset-forwarding discipline this repo wrote down after the
incident was actually executed. My first pass tonight explained the absence of a burst as "short
window × weekend low volume"; that was wrong (8,501 queued messages is not low volume) and I
corrected both the STM and `patterns.md`. Same system, same operation: **without** offset forwarding
→ 17 duplicate ORUs; **with** it → 0. That is the first positive evidence for that rule.

Also answered: last night's "was the 01:35Z revert yours?" — **yes, it was the agent's own**, already
documented and remediated in `VP-17561.md` (orphans repushed, stale rows closed, staging topic fixed).
No open ask for Leo on that. The idempotency gate is still absent (removed in VP-17408), unchanged.

## ⚠️ Automation health — three jobs failing quietly (needs Leo)

1. **The 2026-07-31 dream run never ran.** All 3 attempts died on `API Error: Unable to connect to
   API (ENOTFOUND)` / `curl: (6) Could not resolve host: api.anthropic.com`, `FAILED after 3
   attempts` at 20:43 PDT. Notably its own probe logged **"Network ready (attempt 26)" at 19:20:29,
   one minute before the first failure** — the readiness check passes on something weaker than
   resolving the API host. No alert anywhere; I only know because I read the log.
2. **HL7 daily triage BLOCKED 3 days running** (07-30, 07-31, 08-01) — identical "VPN down, prod DB
   unreachable" text each time. The 2026-07-30 log already recorded that **the pod-side read path
   works without VPN** and should be the job's fallback; that has not been wired in, so the job
   blocked twice more. (I did tonight's back-check from the pod again — results below.)
3. **Daily digest stopped after 2026-07-29** — `long-term-memory/daily-digest/` newest file is
   `2026-07-29.md`. Also silent.

Common shape: each job honestly wrote its own failure into a file nobody reads. Written up as a
factory inbox proposal (below) rather than a lesson PR.

## Ground truth reconciliation

- `reconcile-jira.py --apply`: **128 in sync / 0 flipped / 0 need manual review.**
- **The 7-night VP-16832 / VP-17117 noise is gone** — both are now `Done` in Jira, matching local
  `completed`. No action needed; it resolved itself.
- 5 local ids absent from Jira (expected — `INCIDENT-*` are agent-internal).

## Closeout audit (Done < 24h)

**None due.** JQL `status changed to (Done, Closed, Resolved) DURING (-30h, now)` returned **zero
issues**, so there are no new closures to audit. Carried-forward flags re-checked against Jira +
prod instead:

- **VP-17561** — **PASS (flag cleared)**. Per the section above: Done in Jira, cloud live on prod,
  healthy, no replay from the switch-back. This is the case last night's cross-review predicted —
  re-check *the flag*, not the deploy — and doing so is what cleared it.
- **VP-17538** (customerPay payment-method walk) — **FLAG persists, 4th night.** Still Done, still
  **zero Jira comments**; the ticket's own "needs confirmation of the authoritative rule before this
  is considered final" was never answered. Money path closed on unit tests. Unchanged, not re-argued.
- **VP-17539** (legacy bundle split) — **FLAG persists, 3rd night.** Still Done with 0 comments, and
  **VP-17535 is still `Dev To Do`** while shipped prod code still carries its `TEMPORARY` marker and
  removal instruction. The contradiction is live: a future reader following VP-17535 deletes
  production pricing behaviour. AC 3 (one order per bundle × 6) still unproven for 4 of 6 bundles.
- **VP-17537** — **FLAG persists, 3rd night.** Code live on prod since 2026-07-29, Jira still
  `Dev To Do` and still **unassigned**.
- **VP-17524** (`OUT_OF_RESULT_NORMAL` → TNP) — still `Dev To Do`. Zero errors in tonight's 90-min
  window, but volume was low; not evidence it's fixed.
- **LBS-1674** — still Done, still no execution evidence anywhere. **3rd night**; one sentence from
  Leo would close it.

## HL7 triage — back-checked from the pod (VPN not needed)

Queried `hl7_file_input` from inside the prod pod via its own Prisma client:

- **60h window: 18 received, 0 unfinished.** The two blocked triage days were, in fact, clean.
- **Row 6746 (Vincent Grove) recovered**: `parse_finished=1`, `sample_id=2607187`, so the
  add-provider fix executed today worked. Confirmed independently of the STM's claim.
- ⚠️ **`retry_num` reading conflict, resolved against code.** Last night's back-check recorded row
  6746 as `retry_num=0` ("retries exhausted, will NOT self-heal"); the fix STM records 4→3 and "no
  bump needed"; it is **3** now. Source of truth: `hl7-order.processor.ts` does
  `Math.max(0, retry_num - 1)` on failure, the rescan selects `retry_num > 0`, insert seeds
  `INITIAL_RETRY_NUM` → **retry_num is remaining budget, counting down**. So the fix STM's reading
  is the correct semantics and last night's "exhausted" claim was wrong. Which of the two mid-window
  values was misread is not resolvable without an audit trail, so I wrote the conflict into the LTM
  entry rather than picking a winner — nobody should cite "the retry budget was still pending" as an
  established observation.

## Signals

**9 files in the working set** (incremental since 2026-07-30, covering 2 days).

| File | Signal |
|------|--------|
| `HL7FAIL-20260730-TURNPAUGH.md` | `lasting_insight` (new; add-provider execution + 4 gotchas) |
| `VP-17577.md` | `lasting_insight` (completed; duplicate-booking verdict = works-as-designed) |
| `VP-17561.md` | `lasting_insight` (status corrected `rolled_back` → `completed`) |
| `VP-17559.md` | `completed` (extracted 2026-07-30, nothing new) |
| `VP-16934.md` | `completed` (Epic, audited 2026-07-30) |
| `VP-17538.md` | `completed` + carried flag |
| `VP-17539.md` | `completed` + carried flag |
| `VP-17537.md` | `approaching` (live on prod, Jira `Dev To Do`, unassigned) |
| `journal/2026-07-31-minilm-removal.md` | `undistilled_journal` — **had no frontmatter at all**, so `grep distilled: false` could never have found it |

No `overlap`. `stale`: 6 files, all `INCIDENT-*` (61–74d) — see Notes.

## Operations

- **Distilled journals: 1** — `2026-07-31-minilm-removal` → `patterns.md` new section「Agent repo
  自身的環境陷阱」: verify dream/memory work with `/usr/bin/python3` (launchd uses system python,
  homebrew's lacks `rich`); deleting a config field must be verified by `import`ing that module, not
  by running a caller that lazy-imports it (that false negative shipped and needed hotfix PR #18);
  documented retirement decisions compound. Frontmatter added + `distilled: true` + indexed.
- **Extracted: 5 entries across 3 LTM files**
  - `emr-integration.md` — `last_error` is **not cleared on success** (row 6746 still reads
    `customer_not_found=VINCENT GROVE`); success signal is `parse_finished=1` + `sample_id`.
  - `emr-integration.md` — `retry_num` = remaining-budget countdown (code-cited), plus the
    unresolved conflict note above.
  - `emr-integration.md` — **"the whole clinic is FULL_INTEGRATION, why does it fail?"**: the
    clinic-level row (`customer_id='-1'`, `requested_by=migration_script_emr_result`,
    `ordering_enabled=0`, `customer_npi=NULL`) is a result-only migration artifact that does not
    participate in order routing. Check the provider's own LIVE+ordering row, not the clinic's.
  - `patterns.md` — `.env DATABASE_URL` password is URL-encoded (`urllib.parse.unquote` before
    feeding `mysql`), and the **pod-side prod DB read path** for when VPN is down, including the
    column-name traps (`hl7_file_input` uses `received_time`/`updated_time`, not `created_at`) and
    BusyBox `grep` having no `--include`.
  - `repos.md` — VP-17577: the accession 1:1 guard's designed hole is `resetEventAccession`, which
    does not check whether the reset event is still live → two live future consults for one
    accession while the UNIQUE constraint is never violated (245 reset-while-active since May, 23 on
    future events, 2 accessions currently holding 2). Diagnose from the event table, not the claim table.
- **Merged: 0**
- **Updated: 3** — `VP-17561.md` frontmatter `rolled_back`→`completed` + a verification append;
  `patterns.md` cutover section extended with the re-cutover outcome **and corrected** to attribute
  the clean result to offset forwarding rather than window size; new gotcha: **`lis-emr-v2-config-prod`
  exists in BOTH `default` and `emr-v2` namespaces** — the `default` copy is a 2026-06-17 leftover
  whose value happens to match, so an un-namespaced `kubectl get` "verifies" a dead object.
- **Promoted: 0** — nothing hit the 3-file bar.
- **Cross-ticket review: not due** — 3 completed since the 2026-07-31 marker (TURNPAUGH, VP-17577,
  VP-17561); the bar is 5. Marker stays 2026-07-31.
- **Archived: 0 / Forgotten: 0** — verified, not assumed: only **2** completed STM files are older
  than 30 days (`VP-16784-87` 0.1134 / 65d, `VP-16720` 0.1407 / 61d) and both score above the 0.1
  bar. No archive entry meets forget criteria either.
- **Factory: 0 lesson PRs, 1 inbox proposal** —
  `framework/proposals/2026-08-01-a-job-that-cannot-run-must-alert.md`. The lesson ("a scheduled job
  that can't run must alert; writing BLOCKED into your own log is not a notification; readiness
  probes must probe the real dependency") is strong and reads standalone, but **2 of its 3 cases are
  my own scheduled jobs** = effectively one domain, so it fails the cross-domain bar. Inbox, not PR.

## Score Statistics

```
Auto-linked: 193 new link entries
Scores and indexes rebuilt
  stm: count=145 highest=1.04 lowest=0.01 median=0.24
  ltm: count=10 highest=1.19 lowest=0.11 median=0.80
  archive: count=26 highest=0.81 lowest=0.04 median=0.47
```

## Notes

- **Status-vocabulary drift is silently disabling the archive rule.** STM statuses in use:
  `completed` 92, `active` 36, `done` 4, `resolved` 4, `in_review` 2, plus `blocked`,
  `blocked-on-other-team`, `pending`, `vendor-pending`, `analysis_complete`, `deferred` (1 each).
  Phase 3's archive rule only matches `status: completed`, so the **10 files sitting in other
  terminal states (`done`/`resolved`/`analysis_complete`/`deferred`) can never be archived** no
  matter how old or low-scoring. The 6 `stale` files tonight are exactly this: all `INCIDENT-*`,
  61–74 days old, scores 0.018–0.098, two of them `resolved` (i.e. terminal) and thus invisible to
  the rule. I did **not** mass-rewrite statuses — that is a semantic edit across files, and changing
  the rule itself is an automation-behaviour change that needs a PR per CLAUDE.md. **Ask for Leo:**
  either normalize the vocabulary or teach the rule the terminal-status set; say which.
- The digest job being down since 07-29 means this log is the only place tonight's flags land — the
  dream prompt's "put FLAGs in the daily digest top section" has no digest to write into.
- Journal frontmatter is not guaranteed: tonight's entry had none, so the prescribed
  `grep "distilled: false"` discovery method silently missed it. I found it by listing the directory.
  Worth making the journal writer emit frontmatter, or the dream treat "no frontmatter" as undistilled.
