# Dream Log — 2026-08-10

> **四件事要 Leo 看**（細節在下面）：
> 1. **25 份報告從 08-06/08-07 卡到現在沒送出，時間窗與 VP-17651 的 browser-pool 爆量完全重疊** ——
>    全部停在 `PENDING`/`GENERATING` 且無錯誤字串，所以沒有任何 retry 被排程；backlog 已靜止三天。
>    這 25 家診所沒收到報告，而且不會自己好。
> 2. **VP-17657 / VP-17658 在 Jira 是 Done，但程式碼完全沒動** —— 對 `origin/main` 實測確認，
>    plaintext registry 密碼與 60.11 的 stale 參照都還在。
> 3. **report-pdf-engine 有一個 pod 在 2026-08-10 18:18 PT 被 OOMKilled** —— VP-17651 STM 當天
>    16:35 記下的 watch item 已經兌現：3 browsers/pod 配 4Gi limit（那個 limit 是照 2 browsers 訂的）。
> 4. **dream pipeline 連續三晚（08-07/08-08/08-09）跑到一半被 API 連線中斷砍掉**，
>    做完的 memory 編輯從未 commit（211 個檔案懸在 working tree），也沒有任何 dream log。
>    今晚已補 commit。第 1 點沒被更早發現，多半就是這三晚的 closeout audit 都沒跑完。

## Ground truth reconciliation

- `scripts/reconcile-jira.py --apply`：in sync 128 / **flipped to completed 1**（VP-17651，Jira 狀態 `Inactive`）
  / not found or no access 5（`INCIDENT-20260518`、`INCIDENT-20260528`、`INCIDENT-20260529`、
  `INCIDENT-20260604`、`INCIDENT-2604156666` —— 這五個是本地 incident 檔，本來就沒有 Jira ticket，非異常）。
- 手動補一筆：**VP-17653** frontmatter 是 `status: in_progress` 但 `jira_status: Done`，
  reconcile 判為 in sync（它只同步 `jira_status` 欄位、不會據此翻 `status`）。
  依 STM 內文「implementation complete」與 Jira Done，本次改為 `completed`。

## Closeout audit（Done < 24h，assignee = Leo）

JQL `status CHANGED TO (Done, Closed, Resolved, Inactive) AFTER -36h`，篩出 Hung-Fan Lin 的四張：

- **VP-17653**（Speed up emr-v2 CI/CD）— **PASS**
  - 全部 PR 找到並確認合併：#335/#337/#339/#341 + 對應 promotion #336/#338/#340/#342
    （promotion PR 標題不帶 ticket id，是照時間窗抓的）。
  - Build 全 SUCCESS；AKS prod `db3fb22` / staging `b589f59`，pods Running。
  - Manual prerequisites：無（純 Jenkinsfile / .dockerignore / eslint config，無 DDL、無新 env var）。
  - Live verification **在最後一次 deploy 之後**：main #255 Docker Build 5.4s（29/32 cached，
    證明 cache 修復生效）、test gate 兩支 branch 都在跑（101 suites / 1168 tests / lint 0 errors）。
  - 唯一未獨立驗證項：on-prem staging 的 image SHA（無 VPN）。因為 `parallel` 下任一支失敗會讓
    整個 build FAILURE，而 build 是 SUCCESS，聯合訊號成立 —— 不視為 chain 斷裂。

- **VP-17657**（Move hardcoded registry credentials to Jenkins credentials store）— **FLAG: Jira Done，程式碼未動**
  - Jira 2026-08-10 14:44 PT 轉 Done。對 `origin/main` 的 `Jenkinsfile` 實測：
    line 103 / 313 仍是 `docker login -u 4693646 -p <plaintext>`，line 123 / 321 仍是 ACR access key 明文。
  - 密碼**未輪替**，且已永久留在 git history。VP-17653 STM 自己最後一行就寫著
    「VP-17657 (creds), VP-17658 (docs) untouched」。
  - 這張是 14:44 那一分鐘連同 VP-17658 一起被轉 Done 的（與 promotion PR #340/#342 合併時間吻合），
    推測是誤觸或批次轉換，不是實作完成。

- **VP-17658**（Update stale on-prem endpoint docs）— **FLAG: Jira Done，程式碼未動**
  - `origin/main` 的 `Jenkinsfile` line 360/363/365 仍指向 `192.168.60.11`
    （含那個每次 build 都印「not yet accessible」的 `nc -zv 192.168.60.11 31319`）；docs 亦未更新。

- **VP-17651**（Error Downloading Reports, P1）— **PASS，但帶一個已兌現的 prod watch item**
  - Closure chain 完整：Datadog 確認 root cause（browser-pool exhaustion，236 次 500，08-07 16:30Z 後歸零）
    → prod mitigation 實施並驗證 → report-pdf-engine **PR #9 已合併**
    （https://github.com/Vibrant-America/report-pdf-engine/pull/9，merge 8819d58，2026-08-10 23:34Z）
    → merge 後實測 manifest 與 live 一致。
  - **FLAG（健康訊號）**：今晚實測 ns `report`，
    `report-pdf-engine-5d98c96-vr4vb` **restartCount=1 / OOMKilled / exitCode 137 /
    finishedAt 2026-08-11T01:18:53Z（18:18 PT）**，其餘 7 個 pod restarts=0。
    Deployment 現況 `BROWSERS_PER_WORKER=3`、`POOL_ACQUIRE_TIMEOUT=30000`、
    `limits: {cpu: 2, memory: 4Gi}`。那個 4Gi 是當初為 **2** 個 browser 調上去的（c9d41cd 2Gi→4Gi），
    第三個 browser 加上去時沒有同步調高。
    → 不是 outage（K8s 自癒，8/8 Running），但每次 OOMKill 會讓該 pod 上所有進行中的 render 失敗，
    使用者看到的正是這張 P1 的同一個 toast。建議：memory limit 按比例調高（~6Gi），
    或把 `BROWSERS_PER_WORKER` 退回 2 只保留 replica 加倍。**未自行更動**（別隊的 service + prod 資源變更）。
    namespace 的 `OOMKilling` event 已過期，只剩 `lastState` 這一個痕跡，而它會在下次 rollout 被清掉。
  - 其餘未結項目（不影響 chain）：客戶回測確認、2026-08-10 ~11:00 PT 那筆從未到達我方 server 的失敗、
    report-pdf-engine 5xx 沒有告警。

### Post-deploy health scan（closeout audit 第 4 步）

- **HL7 order 側乾淨**：`DailyJob/hl7_fail/triage_2026-08-10.md` — 過去 72 小時 `Total failed: 0`。
- **Result delivery 側有一筆重大發現**：`DailyJob/result_fail/report_2026-08-10.md` 列出
  **25 份「從未成功投遞、且沒有任何東西在重試」的報告**。它們的 `created_at`（DB 原值，UTC）
  精準落在 VP-17651 在 Datadog 上量到的那兩個尖峰小時裡：

  | 卡住的時間（UTC） | 筆數 | 對應的 Datadog `ResourceRequest timed out` |
  |---|---|---|
  | 2026-08-06 18:04-18:05Z | 6 | 08-06 **18Z = 88 次** |
  | 2026-08-07 15:58-17:00Z | 17 | 08-07 **15Z = 75 次**、**16Z = 54 次** |

  這 23 筆全部停在 `PENDING / PENDING` 或 `GENERATING / PENDING`，**error 欄位是空的** ——
  generation 從未完成，也沒有任何東西把它標成失敗，所以 retry 永遠不會被排程。
  vendor 幾乎都是 MDHQ（一筆 OPTIMANTRA）。另外 2 筆是 08-06 01:0xZ 的 Cascades SFTP 錯誤，與此無關。

  **為什麼很可能是同一起事故**：VP-17651 的 root cause 段落本來就寫了受害者是
  「mostly GET /pdf（互動下載）**+ some internal POST /pdf（base-report-service batch）
  competing for the same pool**」。batch 產報路徑搶不到 browser，留下的正是這種
  「卡在 GENERATING、沒有錯誤」的列。**尚未證實**，要確認得看 base-report-service
  在 pool 回 500 時是否真的什麼都不寫。

  Backlog 是**靜止的、不是還在長**：6（08-06）→ 13（08-07）→ **25（08-08、08-09、08-10 三天不動）**，
  與「爆量在 08-07 16:30Z 結束、受害者被留在原地」完全吻合。

  → **未處理**（dream 不重打 prod 投遞）。`report_2026-08-10.md` 內附每筆的 `GenerateResultHl7`
  re-drive 指令，且成因已排除（併發容量現在是 24）。但 result push **沒有 idempotency gate**，
  重放會再送一次 ORU，所以要逐筆先確認現況再決定，不能整批盲打。Leo 裁示。

**Dependency propagation**：grep 全部 STM 的 `unblocked_by:` 與 `unblock_when:`，
沒有任何 STM 以 VP-17651 / VP-17653 / VP-17656 為 blocker。VP-17651 自己的 `unblock_when`
（Leo 決定如何處理 local fix branch）已由 PR #9 合併滿足，該檔狀態已是 completed。無需插入 RE-CHECK。

## Signals

Working set：incremental since 2026-08-06（最新的 `logs/dream-*.md`）。
註：實際上 08-07 與 08-09 也各有一次「跑到一半」的 consolidation（見 Notes），
其產出還留在未 commit 的 working tree 裡，所以那幾天的檔案雖在窗內、內容已被處理過。

| 檔案 | updated | Signal | 處置 |
|------|---------|--------|------|
| VP-17653 | 2026-08-10 | `lasting_insight`（新） | 蒸餾進 patterns.md；status → completed；補 closeout 記錄 |
| VP-17651 | 2026-08-10 | `lasting_insight`（新） | 蒸餾進 patterns.md；補 OOM 實測 section |
| VP-17589 | 2026-08-09 | `completed` | 已由 08-09 那次 run 蒸餾（emr-integration.md 內有其反例段落），不重複 |
| INCIDENT-20260808-critical-result-tnp | 2026-08-09 | `approaching`（active, 2 天） | 已由 08-09 run 蒸餾進 emr-integration.md；仍未開票，維持 active |
| VP-17628 | 2026-08-07 | `completed` | 已由 08-07 run 蒸餾（patterns.md 的 staging-order-is-a-real-order 修正段） |
| VP-17631 / VP-17524 / VP-17503 | 2026-08-06 | `completed` | 08-06 dream 已處理（3 篇 journal 已 distilled） |

- `undistilled_journal`：**0**（`journal/` 下所有條目 `distilled: true`）。
  但今天 VP-17651 / VP-17653 兩件實作工作**沒有留下任何 journal 條目** —— 見 Notes。
- `overlap` / `stale`：本次無。

## Operations

- **Distilled journals: 0**（無未蒸餾條目）
- **Extracted: 2**（VP-17653、VP-17651 → `long-term-memory/patterns.md`，追加兩個大段落）
  - 「CI 變慢的真正原因不在 Jenkinsfile 裡 —— 先跟 CI server 要每個 stage 的實測時間」
    （含：`parallel` 改變了什麼證據證明什麼／慢往往是另一個 bug 的症狀，
    main build 每次覆蓋 on-prem staging image／被註解掉的 stage 是倒數計時的刪除彈）
  - 「查不到錯誤，先確認你的查詢查得到錯誤」
    （含：pod log 只涵蓋 pod 壽命／Datadog `status` 不是服務 severity／
    明顯的 hardening 可能放大故障／提高併發度沒提高資源上限 = 之後才炸的 OOM）
- **Merged: 0 / Updated: 2**（VP-17653 status + closeout section、VP-17651 OOM section）
- **Promoted: 0**
- **Cross-ticket review: not due** —— 上一次是 2026-08-09（`emr-integration.md` 的
  「【跨 ticket 蒸餾 2026-08-09】result path 只監看有沒有送到」），此後只有 2 張 completed（< 5）。
- **Archived: 0**（STM 無 completed + >30 天 + score<0.1 者；journal 無 distilled + >30 天者）
- **Forgotten: 0**（archive 內無 score<0.05 + >180 天 + 非 emr_integration 者）
- **Factory lesson: 0 PR / 1 inbox proposal**
  - `~/src/project-agent-factory/framework/proposals/2026-08-10-calibrate-a-query-before-trusting-its-zero.md`
  - 課題「把零筆當證據之前，先用已知正例校準查詢」通過 standalone 與 cross-domain 兩關
    （可觀測性查詢的 severity 欄位／排程器永遠 0 bytes 的 stdout／macOS TCC 列目錄失敗，三個不同領域），
    但 **08-06 才剛合併過一個 lesson PR**，而規則要求 PR 頻率約每月一次；
    且此課題與既有的「安靜的觀測窗不是故障證據 — 要量輸入端」高度相鄰，
    可能該併入而非另立。→ 走 inbox，由 Leo 裁決。

## Score Statistics

```
Auto-linked: 6 new link entries
Scores and indexes rebuilt
  stm: count=147 highest=0.97 lowest=0.01 median=0.22
  ltm: count=10 highest=1.20 lowest=0.11 median=0.80
  archive: count=38 highest=0.78 lowest=0.03 median=0.11
```

## Notes

### dream pipeline 連續三晚被 API 連線中斷砍在半路（08-07 / 08-08 / 08-09）

今晚一開始就發現不對：`storage/short_term_memory/_index.md` 的 `Last updated:` 是 2026-08-09，
但最新的 `logs/dream-*.md` 是 08-06，而 working tree 掛著 **211 個未 commit 的檔案**
（含 LTM 內文明確寫著「2026-08-09 dream 再量 22h 窗」的段落）。

`logs/launchd-stdout-2026-08-0{7,8,9}.log` 三份都是同一個結局：

```
API Error: Connection closed mid-response. The response above may be incomplete.
...
FAILED after 3 attempts
```

三晚都是三次 attempt 全滅，而且都死在中段（08-07 死在寫入 memory 時、08-08 死在 Phase 3、
08-09 死在 archive/factory 檢查前）。後果不是「什麼都沒做」，而是**做了一半且沒有留下紀錄**：

- Phase 3 的 LTM/STM 編輯**已經寫進檔案**，Phase 4 的 `memory_scoring.py` 也跑過（所以 index 是 08-09），
- 但 Phase 5 的 dream log 從未寫出，post-dream 的 commit 也從未執行。

也就是說：**「沒有 dream log」不代表那晚沒 dream**，而 `run-dream.sh` 目前沒有任何機制
把這種半完成狀態告訴任何人 —— 三個晚上的 API 失敗完全靜默（這正好是本次蒸餾的
「查不到錯誤，先確認你的查詢查得到錯誤」的又一個案例，只是這次受害者是 pipeline 自己）。

今晚已把那 211 個檔案連同本次的產出一起 commit。**未更動 `run-dream.sh`** ——
它屬於 automation 行為變更，依 CLAUDE.md 必須走 PR 讓 Leo 看到規則改了什麼。
可考慮的方向（等 Leo 裁示）：attempt 之間加 per-attempt wall-clock cap、
或在 `FAILED after N attempts` 時寫一個 stub dream log + 發通知，
讓「半完成」至少留下痕跡。同一個 `Connection closed mid-response` 形態
在 2026-08-05 的 dream log 裡就已經記過（當時受害者是 daily digest），至今未處理。

### 補記（18:43，由 attempt 2 寫入）：retry 在工作已完成後又跑了一次 dream

上面那段寫完後，attempt 1 在 **18:41:52** 也吃到同一個 `API Error: Connection closed
mid-response` —— 但這次是**在所有工作都做完之後**才斷：dream log 已寫出（18:40）、
commit `87e2613` 已建立並 push（18:41:06）。`run-dream.sh` 看到非零 exit 就重試，
於是 **attempt 2/3 在 18:42:22 對一個「已經完成的今晚」重新跑了一次整條 pipeline**。

Attempt 2（本段作者）的處置：先比對狀態而非照 phase 重做 —— reconcile 0 flips（in sync 129）、
working tree 乾淨、無 STM 在上次 commit 後被改、`journal/` 無未蒸餾條目。
重跑 `memory_scoring.py` 驗證冪等：`Auto-linked: 0`、三個 tier 的 count/median 與 attempt 1 完全相同、
`git status` 零 diff。**未重做任何 Phase 3 操作，未覆寫本 log 的既有內容。**

**這是一次 near-miss，值得記一筆**：retry 機制不知道工作已經成功。若 attempt 2 照 Phase 5
的字面指示「寫 dream log 到 `logs/dream-YYYY-MM-DD.md`」，就會**整份覆蓋掉今晚的 closeout audit
與那 4 條要給 Leo 的發現**（25 份卡住的報告、2 個 FLAG、OOMKill）。救回來的是「先驗證再動手」，
不是 pipeline 本身有防護 —— 目前沒有任何機制擋這件事。

所以 `run-dream.sh` 的待決事項多一條（一樣**未自行更動**，要走 PR）：
retry 前先判斷該晚是否已完成（例如 `logs/dream-$(date).md` 已存在且當天已有 dream commit
就直接 exit 0），否則「工作已完成但連線斷掉」會被無限期當成失敗重跑。
今晚的 `Connection closed mid-response` 是**第四晚連續**出現（08-07/08/09 砍在半路、08-10 砍在收尾）。

### 今天的兩件實作工作沒有留下 journal 條目

VP-17651（P1 root cause + prod mitigation + PR）與 VP-17653/17656（CI/CD 重構 + 測試 gate 復原）
都是資訊量很高的 session，但 `journal/` 沒有 2026-08-10 的條目 —— 兩者的推理軌跡只存在 STM 裡。
今晚的蒸餾是直接從 STM 的 Gotchas / Deploy verification 段落取的，沒有遺漏可用素材，
但 journal 少了 episodic 記錄（例如「pool-wedge 假設被 Datadog 時序推翻」這個轉折，
在 STM 裡是結論，在 journal 裡本該是推理過程）。若 work loop 的 journal 步驟被略過了，值得留意。

### 其他觀察

- STM 已達 **147 筆**。archive 條件（completed + >30 天 + score<0.1）今晚一筆都沒命中，
  因為近期 completed 的檔案 score 都還高。這個門檻在 STM 持續成長下會愈來愈難觸發，
  index 也會愈來愈長 —— 之後可能需要按分數而非年齡來設 archive 觸發點。
- VP-17656 目前 Jira 是 `Dev In Progress`，但實作已完成並在 CI 上驗證過（PR #341/#342 已合併，
  test gate 兩支 branch 都在跑）。它不在本次 closeout audit 範圍（未轉 Done），
  但狀態落後於現實 —— Leo 若確認可轉 Done。

### 補記（18:46，由 attempt 3 寫入）：三個 attempt 全部被同一個晚上耗盡

Attempt 2 也在**做完事之後**斷線（addendum 已寫出、commit `9db672c` 已 push，
18:44:51 吃到同一個 `API Error: Connection closed mid-response`），於是 18:45:22 起跑 **attempt 3/3**。

Attempt 3（本段作者）同樣只驗證、不重做，結果與前兩次逐項相同：
reconcile 0 flips（in sync 129）、working tree 乾淨、`journal/` 無未蒸餾條目、
`memory_scoring.py` 重跑 `Auto-linked: 0` 且三個 tier 的 count/median/highest/lowest 完全一致、
`git status` 零 diff。**未重做任何 Phase 3 操作，未覆寫本 log 既有內容。**

重點是 escalation 的形狀：一個**已經在 18:40 就完成**的晚上，把 retry 預算 3/3 全部用光，
每次都是「工作成功 → 收尾串流斷 → 非零 exit → 重跑整條 pipeline」。三次都靠 agent
自行判斷「先比對狀態再動手」才沒有覆蓋掉 closeout audit 的發現 —— 連續三次都要靠這個判斷，
就不該再算運氣好。`run-dream.sh` 的 completion guard（該晚已有 dream log + dream commit 就 exit 0）
從「可考慮」升級為**建議優先處理**；它同時也會止住這種空轉重跑的 token 消耗。
一樣**未自行更動 `run-dream.sh`**（automation 行為變更依 CLAUDE.md 要走 PR）。
