# Dream Log — 2026-08-15

## Ground truth reconciliation

- `python3 scripts/reconcile-jira.py --apply`: **130 in sync, 0 flipped, 0 needing manual review.**
- 5 files not found / no access in Jira — all expected locally-coined incident ids, no Jira issue:
  `INCIDENT-20260518`, `INCIDENT-20260528`, `INCIDENT-20260529`, `INCIDENT-20260604`,
  `INCIDENT-2604156666`.
- (STM total moved 132 → 130 in-sync because two ids reconciled under the archive move on 08-14.)

## Closeout audit (Done < 24h)

Window query run **twice** — Atlassian MCP (caps at 5) and Jira REST `search/jql` with
`maxResults=100`, unfiltered by assignee. Both returned the **same 5 issues**, so the cap did not
truncate tonight. Only one is Leo/agent-owned:

- **VP-17723** (PM-side "report should come through ONLY TWICE") — **PASS, re-verified live.**
  It has no code of its own; its acceptance is VP-17715's prod config, so I re-probed prod rather
  than re-reading the STM:
  - Config unchanged and live: `c0000CMYCWWC500T2I1A85BHS` = `result_push_level=PER_REPORT_GROUP`,
    `deferred_report_short_names=["GUT5"]`, `result_enabled=1`, `status=LIVE`.
    E2E test row `cvp17715e2etest000000001` still correctly retired (`result_enabled=0`).
  - Health: `result_transmission_records` last 24h = **277 rows, all `TRANSMITTED`, zero failures**
    (latest 2026-08-15 21:09:27Z). No regression signal on the touched path.
  - **Residual (VP-17715 open item 4) still open** at ~31h post-flip: the only `GROUP:MAIN` row in
    prod is still the 8/14 E2E replay under the retired test integration. The real integration's
    last push in 48h was a **pre-flip** `SAMPLE_TYPE:Plasma` at 2026-08-14 02:12:59Z; zero pushes
    since the flip. **Not called as breakage** — pre-flip cadence for this integration was already
    ~1 push/day, and suppressing interim per-type pushes is exactly the designed behaviour. One
    quiet window proves nothing. Carried to the next dream.
  - **Gotcha caught mid-audit** (nearly a false FLAG): the real row's `updated_at` still reads
    `2026-07-16 23:26:51` even though the flip ran 8/14. `SHOW COLUMNS` shows
    `ehr_integrations.updated_at` is a plain `datetime` with **no `ON UPDATE CURRENT_TIMESTAMP`**,
    and the rollout SQL never set it. Distilled to `patterns.md`.

Out of scope (other engineers' tickets, listed for completeness, not audited):
VP-17720 (Fan Zhou), VP-17719 (Michael Kingsley), VP-17721 (Jiafan Qian), VP-17684 (Zhibin Chen).

### Carried-forward flag

- **VP-17691** — yesterday's FLAG (Done + live on prod, but **no live probe has ever run**) is
  **still open**. The STM was reconstructed on 08-14; the missing item is evidence, not memory.
  Needs a work-session probe of the partner API error envelope / request id / `chargeIndicator`
  validation. Not closable from dream.

### Dependency propagation

Grepped all STM frontmatter for `unblocked_by:` entries and `unblock_when:` text naming VP-17723
or VP-17715: **none**. No RE-CHECK markers inserted, no digest lines owed.

## Scheduled-job blind spot (the real finding tonight)

Both prod-failure watches **silently produced nothing for two consecutive days**:

| job | 2026-08-14 | 2026-08-15 (scheduled) | 2026-08-15 (manual re-run) |
|---|---|---|---|
| `DailyJob/hl7_fail` | BLOCKED (VPN) | BLOCKED 07:21 PDT | **ran clean** |
| `DailyJob/result_fail` | BLOCKED (VPN) | BLOCKED 08:46 PDT | **ran, found new failures** |

The scripts behaved correctly — each wrote `## BLOCKED — prod DB unreachable` and said in bold
"This is NOT a 'no failures' result". The failure was downstream: **08-14's dream ran to completion
without noticing that day's failure monitoring never executed.** The headless VPN reconnect is
structurally unable to help — `Connect capability is unavailable. Another Cisco Secure Client
application acquired it.`

VPN/DB was reachable by tonight's run, so I re-ran both. The backfill was **not** empty:

- **HL7 triage: clean.** 11 `hl7_file_input` rows in 72h, 0 with `parse_finished=0`, and the job
  proved connectivity before trusting the empty result.
- **result_fail: not clean.** Undelivered reports **23 (08-13) → 26**. New this window:
  sample 2598303 (Unprescribed), 2597682 (MDHQ), and **2607174 (Cascades)** —
  a fresh `PERMANENT FAILURE after max retries` on 2026-08-13 13:46.

### Needs Leo's decision: Cascades

`Cascades` now has **6 total failures and its last success is 2026-06-01** — ~2.5 months with no
successful delivery. Only two prior passing mentions exist in memory (`VP-17651` noted the 08-06
SFTP rows as "unrelated"; `VP-17631` notes Cascades is an on-prem integration delivered from a
cloud pod). Nothing tracks it as an open problem.

I am **not** filing a ticket: per standing rules a found defect gets ticketed in a work session with
Leo confirming the type, never auto-filed from dream, and never as type Bug without confirmation.
Flagging for triage.

### Also observed: daily digest

`long-term-memory/daily-digest/` last entry on `origin/main` is **2026-08-13** — no 08-14, no 08-15,
and `logs/daily-digest/` does not exist on this repo path. Stated as an observation, **not** a
"digest is dead" call: this job has silent gaps in its own history (07-25→07-28, 08-08→08-10) and
the standing warning is that absent local output never proves a job didn't fire. What is solid here
is output-side, not log-absence: the files are not on main.

## Signals

Working set built incrementally since the last dream (`logs/dream-2026-08-14.md`, 2026-08-14):

- **0 files** in the incremental set.
  - STM with frontmatter `updated:` on/after 2026-08-14: none (only the generated `_index.md`).
  - STM flipped to `completed` by Phase 0: none.
  - Journal entries with `distilled: false`: none — both 08-14 entries
    (`vp17715-report-group-push`, `vp17714-clinician-switch-zoom-room`) are already `distilled: true`.
- Expected: 2026-08-15 was a Saturday with no work session. Yesterday's dream distilled everything.
- **+1 file touched anyway**, driven by the closeout audit rather than the incremental scan:
  `VP-17715.md` (`lasting_insight`) — new dated re-probe section, `updated:` → 2026-08-15.

## Operations

- **Distilled journals: 0** (none undistilled).
- **Extracted: 1** — VP-17715's `updated_at` gotcha → `patterns.md`.
- **Merged: 0 / Promoted: 0.**
- **Updated: 2** —
  - `patterns.md`: new section *"`ehr_integrations.updated_at` 不會自動更新 — 不可當「config 何時被改」的證據"*.
    Placed next to the existing `order_clients 無 updated_at 欄位` entry, which it directly qualifies
    ("has the column" ≠ "the column is maintained"), with the counter-example that
    `result_transmission_records.updated_at` **does** advance — so the replay-detection technique at
    line 1299 stays valid. Verify per table with `SHOW COLUMNS ... Extra`, never by analogy.
  - `patterns.md`: new distillation *"排程 job 寫出 BLOCKED 報告 = 那天沒有訊號，不是那天沒有問題"* —
    consume-side discipline for any pre-flight-guarded scheduled job (read the report's first
    section, treat BLOCKED/SKIPPED as a gap, escalate on two consecutive silent days, backfill once
    the precondition returns rather than waiting for the next tick). Framed as the upstream version
    of the existing "empty result ≠ no failures" rule: when no query was ever issued, there isn't
    even an empty result — only a file that looks normal.
  - `VP-17715.md`: dated re-probe section (counted under Extracted above, not double-counted here).
- **Cross-ticket review: not due.** Marker is 2026-08-14 (yesterday's review covered VP-17651,
  VP-17653, VP-17685, VP-17691, VP-17715). Zero STM files reached `completed` since; threshold is 5.
- **Archived: 0 / Forgotten: 0.** Nothing met the criteria (completed + >30d + score < 0.1).
- **Lesson PRs to project-agent-factory: 0. Inbox proposals: 0.**
  The scheduled-job lesson was the only candidate and it fails the **cross-domain evidence** bar —
  both supporting cases (`hl7_fail`, `result_fail`) are the same employer domain and, worse, the
  same VPN precondition and the same night. That is one incident with two filenames, not two
  incidents. Default is do-not-promote; it stays in this repo's LTM. Re-propose only if the same
  consume-side blindness shows up somewhere with an unrelated precondition.

## Score Statistics

```
Auto-linked: 48 new link entries
Scores and indexes rebuilt
  stm: count=148 highest=1.00 lowest=0.01 median=0.20
  ltm: count=10 highest=1.24 lowest=0.11 median=0.81
  archive: count=41 highest=0.76 lowest=0.03 median=0.08
```

STM count flat at 148 (no new tickets, nothing archived). LTM stays at 10 files; median 0.81.
Journal index needed no append — no new entries today.

## Notes

1. **The memory system had nothing to do tonight, and that was correct.** The incremental working
   set was genuinely empty. Everything of value tonight came from Phase 0.5 probing prod, not from
   consolidating files. Worth remembering the next time an empty working set looks like a failure:
   on a no-work day it is the expected result.

2. **Tonight's near-miss is the same shape as the lesson it produced.** I was one step from FLAGging
   VP-17723 because `updated_at` said the config was last touched on 7/16. The field value itself —
   the actual ground truth — said `PER_REPORT_GROUP`. Reading a *proxy* for the fact instead of the
   fact is precisely what the closeout audit exists to prevent, and it almost caught me inside the
   audit itself.

3. **Two open items for Leo, in priority order:**
   - **Cascades** has not delivered successfully since 2026-06-01 and picked up a new permanent
     failure on 08-13. Needs a triage decision (and a ticket, if Leo agrees it is one).
   - **VP-17691** still has no live probe, three days after going live on prod.

4. **The blind spot is structural, not a one-off.** Nothing currently reads the daily jobs' output
   before the next tick, so a BLOCKED day is indistinguishable from a clean day to every downstream
   consumer including dream. The obvious fix is a freshness check in dream's Phase 0.5 — but that is
   an automation behaviour change, which per this repo's rules must reach Leo as a PR rather than a
   direct commit to main. **Not done tonight; proposed here for Leo's call**, since it also changes
   what dream escalates on and he should decide the threshold.
