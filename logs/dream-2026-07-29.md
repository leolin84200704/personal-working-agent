# Dream Log — 2026-07-29

## Ground truth reconciliation
- Jira reconcile (`--apply`): **123 in sync / 0 flipped / 2 need manual review**
  - VP-16832, VP-17117 — local `completed`, Jira `QA Review` (**6th consecutive night**; QA has not moved them since 2026-07-24. Worth asking QA or accepting the local status as wrong.)
- 5 local ids not in Jira (expected — INCIDENT-* are agent-internal, no ticket).

## Closeout audit (Done < 24h)

Audited every Leo/agent closure in the window (5 tickets). All GitHub Actions deploy runs for the
4 promotions into `main` today concluded `success` (#298 18:17Z, #300 20:45Z, #302 23:00Z, #306 23:39Z).

- **VP-17531** (Done) — **PASS**. PR #297→staging + promotion #298→main; both deploys success.
  Manual prereqs: none. Live evidence AFTER deploy: staging external gateway retest 7/7, prod pod
  dist verified `ALIAS_PRESENT`, prod `POST /v1/orders/cancel` probed without a token → 401 (not 404)
  = route live, zero orders touched. Strongest closure of the batch.
- **VP-17533** (Done) — **PASS**. PR #299→staging + promotion #300→main; deploys success.
  Manual prereq (the VP-17474 failure class) **verified against ground truth, not the PR body**:
  `SHOW COLUMNS FROM hl7_file_input` on prod includes `last_error`. Prod dist contains the
  `last_error` write path. Live evidence: row 6735 recorded its BestDeal 400 into `last_error` on the
  first post-deploy rescan. Post-deploy health clean.
- **PO-256** (Canceled) — **PASS**. Analysis-only oncall triage, no code, no deploy, nothing to
  verify. Root cause is another team's service (RBAC Container App), self-recovered in ~15 min;
  open items handed to Leo per the scope rule rather than self-filed.
- **VP-17532** (Inactive) — **PASS**. Analysis-only, no code change. Conclusion is evidence-backed
  (v2_schedule row-id ordering dates the config rewrite after the last disputed booking); Leo posted
  the explanation himself.
- **LBS-1674** (Done 15:31 PT — "Re-push sample-based PDF reports to Cerbo, Jul 16–24", portal 5492
  / provider 504224) — **FLAG: closed with no verifiable evidence that the re-push ran.**
  - No STM file, no commit in this repo, no Jira comment on the ticket.
  - Ground-truth probe on prod: `result_transmission_records` has **zero** rows for
    `result_client_id` 504224 or 5492 ever; clinic 5492 maps to customers 4953/42579, whose pushes
    today are 13/1/6/3 per hour spread across the day (normal cadence) — **no 9-day backfill burst**,
    and nothing at all in the hour around closure (21:00–23:59Z shows only 4 unrelated single rows).
  - **Inconclusive, not disproof**: sample-based PDF pushes for Cerbo may run through the legacy Java
    EMR-Backend path, which does not write this table. Leo should confirm how it was executed — and
    the class-A rule says "a repush you didn't post-verify is not done".

### Side flag (not a Done ticket, but prod-affecting — read this one)
**VP-17537 + VP-17538 are LIVE ON PROD while Jira says `Dev To Do` / `Dev In Progress`.**
Verified by dist exec on prod pod `...-f85fbb9f6-bhmw2`: `order-request.mapper.js` contains
`accession_id: of.julienBarcode || undefined`, and `charge-client.service.js` contains
`getPaymentMethodCandidates`. Promotion #306 carried both.
Two items the STM had explicitly left open now apply to production code:
1. VP-17538's **wallet priority ORDER is unconfirmed** — the STM says "do not treat the PR as final
   until answered"; no priority definition was found in Jira or Confluence, so the shipped order is
   just charging's array order.
2. VP-17538's **blast radius on the legacy HL7 customerPay path** was flagged as wanting explicit
   sign-off (HL7 orders that used to ship unpaid on a dead first method will now charge a later one).
3. Live customerPay verification for BOTH tickets is still blocked by the charging ACH bug — so this
   is deployed-but-unproven behavior on a money path.

Post-deploy prod health is clean (see below), so this is a governance flag, not an active incident.

### Post-deploy health (emr-v2 prod, since the 23:39Z deploy)
- Pod log last 2h: 6 matches for error/fail keywords, all of them `[SFTP] Metrics: N successful,
  **0 failed**` lines. No exceptions, no 5xx.
- `hl7_file_input`: 2 rows received in 6h, **0 unfinished**; **0 rows stuck** (`parse_finished=0`)
  in the last 24h — i.e. VP-17533's bounded-retry change is not stranding anything.

## Signals
7 files in the working set (incremental since 2026-07-28). Note: 2 of them were missed by a
`^updated: 2026-07-` grep because their dates are YAML-quoted (`'2026-07-29'`) — found via `ls`.

| File | Signal |
|------|--------|
| `BETA-E2E-20260729.md` | `lasting_insight` (completed, substantial lessons) |
| `PO-256.md` | `lasting_insight` (analysis_complete / Jira Canceled, reusable dependency-chain knowledge) |
| `VP-17532.md` | `lasting_insight` (completed / Jira Inactive, method lesson) |
| `HL7FAIL-20260729-PLESSEN.md` | `approaching` (blocked-on-other-team, age 0d) — extracted the settled parts anyway |
| `VP-17537.md` | `approaching` (in_review; **corrected to prod-live**) |
| `VP-17538.md` | `approaching` (in_review; **corrected to prod-live**) |
| `journal/2026-07-29-beta-e2e-cancel-gateway.md` | `undistilled_journal` |
| `journal/2026-07-29-vp17532-availability-vs-booked-events.md` | `undistilled_journal` |

No `stale` and no `overlap` in the working set.

## Operations
- **Distilled journals: 2** — both 2026-07-29 entries → `patterns.md` + `emr-integration.md`;
  frontmatter added (they had **none** — see Notes) and `distilled: true` set.
- **Extracted: 4** — BETA-E2E (gateway passthrough + beta-client token recipe + two-tenant proof →
  `emr-integration.md`), HL7FAIL-PLESSEN (BestDeal panel gap + probe recipe + `last_error` semantics +
  manual BullMQ re-drive → `emr-integration.md`), PO-256 (permission-chain timeout budget + FE
  swallow-error class → `patterns.md`), VP-17532 (config-history dating → `patterns.md`).
  Also added a new `emr-integration.md` section for VP-17537/17538 customerPay payload + payment-method walk.
- **Merged: 0**
- **Updated: 3** — stale claim in `emr-integration.md` ("Gateway route /v1/orders/cancel 尚未由
  api-product 配置；staging E2E 未跑" → now prod-live via VP-17531, and VP-17517's Jira caveat dropped);
  VP-17537.md + VP-17538.md got dream-audit sections recording prod-live status (`updated:` → 2026-07-30).
- **Promoted: 1** — new consolidated `patterns.md` section **「部署驗證的終局判準：exec 進 pod grep dist
  內容」** with a 5-row table of proxy signals that have each lied once (CI success, image tag,
  readyReplicas, single kubectl read ×2, stale local checkout). This absorbs the VP-17497 and
  FHIR-ONDEMAND fragments into one rule; the new fact is that **Jenkins tags with the triggering
  commit but builds branch HEAD at build time**, so the image tag is not a version proof in either
  direction.
- **Cross-ticket review: RAN (new marker 2026-07-29)** — 7 completed since the 2026-07-27 marker
  (VP-17517, BETA-E2E-20260729, VP-17531, VP-17532, VP-17533, PO-256, LBS-1674). Findings written to
  `patterns.md`: (a) "is the code actually running" is the batch's dominant thread → became the
  PROMOTED section; (b) the scope-boundary rule fired **5/5 correctly in one day** (PO-256, BestDeal,
  charging ACH, QA-client OAuth handed off; VP-17531 + VP-17537 self-filed) — rule is settled, the
  residual risk is now that handed-off diagnoses go unfixed (BestDeal still broken, propped up by the
  VP-17535 workaround); (c) Leo's two rejections of the VP-17535 design establish **an interim
  workaround must not hardcode a drifting value — judge a temp fix by whether it can expire silently,
  and pair it with a removal tracker**.
- **Archived: 0** — only VP-16850 met the numeric bar (completed, 2026-06-02, 0.09) but it was cited
  today by VP-17532's exclusion reasoning; archiving a memory the day it proved useful is wrong, so
  held per the be-conservative rule (its auto-links should lift the score anyway). Oldest journal is
  2026-07-02 (27d) — first journal archive is due 2026-08-01.
- **Forgotten: 0** — no archived file is under 0.05, none is older than 180 days.
- **Factory proposals: 1 inbox item, 0 PRs.** `framework/proposals/2026-07-29-verify-artifact-
  content-not-version-label.md` (committed to project-agent-factory main, 61b2af7). It passes the
  standalone and discipline bars but **fails cross-domain evidence** — all 5 cases are the same
  employer domain's backend infra, which is ONE domain. The file records why it stopped short of a PR
  so it can be upgraded when a second domain hits the same class. `ENGINEERING-LESSONS.md` already
  has the narrower rolling-deploy rule; the proposal is written as its successor, not a duplicate.

## Score Statistics
```
Auto-linked: 476 new link entries
Scores and indexes rebuilt
  stm: count=141 highest=0.81 lowest=0.01 median=0.26
  ltm: count=10 highest=1.14 lowest=0.11 median=0.78
  archive: count=25 highest=0.82 lowest=0.05 median=0.48
```
Journal index updated by hand (scoring script does not manage it): 10 entries, 2 new.

## Notes
- **Journal entries were written with no frontmatter at all today.** Both 2026-07-29 files started
  straight at the `#` heading, so `grep "^distilled: false"` returned nothing and the working-set
  build silently missed them — they were only caught by listing the directory. A journal with no
  frontmatter is invisible to the distillation query and would never be distilled. Frontmatter added
  retroactively; the work-loop journal step should be checked against
  `~/project-agent-factory/framework/templates/`.
- Same shape, second instance: two STM files used **quoted** `updated: '2026-07-29'`, which an
  unquoted-date grep misses. Frontmatter date format is inconsistent across writers; the working-set
  query should tolerate both (or the scoring script should normalize on write).
- STM count jumped 135 → 141. Six of tonight's files are same-day incident/session records
  (BETA-E2E, HL7FAIL, PO-256, VP-1753x) — legitimate, not shell padding, but the tier is growing
  ~6/day on busy days and Phase 1's 40-file cap is the only thing keeping this incremental.
- Prod DB access tonight went through the AKS prod pod's Prisma client (`kubectl exec ... node -e`)
  rather than a local mysql client — no VPN needed, and it doubles as the dist-content probe. Worth
  keeping as the default read path for dream audits. First attempt used `created_at` on
  `hl7_file_input`, which does not exist (`received_time` / `updated_time`); columns dumped via
  `SHOW COLUMNS` before re-querying.
- 6 consecutive nights of VP-16832/VP-17117 sitting in QA Review is no longer noise — it is either a
  stuck QA queue or two local statuses that are simply wrong. Recommend Leo resolve it either way so
  the reconcile output stops carrying permanent noise.
