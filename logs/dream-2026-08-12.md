# Dream Log — 2026-08-12

## Ground truth reconciliation

- `scripts/reconcile-jira.py --apply`: **129 in sync, 0 flipped to completed, 0 need manual review**.
- 5 files not found in Jira (unchanged, expected — local incident records with no ticket):
  `INCIDENT-20260518`, `INCIDENT-20260528`, `INCIDENT-20260529`, `INCIDENT-20260604`, `INCIDENT-2604156666`.

## Closeout audit (Done < 24h)

`project = VP AND assignee = currentUser() AND status changed to Done after -30h` → **2 tickets**.
Two prior FLAGs re-verified. Today was the biggest ship day in weeks: **13 PRs merged**
(#343–#355 on lis-backend-emr-v2), covering VP-17685, VP-17686 and VP-17687/17691.

**Deploy chain, verified once for all of today's merges** (two signals per
`feedback_verify_deploy_with_two_signals`):
`kubectl -n emr-v2` → prod pod image tag `7b71b96f3f1b1bc7a7eac778bffdbba306a1ec47` == `origin/main`
HEAD `7b71b96`; staging pod `eb3e55ee…` == `origin/staging` HEAD `eb3e55e`; both pods started
`2026-08-12T21:51:4xZ`, i.e. **after** the last promotion merged (21:41:23Z); Ready, `restarts=0`.
No migration / `.sql` / env / config file anywhere in `bd546b4~1..7b71b96` → **no manual
prerequisites** gating any of it.

### VP-17685 — julien barcode via coresamples v1 → **FLAG: closed on a fix whose working state has never been observed**

Done 2026-08-12 11:41 PT (18:41Z). Audit record written to a **new** `storage/short_term_memory/VP-17685.md`
(the work session left no STM — see Notes).

| Check | Result |
|---|---|
| All merged PRs in window | **Three** code PRs, not one — #343 (18:42Z), #348 (20:20Z), #350 (20:48Z) — each with its own untitled "Staging" promotion (#344/#349/#351). Searched by merge time as well as ticket id; a ticket-id search on the promotions returns nothing. |
| Deploy status | PASS — see the shared verification above. |
| Manual prerequisites | None exist. PASS. |
| Post-deploy health | PASS — prod errors since 21:51Z are startup noise (pg SSL deprecation, redis `allkeys-lru` notice, one `Called end on pool more than once` from the **old** pod terminating) + 3 pre-existing `ECONNRESET`. No new error class. |
| Live verification post-dating deploy | **FAIL — none, ever.** |

Two findings, and the first is the reason this is a FLAG rather than a caveat:

1. **The fix as first shipped could not call the RPC at all.** After #343/#344 were live, staging
   logged `generateBarcodeForSampleID failed for sample 2554096-2554098: client.generateBarcodeForSampleID
   is not a function` ×3 (20:03:53Z → 20:15:06Z). The gRPC client exposes the method under its
   **proto** name; the camelCase alias the code used drops the trailing acronym, so the property was
   `undefined`. #348 fixed only the name. Jira was transitioned Done at 18:41Z — **60 seconds before
   #343 even merged**, and ~2h before the code became capable of working.
2. **The ticket's own acceptance step was never run.** Its description ends "Still to do after
   deploy: place an order on a no-charge path against staging and confirm `hl7_file_input.julien_barcode`
   is populated." Datadog for `generateBarcodeForSampleID|julien_barcode|generateAndSaveBarcode`
   across prod **and** staging since the 21:51Z deploy returns **0 rows of any kind** — no success,
   no failure. The only live evidence this path has ever produced is the three failures above.

Notably, nothing in CI could have caught (1): 375 green specs, clean `tsc`, `nest build` clean, and
the freshly-restored CI gate all passed, because the client is **mocked** in the specs — a mock
answers to whatever name the caller invents. Distilled to LTM (below).

### VP-17656 — restore test/lint guardrails in emr-v2 CI → **PASS**

Done 2026-08-12 10:43 PT; code shipped earlier (#341 → staging 2026-08-10 21:58:25Z, #342 promotion
21:58:58Z). No later PRs.

| Check | Result |
|---|---|
| All merged PRs in window | #341 + #342 only. |
| Deploy status | N/A — the artifact is the pipeline itself. |
| Manual prerequisites | None. |
| Post-deploy health | N/A — no runtime surface. |
| Live verification | **PASS, and unusually strong.** `origin/main:Jenkinsfile:61` has `stage("test")` running `npm run lint:ci` + `npx jest --ci` with **no `when` block** (so: every branch — requirement 2), while `stage("build and push image")` at :86 is gated to main/staging. Since build follows test, the 13 images built today could not exist unless the gate ran green 13 times on real merges. Prod running `7b71b96` is that proof. |

Caveat worth one line: the gate is now load-bearing and it did **not** catch VP-17685's method-name
bug. That is a property of mocked specs, not of the gate — but "CI is green" is now a stronger-sounding
claim than it is.

### VP-17657 / VP-17658 — **FLAG PERSISTS (4th night)**

Both still Done in Jira (2026-08-10 14:44 PT), both re-verified against `origin/main` tonight —
still zero code change:

- **VP-17657** (registry creds → Jenkins credentials store): `Jenkinsfile:103` and `:313` still carry
  `docker login -u 4693646 -p <plaintext>`; `:123` / `:321` still the plaintext ACR access key.
  Committed to git and **un-rotated**.
- **VP-17658** (stale on-prem endpoint docs): `Jenkinsfile:360/363/365` still echo and `nc`-check
  `192.168.60.11`; `docs/` still references it.

This is the 4th consecutive night with no change and no decision. Escalating the ask rather than the
volume: **reopen both, or close them won't-do** — either is fine, but a Done ticket that never
shipped will keep consuming an audit slot every night.

### Dependency propagation

**None due.** Grepped all 148 STM frontmatter for `unblocked_by:` / `unblock_when:` naming
VP-17685 / VP-17656 / VP-17686 / VP-17691 / VP-17657 / VP-17658 → no dependents.

## Signals

- **Working set: 2 files** (incremental since 2026-08-11), selected by frontmatter `updated:`
  (quoted **and** unquoted — last night's `updated: '2026-08-11'` is YAML-quoted and a naive
  `^updated: 2026-08-1` grep misses it).
  - `storage/short_term_memory/VP-17651.md` — `lasting_insight`, already extracted 2026-08-11. No action.
  - `long-term-memory/failures.md` — uncommitted regeneration by `scripts/extract-failures.py`. Inspected
    because the diff looked alarming (−82/+30, `score: 1.1873 → 0.0`): **benign**. The only body change
    is the removal of one empty entry (`VP-16784-87`, "（無 — verification-only session）"), 69 → 68
    entries; the rest is frontmatter `links:` churn. Worth knowing that this script rewrites
    `score:` to `0.0` and resets `created:` to today, which `memory_scoring.py` then recomputes.
- **0 undistilled journals** — all 14 entries are `distilled: true`; newest is still 2026-08-06.
- **Health signal, pre-existing, not from today's deploy**: `Unknown reference range type` on prod
  has fired **111 times 2026-08-06 01:08Z → 2026-08-13 00:59Z** and is still firing. The 2026-08-09
  dream re-probe recorded "0 occurrences in 22h" and read it as consistent with ~1/day — that window
  was the anomaly; the real rate is ~15/day. Appended to `INCIDENT-20260808-critical-result-tnp`.
- **Zero STM and zero journal entries for today's four tickets.** VP-17685, VP-17686 and
  VP-17687/17691 all have code live on prod tonight; VP-17656 was closed. None left a memory record.

## Operations

- **Distilled journals: 0** (none pending).
- **Extracted: 1** → `long-term-memory/patterns.md`, new section
  「【家族延伸 2026-08-12】mock 掉的 collaborator 沒辦法驗證『你叫它的那個名字存在』(VP-17685)」.
  The new angle, checked against the three existing neighbours before writing: line 1145 covers
  *mocking away a dependent path*, line 652 covers *fixtures setting fields the live pipeline never
  sets*, line 1089 covers the three `grpcurl` error strings. None covers **the name itself being the
  mocked thing** — that unit-test green proves caller self-consistency, never that the callee answers
  to that name, so any call across a process boundary whose name does not exist at compile time
  (proto-generated clients, dynamic dispatch, string event/topic keys) needs **one real call** to
  count as verified. Includes the `client.X is not a function` (never left the process) vs
  `Code: Unimplemented` (server has no such method) discriminator.
- **Created: 1** — `storage/short_term_memory/VP-17685.md`. Dream does not normally write STM, but the
  closeout audit's output needs somewhere to live or tomorrow's run re-derives it from scratch (this
  is what last night did inside VP-17651.md; VP-17685 had no such host). Contents are restricted to
  what was verified against ground truth, plus a pointer to the Jira description for design rationale.
- **Updated: 3** — `INCIDENT-20260808-critical-result-tnp.md` (new dated re-probe section, `updated:`
  → 2026-08-12); `journal/_index.md` ("Last updated", plus a note that 4 tickets shipped without
  entries); `patterns.md`.
- **Self-correction inside this run**: the first draft of the INCIDENT append claimed "every one of
  the 111 is `RANGE_ERROR`". Wrong — the pattern search wildcards the type name, so 111 is the whole
  `default:` arm and provably includes the two `RESULT_HIGH_CRITICAL_M` hits already documented in
  that same file. Only the 6 occurrences in the last 2 days were read individually and are all
  `RANGE_ERROR`. Corrected before commit, and the file now says the **per-label split, not the total,
  is the number to act on** — because `RANGE_ERROR` may legitimately be TNP while critical-high/low
  is the patient-facing defect. On a patient-safety record an overclaim is worse than no claim.
- **Merged: 0** / **Promoted: 0** / **Archived: 0** / **Forgotten: 0** — nothing met the criteria.
- **Cross-ticket review: not due.** Last one 2026-08-09; `completed` since then = VP-17651, VP-17653,
  VP-17685 (3) — below the 5 threshold. Will likely fire within days given today's pace.
- **Factory lesson PRs: 0. Inbox proposals: 0.** The mock-seam lesson is genuinely good and reads
  close to standalone, but it fails the **cross-domain-evidence** bar: every supporting case
  (VP-17685, VP-17260, VP-17286/17411) is from this one employer-domain, and it leans on
  proto/gRPC specifics that do not survive de-identification cleanly. Held in this repo's LTM.
  Default is do-not-promote and that is the correct outcome here.

## Score Statistics

```
Auto-linked: 187 new link entries
Scores and indexes rebuilt
  stm: count=148 highest=1.01 lowest=0.01 median=0.21
  ltm: count=10 highest=1.21 lowest=0.11 median=0.80
  archive: count=38 highest=0.77 lowest=0.03 median=0.11
```

STM 147 → 148 (VP-17685). Top score moved 0.94 → 1.01
(`INCIDENT-20260808-critical-result-tnp`, correctly — it gained fresh evidence tonight).

## Notes

**Carry forward to Leo (digest top section):**

1. **VP-17685 is Done but has never been seen working.** Its own post-deploy step — place a
   no-charge order on staging, confirm `hl7_file_input.julien_barcode` populated — is unrun, and the
   only live evidence so far is the three `is not a function` failures from the pre-#348 build. One
   staging order settles it. This is the single highest-value 10 minutes on the list.
2. **VP-17686 and VP-17691 have code live on prod but Jira says "Dev To Do" / "Dev In Progress".**
   The usual lag runs the other way (Done before deployed); this is the inverse and it is the
   riskier direction, because a reader concludes the fix is not out yet. Dream does not transition
   tickets — flagging for Leo. (Also unstated anywhere but the VP-17686 description: the six
   `order_intake` rows already stuck at `received` on staging stay stuck — the fix only prevents new
   ones — and a prod audit found 61 samples with BestDeal drops, handed to the order team.)
3. **VP-17657 / VP-17658 — 4th night.** Plaintext registry credentials still in git, still
   un-rotated. Needs a decision, not another audit line.
4. **`Unknown reference range type` fires ~15x/day on prod, not ~1/day** as the 08-09 re-probe
   suggested. Still unticketed, still awaiting Leo. Read the per-label split, not the total: the
   frequent label (`RANGE_ERROR`) may be legitimate TNP; the rare one (critical-high/low) is the one
   masking real patient values.
5. **25 undelivered reports: unchanged, 5th day.** `DailyJob/result_fail/report_2026-08-12.md` lists
   the same 25 as 08-08 through 08-11 — backlog flat, confirming these are one-off victims of the
   08-06/08-07 browser-pool bursts, not an ongoing leak. All at `PENDING/PENDING` or
   `GENERATING/PENDING` with **no error string**, so nothing will ever schedule a retry. Cause has
   passed; safe to re-drive, but per ghost-rescue discipline verify each sample's current delivery
   state first — result push has no idempotency gate, so a blind replay re-delivers the ORU.
   **Dream does not re-drive prod deliveries. Leo decides.**
6. **HL7 triage clean** — `DailyJob/hl7_fail/triage_2026-08-12.md`: no failed records.

**Process observation (the one worth acting on):** today shipped 13 PRs across 4 tickets and left
**no STM and no journal entry** — the largest single-day output in weeks is, as far as memory is
concerned, invisible. Every fact in tonight's audit had to be re-derived from GitHub, kubectl and
Datadog. Two concrete costs already visible: VP-17685's audit had no STM to attach to (dream created
one, which it should not have to), and the mock-seam lesson would have been lost entirely had the
closeout audit not happened to run the same night. The work loop's per-ticket STM is what feeds LTM;
skipping it does not defer the cost, it deletes the knowledge.

**Repo hygiene — resolved mid-run, no action needed.** At the start of this run, commit `573747a`
("Make git-push branch guard repo-aware and fail-safe" — `.claude/hooks/validate-git-push.sh`, its
test, `src/agent/permissions.py`) sat on `feature/leo/git-branch-guard-workrepos` with no open PR,
which this repo's CLAUDE.md requires for automation-behaviour changes. Dream committed tonight's
memory output to `main` rather than onto that branch, to keep the unreviewed guard change isolated.
It then landed as **PR #22** while this run was in progress, so `main` now carries it and tonight's
dream commit is rebased on top. Recording the sequence only because the isolation decision above
would otherwise look arbitrary.

**Memory state:** healthy. STM 148, median 0.21. Working set stayed at 2 files — the incremental
selection is holding, but note the YAML-quoting trap in the `updated:` grep for whoever writes the
next Phase 1.
