# Dream Log — 2026-08-05

> Run started 18:34 PDT on **attempt 3/3** — attempts 1 and 2 both died on
> `API Error: Connection closed mid-response`. Working set incremental since **2026-08-04**.
> No work session happened today (no STM has `updated: 2026-08-05`, no new journal), so the
> memory-consolidation half of this run is genuinely near-empty. The substance tonight is
> automation health.

## ⚠️ Automation health — one root cause behind all three jobs

**Every one of the three automations failed today with the same API error signature.** This is
not three separate config bugs; it is platform-level API instability.

| Job | Fires | Today's outcome |
|---|---|---|
| dream (6:30 PM) | ✅ nightly | attempts 1+2 `Connection closed mid-response`; **attempt 3 = this run** |
| HL7 triage (4:00 AM) | ✅ nightly | attempt 1 `Connection closed mid-response` (04:13→06:35), attempt 2 `Request timed out` (07:36→11:18) → **no `triage_2026-08-05.md`** |
| daily digest (00:00) | ✅ nightly | attempts 1+2 `Connection closed mid-response` (00:13→06:03) → `final rc=1` |

Notes that matter:

1. **The digest is NOT dead, and the last three dream logs were wrong about it.** 08-01, 08-04,
   and my own first read tonight all concluded "digest dead / still silent" from a single piece
   of evidence: `long-term-memory/daily-digest/` newest is still `2026-07-29.md`. I then found
   launchd's `StandardOutPath` is **0 bytes, mtime 6/23, never written** and nearly recorded
   "this job has never executed." Both readings are wrong. The script keeps its **own** per-run
   log at `~/.lis-daily-digest/main/logs/daily-digest/YYYY-MM-DD_HHMMSS.log`, and there is one
   for **every night 07-30 → 08-05**. The job fires, runs `claude`, and dies on the API.
   → Distilled into `patterns.md` as variant **5** of the 2026-08-04 family entry (see
   Operations). Worth noting the family's first hit came from *dream's own diagnosis*, not ticket
   work.
2. **HL7 triage today was not VPN-blocked** — pre-flight explicitly logged
   `Pre-flight OK: lisportalprod2...:3306 reachable`. So the usual VPN hypothesis does not apply;
   it was purely the API. Last night's "HL7 triage RECOVERED" has regressed after one day.
3. **These retries burn hours.** Digest 00:13→06:03 (5h50m), triage 04:13→11:18 (7h05m across two
   attempts, one of which hung until "Request timed out"). The retry loops have no per-attempt
   wall-clock cap, so a mid-response hang consumes the whole morning.
   → Suggest a per-attempt timeout in `run_triage.sh` / `daily-digest.sh`. **Not changed
   tonight** — both are automation-behaviour files and per CLAUDE.md must go through a PR.
4. **`failures.md` has now been clobbered by the retry/concurrency race twice.** Tonight I found
   it with the exact signature commit `90b581c` (2026-07-29) already documents: *"a losing dream
   instance overwrote failures.md with a score=0.0 stub at 19:39:43, **after** the good commit."*
   The 08-04 state matched — `score: 0.0`, `created:` bumped to the run date, `links:` truncated
   60→9 — except the body was **not** a stub: it held the full, genuine VP-17544/VP-17591
   distillations. So it is a *partial* clobber (frontmatter regenerated from scratch, content
   preserved), and it landed after the good commit, which is why the content was never committed.
   → Two things follow. **(a)** This is a data-integrity bug in the retry design, not bad luck:
   an attempt that dies mid-response has already written files, and the next attempt does not
   know that. The 3-attempt loop that saves the run also endangers it. **(b)** `failures.md` is
   the file hit both times — it is the largest and most-linked LTM file, so it is worth checking
   `git status` for it specifically at the start of every run. Tonight's own attempts 1 and 2 both
   died inside Phase 0.5, before any LTM write, so nothing of mine is at risk.
   → **Not fixed tonight** (retry loop lives in `run-dream.sh` = automation behaviour = PR-gated).
   Cheapest fix is probably a lock/`git stash` guard or having each attempt commit its own work
   before exiting.
5. **Still open from 08-04 and not addressed** (same PR-gated reason): `dream.md` Phase 0.5
   should pin the `AFTER -24h` JQL form; the `DURING (-30h, now)` form silently returns zero.

## Ground truth reconciliation

- `reconcile-jira.py --apply`: **134 in sync / 0 flipped / 0 need manual review.**
- 5 local ids absent from Jira (`INCIDENT-*`) — expected, agent-internal.

## Closeout audit (Done < 24h)

**None due.** Verified with the working `AFTER` form:
`project = VP AND status CHANGED TO (Done, Closed, Resolved) AFTER -30h` returned 19 issues, of
which **5 are agent-owned** (VP-17544, VP-17587, VP-17591, VP-17593, VP-17595) — all with
`resolutiondate` 2026-08-04 15:13–15:26 PDT, i.e. **before** last night's 18:30 run, which
audited all five as PASS against deploys, manual ConfigMap/DSN prerequisites, and live
post-deploy probes. Nothing agent-owned has closed since. The other 14 belong to other
assignees (Fangyuan Yang, Zhiheng Wu, Yekai Liu, Jiafan Qian, Fan Zhou, Zhibin Chen, Rui Chen)
and are out of scope.

- Carried forward, unchanged and still correct: **VP-17591's reported symptom is not fixed** —
  the empty address originates in billing, outside emr-v2. Leo closed it knowingly.

**Dependency propagation:** no-op. Grepped all 152 STM files for `unblocked_by:` and
`unblock_when:` naming 17544 / 17587 / 17591 / 17593 / 17595 — zero dependents. The three live
`unblock_when:` blockers (BIOINSIGHTS vendor perms, PLESSEN BestDeal panel 18019, VP-17475
pricing catalog API) are all unrelated.

## Signals

**1 file in working set** (incremental since 2026-08-04).

| Signal | Files |
|---|---|
| journal `ARCHIVE` (distilled + >30d) | `2026-07-02-report-finished-pre-golive-drop` (34d) |
| `lasting_insight` / `completed` / `approaching` | none new |
| `undistilled_journal` | **none** — all 14 journal entries are `distilled: true` |
| `stale` / `overlap` | none |

The six STM files with `updated: 2026-08-04` (VEJO-DELETION-20260804, VP-17589, VP-17591,
VP-17593, VP-17594, VP-17595) were all inside last night's 13-file working set and are already
processed. `find -newermt` lists ~150 STM files as modified since 18:30 — that is Phase 4
rewriting `score:` in frontmatter, **not** content change. Frontmatter `updated:` is the only
trustworthy signal here, exactly as dream.md prescribes.

## Operations

- **Distilled journals: 0** (none undistilled).
- **Extracted: 0** new from STM.
- **Promoted: 1** → `patterns.md`, appended as **variant 5** of the 2026-08-04 family entry
  「某環節看起來正常，但沒驗證它真的在做我以為的事」: *"scheduled job is dead" inferred from
  output absence, when the job runs nightly and fails on the API — find the job's own log before
  concluding, because launchd's stdout is often permanently empty and empty looks like
  never-ran.* Placed in the existing family rather than as a standalone bullet, since the whole
  argument of that entry is that ungrouped variants don't fire.
- **Recovered: 1 clobbered file — second occurrence of a known race, see below.**
  `long-term-memory/failures.md` sat uncommitted with a **+352/−101** diff carrying the real
  VP-17544 and VP-17591 distillations. It was absent from last night's 193-file commit `4aed1f6`
  (which *did* include `emr-integration.md` and `patterns.md`), so the extraction survived only
  in the working tree. Committed tonight.
- **Updated: 1** → `failures.md` frontmatter `created:` restored `2026-08-04` → **`2026-07-29`**
  (its true creation date). `score` was `0.0` and `links:` truncated from ~60 entries to 9; both
  regenerated correctly by tonight's rescore.
- **Merged: 0 / Resolved: 0.**
- **Cross-ticket review: not due** — ran last night (2026-08-04, 7 completed since the
  2026-07-31 marker). Marker is now **2026-08-04**; next review at 5 more `completed`.
- **Archived: 1** → `journal/2026-07-02-report-finished-pre-golive-drop.md` →
  `archive/journal/` (distilled, 34d), `archived: 2026-08-05` stamped, index line annotated.
  `archive/journal/` did not exist and was created.
- **Forgotten: 0** — zero archive files meet `score < 0.05 AND age > 180d AND
  category != emr_integration`.
- **STM archive: 0** — zero files meet `completed AND >30d AND score < 0.1`.

## Factory routing

**No lesson PR, no inbox proposal.** Tonight's one insight (variant 5) fails the cross-domain
bar: its evidence is three dream logs about one automation set — a single domain, and arguably a
single subsystem. It is also close kin to inbox item
`2026-08-04-verification-criteria-fail-toward-pass.md` already awaiting Leo's triage. Default is
DO NOT PROMOTE and that is the correct outcome here.

## Score Statistics

```
Auto-linked: 49 new link entries
Scores and indexes rebuilt
  stm: count=152 highest=0.95 lowest=0.01 median=0.22
  ltm: count=10 highest=1.19 lowest=0.11 median=0.80
  archive: count=26 highest=0.79 lowest=0.04 median=0.46
```

`failures.md` rescored 0.0 → **1.1873** (highest in LTM), confirming the zeroed score was an
artifact of last night's write ordering, not a real decay.

## Notes

- **The most useful thing tonight was catching my own repeat of the shape distilled last night.**
  Three consecutive dream logs asserted a dead job from absent output; I was one step from
  writing a fourth, with *stronger-sounding* evidence (a 0-byte launchd log) that was equally
  circumstantial. The fix was one `ls` in the script's own log directory. The family entry now
  has a variant whose source is dream's own reasoning, which is the case most likely to recur —
  nobody reviews dream's diagnoses the way Leo reviews ticket work.
- **Two nights running, dream has needed a retry to complete** (08-02, 08-03 died entirely;
  08-05 needed attempt 3). The 3-attempt loop is doing real work — without it there would be no
  log tonight. But the same instability silently cost today's HL7 triage report and today's
  digest, and neither has a retry budget that outlives a mid-response hang.
- **For Leo, in priority order:** (1) no `triage_2026-08-05.md` — today's HL7 failures were never
  reviewed; (2) `failures.md` has been silently clobbered by the dream retry race **twice**
  (07-29, 08-04) and last night's distillation was one `git checkout` away from being lost —
  this is the one item here that destroys data rather than just skipping work; (3) the digest has
  been failing every night since 07-30 for an API reason, not a config reason, so nothing needs
  "fixing" in the digest itself; (4) four PR-gated automation changes are queued and blocked on
  review: per-attempt timeouts in the two job scripts, a write guard in `run-dream.sh`, and the
  `dream.md` Phase 0.5 JQL pin.
- Open non-blocking items carried forward unchanged: emr-v2 + transv2 **staging DBs still on
  192.168.60.11** (decom blocker awaiting infra decision), ClickHouse 192.168.62.85 / CDC feed
  ownership unconfirmed, transv2 carlos brokers pending removal of remaining dual-publish legs.
