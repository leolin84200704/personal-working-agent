# Dream Log — 2026-08-14

## Ground truth reconciliation

- `python3 scripts/reconcile-jira.py --apply`: **132 in sync, 0 flipped, 0 needing manual review.**
- 5 files not found / no access in Jira, all expected (locally-coined incident ids, no Jira issue):
  `INCIDENT-20260518`, `INCIDENT-20260528`, `INCIDENT-20260529`, `INCIDENT-20260604`,
  `INCIDENT-2604156666`.

## Closeout audit (Done < 24h)

Three tickets reached Done in the window. Note the JQL trap: `assignee = currentUser()` **missed
VP-17715 entirely** because it is unassigned — the unfiltered project-wide query is what surfaced it.
Filtering the closeout audit by assignee will silently skip unassigned tickets that the agent shipped.

- **VP-17715** (PER_REPORT_GROUP push level) — **PASS.** Fully re-verified against prod, not against
  the STM: image `lisportalprod.azurecr.io/lis-backend-emr-v2:52282e3f…` == `origin/main` HEAD
  `52282e3`; second signal `GROUP:MAIN` present in `dist/…/kafka-report-finished-listener.service.js`;
  DDL live (`deferred_report_short_names` queryable and populated); config
  `c0000CMYCWWC500T2I1A85BHS` = PER_REPORT_GROUP / `["GUT5"]` / result_enabled=1 / LIVE; E2E row
  persisted (`cmstb9o0k0001zr08hlhvpwak`, sample 2597140, `GROUP:MAIN`, TRANSMITTED 19:00:18Z); test
  integration correctly retired (`result_enabled=0`). Health since deploy: **612 transmissions in 30h,
  all TRANSMITTED, zero failures**; 3 prod error lines in 24h, all benign kafkajs rebalance at pod start.
  This is the cleanest closure chain in the recent set — see the cross-ticket review below.
- **VP-17723** (PM-side "report should come through ONLY TWICE") — **PASS by proxy.** No code of its
  own; it is the customer-requirement ticket that VP-17715 implements, closed after the config flip.
  Its acceptance is VP-17715's live config, verified above. Shares VP-17715's residual watch.
- **VP-17691** (partner API error envelope + request id + chargeIndicator validation) —
  **FLAG: no STM, no post-deploy live verification.**
  - Code is genuinely live: PRs #352→#353 and #354→#355 merged 2026-08-12 21:19–21:41Z;
    `INVALID_CHARGE_INDICATOR`, `partner-request-id.middleware.js` and `partner-api-error.filter.js`
    all present in the running prod `dist`. Deploy is not in doubt.
  - What is missing is acceptance. The ticket's verification section is **jest + tsc only**. Prod pod
    logs show **zero partner API traffic in 24h**, so there is no organic evidence either. The change
    is behaviour-changing: an integrator sending a non-`C` `chargeIndicator` previously got a silent
    patientPayLater 201 and now gets a 400.
  - The **2026-08-12 dream already flagged this ticket** for having no STM. Two days later it was
    transitioned Done and the STM still did not exist. Dream wrote one tonight
    (`storage/short_term_memory/VP-17691.md`), reconstructed from Jira + the merged PRs + tonight's
    live prod probe, explicitly marked as reconstructed and with the unverified parts named.

**Dependency propagation:** no STM carries an `unblocked_by:` or `unblock_when:` naming VP-17715,
VP-17691 or VP-17723. Nothing to re-check, no digest lines owed.

**Not escalating.** The one FLAG is a verification gap on a low-traffic surface, not active breakage —
all health signals on the touched service are clean.

## Signals

Working set: **6 files** (incremental since 2026-08-12) — plus one that the selection missed, below.

| File | Signal | Action |
|---|---|---|
| `journal/2026-08-14-vp17714-clinician-switch-zoom-room.md` | `undistilled_journal` | distilled → `patterns.md`, marked `distilled: true` |
| `journal/2026-08-14-vp17715-report-group-push.md` | `undistilled_journal` (**missed by the grep**) | distilled → `emr-integration.md` + `patterns.md`; frontmatter added |
| `storage/short_term_memory/VP-17715.md` | `lasting_insight` | extracted → `emr-integration.md`; closeout audit appended |
| `storage/short_term_memory/VP-17493.md` | `lasting_insight` | extracted → `emr-integration.md` (2026-08-13 escalation section) |
| `storage/short_term_memory/VP-17685.md` | `lasting_insight` | lesson 1 already in LTM; **lesson 3 extracted tonight**; resolved a contradiction it created |
| `storage/short_term_memory/VP-17714.md` | `approaching` | active, created today, PR #565 not deployed. No action |
| `storage/short_term_memory/INCIDENT-20260808-critical-result-tnp.md` | `approaching` | re-probed, appended |

**Selection bug worth fixing:** `2026-08-14-vp17715-report-group-push.md` has **no frontmatter at
all**, so both the `distilled: false` grep and the journal index skipped it. It was only found when
the index was rebuilt from the directory listing rather than from the old index. A journal entry
without frontmatter is invisible to this pipeline — it would have been distilled never. Frontmatter
added retroactively; the index now carries a warning. Last night's log flagged the YAML-**quoting**
trap in the same grep; this is the same class one level worse (no key at all to quote).

## Operations

- **Distilled journals: 2** (both 2026-08-14 entries).
- **Extracted: 4** — VP-17715 (PER_REPORT_GROUP mechanism + contract), VP-17493 (event-firing
  semantics + content verification without ground truth), VP-17685 lesson 3 (non-fatal catch),
  VP-17714 (path-fingerprint discipline).
- **Merged: 0.** **Promoted: 0** (no pattern hit 3+ new STM files).
- **Updated / Resolved: 2**
  - `patterns.md` line ~1197: the "staging quirk — `generateBarcodeForSampleID` fails, prod healthy"
    note is **struck through and annotated**. VP-17685 falsified it twice: the tzdata root cause is
    gone (barcode moved to coresamples v1 on 2026-08-12), and "prod healthy" was never true — prod
    failed on every call too, swallowed by a non-fatal catch. New state is honestly recorded as
    *unknown*, since Datadog returns zero rows of any kind for that path post-fix.
  - `INCIDENT-20260808-critical-result-tnp`: re-probed. **7 occurrences in 48h, 100% `RANGE_ERROR`,
    zero critical-high/low labels.** That is ~3.5/day against the ~15/day implied by the 08-12
    reading — the third window in a row to give a materially different rate. Recorded that the rate
    is not extrapolatable and, more importantly, that the drop does **not** mean the defect went
    away: the missing critical-high/low switch arms are simply rare, which is exactly the
    "per-label split, not the total" trap this file already warns about.
- **Cross-ticket review: RAN** (marker was 2026-08-09; 5 completed since: VP-17651, VP-17653,
  VP-17685, VP-17691, VP-17715). New marker: **2026-08-14**. Written to `patterns.md`.
  The finding: 07-31's review concluded "Done is treated as the first step of the process"; this
  batch agrees but **replaces the predictor**. What predicts a clean closure is not ticket quality or
  change size — it is **whether a live STM existed during the work**, and the ordering is monotonic:
  VP-17715 (STM written live, agent walked the chain before transitioning) is the only all-green one;
  VP-17685 (STM written after the fact by an audit) had Done land 60s before the fix merged;
  VP-17691 (no STM at all) had to be reconstructed two days later. Third sub-finding: **half of what
  closeout audits catch belongs to a neighbouring ticket** (VP-17653's night caught VP-17657/17658;
  tonight caught VP-17691) — batch Done-transitions are a risk signal independent of ticket content.
- **Archived: 5** — STM `QH-1860`, `VP-16413`, `VP-17407` (completed, >30d stale, score <0.1);
  journal `2026-07-13-vp17286-e2e-staging`, `2026-07-14-payment-chain-closeout` (distilled, >30d).
- **Forgotten: 0** (no archived file met score <0.05 AND age >180d AND non-`emr_integration`).
- **Factory lesson PRs: 0. Inbox proposals: 1.**
  `framework/proposals/2026-08-14-instrument-the-success-path-too.md`, committed and pushed to
  project-agent-factory `main` (`48b8de8`). Lesson: *a permanently silent success and a permanently
  silent failure look identical* — wrapping a call in a non-fatal catch deletes your ability to know
  it ever worked, so the success path needs a signal too. It reads fully standalone and prescribes a
  discipline, but **fails the cross-domain bar** (both supporting cases are from one employer
  domain), so it went to the inbox rather than a PR, as the rule requires.
  Factory repo housekeeping: it was parked on `feature/leo/doc-drift-closure` (1 commit ahead of
  main, unmerged). Dream checked out `main`, committed the inbox item, pushed, and **restored the
  branch**. Note the factory lives at `~/src/project-agent-factory`; `~/project-agent-factory` does
  not exist on this machine, contrary to what several docs still say.

## Score Statistics

```
Auto-linked: 420 new link entries
Scores and indexes rebuilt
  stm: count=148 highest=1.04 lowest=0.01 median=0.21
  ltm: count=10 highest=1.24 lowest=0.11 median=0.81
  archive: count=41 highest=0.76 lowest=0.03 median=0.08
```

STM count held at 148 (−3 archived, +1 new `VP-17691` reconstruction, −1 net elsewhere). Median 0.21,
unchanged from last night — the archive sweep is keeping pace with intake.

## Notes

**Residual watch, carried to tomorrow.** VP-17715's own open item 4 asked for exactly this check and
it is **not yet satisfiable**: the only `GROUP:MAIN` record in prod is the E2E replay under the
now-retired test integration. **No organic main-group push has fired for the real cust-4953
integration.** That is expected — only ~5h since the flip, and no 4953 order has completed its
non-GZ report set in that window — but the feature's real-world behaviour has still never been
observed. This is the same shape as VP-17685's "right fix, correctly deployed, acceptance step
unperformed", so it is worth not letting it drift. Re-check tomorrow night:
`result_transmission_records` where `push_scope_key = 'GROUP:MAIN'` and
`integration_request_id = 'c0000CMYCWWC500T2I1A85BHS'`.

**For the digest (top section).**
1. `VP-17691`: Done + live on prod, but no live probe has ever run and the STM had to be written by
   dream. One partner-surface call (valid indicator / invalid indicator → 400 + code / caller-supplied
   `x-request-id` echoed) closes it. Its doc-drift items are also still open.
2. `VP-17715`: shipped clean; watch for the first organic `GROUP:MAIN` push for cust 4953.
3. `INCIDENT-20260808-critical-result-tnp` remains **unticketed and awaiting Leo** — 7 days now. The
   log noise has dropped but the patient-facing defect (critical-high/low delivered as "Test not
   done") is untouched.
4. **Both morning jobs were BLOCKED today** (`DailyJob/hl7_fail/triage_2026-08-14.md`,
   `DailyJob/result_fail/report_2026-08-14.md`): prod MySQL unreachable from this machine at
   06:44 PDT, VPN down overnight as usual. Both files correctly say **"this is NOT a no-failures
   result"**. Nobody has looked at today's HL7 intake or result-delivery failures.
   Yesterday's run — the last real data — found 1 Type A (`emr_code_not_found`, record 6848,
   Parsley Health LA) plus 23 undelivered reports and a Cascades destination failing since 08-06.
   **Dream could not substitute.** The pod-side DB path that worked all night for
   `ehr_integrations` and `result_transmission_records` does **not** work for `hl7_file_input`:
   every query against that table timed out at 150–180s, including a plain
   `ORDER BY id DESC LIMIT 20`. So the triage gap is real and still open — re-run both jobs after
   reconnecting the VPN. (Worth noting for whoever tunes those jobs: "query prod from the AKS pod"
   is a genuine VPN-free path for the integration tables, but it is not a general one.)

**Memory-state observation.** Two of the last three nights, the thing dream had to fix was not a
memory that had gone stale but a memory that **never got written** — VP-17691's missing STM, and
tonight a journal entry with no frontmatter that the pipeline could not see. The incremental
selection in Phase 1 is efficient precisely because it trusts frontmatter, which means a file
written without it is not merely unranked, it is invisible. Worth considering a cheap guard: a Phase
1 pre-step that lists `journal/*.md` and `storage/short_term_memory/*.md` and reports any file whose
first line is not `---`. That is a change to automation behaviour, so per this repo's CLAUDE.md it
would need a PR rather than a direct commit — flagging it here for Leo rather than doing it.
