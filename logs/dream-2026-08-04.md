# Dream Log — 2026-08-04

> Run started 18:30 PDT. Working set incremental since **2026-08-01** — the last run that
> actually finished. The 08-02 and 08-03 runs both died on API errors (see Automation health);
> nothing was lost, because the window is computed from the newest dream log, not the calendar.

## ⚠️ Automation health

1. **Two dream runs failed silently (08-02, 08-03).** Combined with 07-31, that is **3 of the
   last 5 nights**.
   - 08-02: all 3 attempts `API Error: Unable to connect to API (ENOTFOUND)`. Same signature as
     07-31, and again the readiness probe logged **"Network ready (attempt 1)"** immediately
     before the first failure — the check still passes on something weaker than resolving the
     API host.
   - 08-03: all 3 attempts `API Error: Connection closed mid-response` — a *different* failure
     (the run started, did real work, then got cut off mid-stream). Attempt 1 got as far as
     Phase 0.5 before dying.
2. **The 08-03 run left behind a real finding worth keeping.** Its own transcript says:
   > "Jira's `status changed to Done DURING` JQL returned zero — but 10 of Leo's tickets
   > resolved today. The prescribed audit query is producing false negatives."
   Confirmed tonight: `status CHANGED TO (Done, Closed, Resolved) AFTER -24h` works correctly
   (returned 21 issues, 5 of them Leo's). **The `DURING (-30h, now)` form used by the
   2026-08-01 run is what silently returns zero.** The 08-01 log recorded "None due" on the
   strength of that query — that "None due" should be treated as unverified, not as evidence.
   → dream.md Phase 0.5 should pin the `AFTER -24h` form. Not changed tonight: `dream.md` is an
   automation-behaviour file and per CLAUDE.md must go through a PR, not a direct main commit.
3. **Daily digest still dead** — `long-term-memory/daily-digest/` newest is still `2026-07-29.md`
   (6 days). Unchanged since the 08-01 log flagged it. Still silent.
4. **HL7 daily triage RECOVERED** — `triage_2026-08-02/03/04.md` all present after 3 consecutive
   blocked days. No longer an open item.

## Ground truth reconciliation

- `reconcile-jira.py --apply`: **134 in sync / 0 flipped / 0 need manual review.**
- 5 local ids absent from Jira (`INCIDENT-*`) — expected, agent-internal.

## Closeout audit (Done < 24h)

Five agent-owned tickets reached Done in the window. **All five PASS.** Every merge, every
deploy run, and every manual prerequisite was checked against ground truth, not against the
STM's plan of record.

- **VP-17544** (DOB/Sex interception + Sentry restore): PASS — emr-v2 #313→#314 and #315→#317,
  all four GH Actions runs `success`. Manual prereq **verified applied**: `SENTRY_DSN` +
  `SENTRY_ENVIRONMENT` patched into BOTH live ConfigMaps (prod 149→151 keys), pod log flipped
  from `SENTRY_DSN not configured` to `Sentry initialised (environment=production)`. Live
  evidence post-deploy: e2e through the pod's own compiled `dist` → `flush result: true`, and
  Leo confirmed receipt in `#emr-orders-bot`.
- **VP-17587** (alerting live): PASS — no separate STM; fully documented inside VP-17544. Resolved
  by reusing existing Sentry project 49 rather than provisioning a new one, which cleared a
  blocker that had held for a day.
- **VP-17591** (patient address / NY routing): PASS — #316→#317, both runs `success`. Live
  post-deploy probe at 19:16Z: `hl7_file_input id=6788` parsed clean on pod `777c956c9b`
  (≠ the AKS hash, so the on-prem pod is confirmed updated too), `retry=5` undecremented,
  empty error + empty code = no mis-interception, no `NY_ADDRESS_REQUIRED` false trigger.
  ⚠ Note, not a flag: the **reported symptom is not fixed** — the empty address originates in
  billing, outside emr-v2. Leo closed it knowingly; the STM says so explicitly.
- **VP-17595** (result consumer cloud-only): PASS — #318→#319, both `success`. Prod pod log
  `✅ Kafka consumer running on cloud cluster, topic=general-sample-events` under group
  `emr-result-consumer-cloud-production`. Post-deploy manual step verified: `KAFKA_BROKERS` +
  `KAFKA_TOPIC` deleted from both live ConfigMaps, cloud keys intact.
- **VP-17593** (booking verification email → Azure hub): PASS — transv2 #551→#553, all runs
  `success`. Staging E2E was a **full real chain** (API 201 → Azure hub → consumer → Postmark →
  Leo's inbox). Prod-side verification was dist inspection only, no prod send — acceptable here
  precisely because staging and prod **share the same Event Hub and topic**, so the staging E2E
  already exercised the prod consumer path. Recorded rather than flagged.

**VP-17594** correctly has nothing to audit: dropped to Inactive, PR #552 closed unmerged.

**Post-deploy health (probed live, not inferred):** emr-v2 prod pod up 3h17m / 0 restarts,
**zero real errors in 3h** (the single grep hit was the substring `exception` inside a
`customer_exception_net30` setting name). transv2 prod: 3 pods, 3h29–31m, 0 restarts.

**Dependency propagation:** no-op. Grepped all 152 STM files for `unblocked_by:` entries and
`unblock_when:` text naming 17544 / 17587 / 17591 / 17593 / 17595 — **zero dependents**. The
three live `unblock_when:` blockers (BIOINSIGHTS vendor perms, PLESSEN BestDeal panel, VP-17475
pricing catalog API) are all unrelated to tonight's shipments.

> Tooling note for future runs: `timeout` does not exist on macOS. Two of my first pod probes
> returned empty and looked like "no such pods" — they were `command not found`. Same shape as
> the lesson distilled tonight: a tool that silently does nothing reads as a negative result.

## Signals

**13 files in working set** (incremental since 2026-08-01).

| Signal | Files |
|---|---|
| `lasting_insight` | VP-17544, VP-17591 (both with substantial Retrospective + Lessons) |
| `completed` | VP-17537, VP-17538, VP-17561, VP-17593, VP-17595, VP-17412, VEJO-DELETION-20260804 (VP-17594 = dropped; VP-17475 = done, frontmatter `updated:` not bumped by its edit) |
| `approaching` | VP-17589 (active, created today — clinic 128087 discount) |
| `undistilled_journal` | 2026-08-03-blocked-verdict-shelf-life, 2026-08-04-dob-sex-interception-and-address-chain, 2026-08-04-onprem-decom-audit |
| `stale` / `overlap` | none |

VP-17412 and VP-17589 have `## Lessons Learned` headings with **empty bodies** — nothing to
extract; not treated as `lasting_insight`.

## Operations

- **Distilled journals: 3** (all three flipped to `distilled: true`).
- **Extracted: 2** → `emr-integration.md` (HL7 demographics interception: the Sex→`Male` silent
  normalize that makes the downstream blank-check dead code; DOB mis-tagged as
  `emr_code_not_found`; the four-layer address responsibility chain with three different
  criteria; the HL7-only NY rule and why it is stricter than a DB fallback; Sentry project 49 as
  the real alerting path; on-prem Kafka decom state incl. the `envFrom` vs `configMapKeyRef`
  cleanup hazard and the shared staging/prod notification hub) and `patterns.md`.
- **Promoted: 1** → `patterns.md` **「家族蒸餾」**: four same-shaped failures in one day
  ("something looked normal but I never verified it was doing what I assumed"). The point of the
  entry is the meta-observation: the individual lessons existed and **none of them saved the
  next one**, because they were never grouped as one named family.
- **Merged: 0 / Updated: 0** (nothing contradicted; no stale dates found).
- **Cross-ticket review: RAN** (marker was 2026-07-31; 7 completed since: VP-17537, VP-17412,
  VP-17544, VP-17587, VP-17591, VP-17593, VP-17595, plus VEJO-DELETION-20260804). Three
  systemic patterns recorded, the sharpest being: **the missing capability had usually been
  *lost in migration*, not never built** — true for Sentry reporting, for setting.service's
  never-provisioned Azure leg, and for the on-prem fallback that had decayed into a path that
  masked cloud failures.
- **Archived: 0 / Forgotten: 0** — zero files met the thresholds (completed AND >30d AND
  score <0.1). Oldest journal is 2026-07-14, well inside the 30-day window.

## Factory routing

**No lesson PR tonight** — correct outcome, not an omission. Three lessons from this work
(`void` doesn't discard rejections / search flags degrade silently / repo deploy config isn't
runtime truth) were already merged into `ENGINEERING-LESSONS.md` earlier today as #17/#18/#19.
The two remaining candidates each have strong evidence but **only within one domain**, so both
went to the inbox instead of a PR:

- `framework/proposals/2026-08-04-confirm-your-side-sends-it.md` — before fixing a
  "downstream got an empty field" bug, open your own outgoing DTO and confirm the field exists.
  The expensive part of the VP-17591 case is that **every signal along the way was green**:
  tests, PR, deploy, live verification all passed, because they measured whether the change
  worked, not whether it was the right change.
- `framework/proposals/2026-08-04-verification-criteria-fail-toward-pass.md` — a wrong
  verification criterion fails *toward* pass, not randomly. Cheapest self-check: "would this
  criterion pass if nothing had happened?" Prefer `id > baseline` over time windows; a trailing
  `| tail` swallows the exit code and turns a failed push into a success message.

Both committed and pushed to project-agent-factory `main` as unreviewed inbox items
(`6964cd7`), per dream.md. Leo triages the inbox on his own schedule — nothing to review now.

## Score Statistics

```
Auto-linked: 500 new link entries
Scores and indexes rebuilt
  stm: count=152 highest=0.98 lowest=0.01 median=0.23
  ltm: count=10 highest=1.19 lowest=0.11 median=0.80
  archive: count=26 highest=0.80 lowest=0.04 median=0.46
```

## Notes

- **The strongest thing in tonight's working set is not a fact, it is a shape.** Four distinct
  failures on 08-04 were the same failure: a mechanism that looked like it was doing its job and
  wasn't (`void` swallowing a rejection, a search flag silently becoming a different search, a
  repo config that isn't the runtime source, a code change to a value that never leaves the
  service). Two of those variants were *already written down* and neither fired. That is the
  argument for the family entry rather than a fifth standalone bullet.
- **Leo's judgement beat mine three times in this batch** and the three have one shape: I
  ranked the clean solution above the one that could ship immediately (new Sentry project vs
  reusing 49), or I turned someone else's cleanup into my architecture gap (the 90 stuck rows),
  or I took on another team's scope (VP-17594). Recorded in `patterns.md` because the pattern is
  actionable, not just self-critical.
- **The 08-01 log's "closeout audit: none due" was probably wrong.** It rested on the
  `DURING (-30h, now)` JQL form that the 08-03 run caught returning false negatives. Any closure
  from 07-31/08-01 was therefore never actually audited. Not retro-audited tonight — out of the
  incremental window and those tickets have since had four days of prod exposure — but flagged
  so it isn't mistaken for a verified clean bill.
- Open non-blocking items carried forward: emr-v2 + transv2 **staging DBs still on
  192.168.60.11** (decom blocker awaiting an infra decision), ClickHouse 192.168.62.85 / CDC
  feed ownership unconfirmed, and transv2's carlos brokers which must stay until the remaining
  dual-publish local legs are removed.
