# Dream Log — 2026-08-16

## Ground truth reconciliation

- `reconcile-jira.py --apply`: **130 in sync, 0 flipped to completed, 0 need manual review.**
- 5 ids not found in Jira: `INCIDENT-20260518`, `INCIDENT-20260528`, `INCIDENT-20260529`,
  `INCIDENT-20260604`, `INCIDENT-2604156666` — all locally-coined incident ids with no Jira
  counterpart by design, not access failures. Same 5 as prior runs; expected steady state.
- Repo sync before starting (principle 0): `git fetch` showed `origin/main` had advanced
  `1c969b0..ec19d2f` while the local branch `fix/leo/githooks-doc-update` sat at `3b3ce72`.
  That branch has **0 commits not in `origin/main`** — it was merged as PR #29. No divergence.

## Closeout audit (Done < 24h)

**None due.** Verified three independent ways rather than trusting one query:

- Jira, assignee = agent, `status changed to (Done, Closed, Resolved) after -36h` → 0 issues.
- Jira, **whole VP project**, same window, any assignee → 0 issues. (Ran the unscoped query
  deliberately: an assignee filter would hide a ticket Leo closed under his own account.)
- Merged-PR sweep by merge time, **not** by ticket id, across `lis-backend-emr-v2`,
  `LIS-backend-v2-order-management`, `LIS-transformer-v2`, `LIS-backend-v2-pricing`,
  `LIS-backend-v2-charging`, `LIS-backend-v2-coreSamples` since 2026-08-15 → **0 merges**.
  This is the check that catches untitled "Stage test"-style promotion PRs; nothing shipped.

The only merges in the window are in this personal repo (`ec19d2f`, `1c969b0` — framework-sync
and memory-store migration). No prod-reaching code, so steps 2–5 of the audit have no subject.

**Dependency propagation (step 6): nothing to propagate.** No ticket reached Done and no code
was verified live on prod in the window, so no `unblocked_by:` / `unblock_when:` dependent
could have been unblocked. No STM re-check markers inserted.

### Daily-job freshness — the gap flagged in yesterday's log is clear today

Yesterday's note #4 said nothing reads the daily jobs' output before the next tick, so a BLOCKED
day is indistinguishable from a clean day. I read both of today's reports directly (this is a
read, not the automation change — that still needs a PR and is still not done):

- `DailyJob/hl7_fail/triage_2026-08-16.md` — **ran with verified connectivity**, not BLOCKED.
  It states `SELECT 1` succeeded and DB `NOW()` returned a sane 2026-08-16 11:00:27, then
  sanity-checked the empty result against the 72h window. 0 failures, genuinely.
- `DailyJob/result_fail/report_2026-08-16.md` — ran, produced content.

So the specific worry (silent BLOCKED days) did not materialise tonight. The structural gap
itself is unchanged.

### Standing flags carried forward (both outside the 24h window, neither resolved)

- **FLAG — Cascades has not delivered successfully since 2026-06-01.** Today's report still
  lists it as the sole destination whose last attempt failed: last success `2026-06-01`, last
  failure `2026-08-13 13:46`, 6 total failures. Two distinct error classes across its 3
  undelivered rows — an SFTP connection failure (`getConnection` after 3 attempts, samples
  2593996 / 2594005 on 08-06) and a `GENERATION_ERROR` permanent failure after max retries
  (sample 2607174 on 08-13). **Second night running with no change. Needs a triage decision
  from Leo, and a ticket if he agrees it is one.** 2.5 months of non-delivery for this vendor.
- **FLAG — VP-17691 still has no post-deploy live probe**, now 4 days after going live on prod
  (confirmed live 2026-08-14: pod image == `origin/main` 52282e3). Its STM is untouched since
  then; both open items stand — (1) one live call exercising a valid `chargeIndicator`, an
  invalid one (expect 400 + code), and an echoed `x-request-id`; (2) the doc still says
  `specimenId`/`sampleCollectionTime` are reserved and conflates `patient_not_found` with
  EligibilityFailure. Blast radius stays small — prod shows zero partner API traffic — but the
  change is behaviour-changing, so "no traffic" is not evidence of correctness.

Neither flag indicates *active* breakage with health signals firing, so per the rule I did not
stop dreaming to escalate. Both go to the digest top section.

## Signals

**5 files in the working set** (incremental since 2026-08-15).

| File | Signal |
|------|--------|
| `journal/2026-08-16-native-auto-memory-retirement.md` | `undistilled_journal` |
| `journal/2026-08-16-remaining-memory-stores-migration.md` | `undistilled_journal` |
| `journal/2026-07-15-vp17421-stale-reminders.md` | archive candidate (distilled, 32d) |
| `storage/short_term_memory/VP-16416.md` | archive candidate (completed, 42d, score 0.09) |
| `long-term-memory/leo-working-rules.md` | new, unindexed (created in-session 08-16) |

**Zero STM files were updated on or after 2026-08-15** — newest `updated:` across all 147 STM
files is 2026-08-12. Consistent with the closeout audit finding nothing shipped: a weekend with
no ticket work. Second consecutive night with an empty STM working set.

## Operations

- **Distilled journals: 2.** Both are records of the auto-memory retirement (36 + 68 entries).
  Their *factual* content was already written into LTM by the work session itself, so
  distillation here was confined to the **lessons**, which had no home yet. Verified rather
  than assumed — `leo-working-rules.md` exists with the 10 rules, and `patterns.md:2014` has the
  `core.hooksPath` section, so re-copying either would have been the exact duplication that
  retired the store.
- **Extracted: 1** — `patterns.md`, new section *「本 repo 已知的明文憑證位置（未修，待 Leo 決定）」*,
  placed next to the existing Cloud Report Service plaintext-secret note so the two known
  exposures read as one list.

  I checked this claim against ground truth instead of copying it, and **my first check was
  wrong in the safe direction**: grepping `password\s*[=:]\s*["']` found nothing, which would
  have made the journal look mistaken. `git grep` on the literal credential found **9 tracked
  files** — 8 under `DailyJob/` exactly as the journal said, plus 1 in the 08-16 archive it had
  already noted. The pattern missed them because most assign to a bare `DB_PASS` constant.
  Recorded with the operational detail that matters: only `result_fail_runner.py` reads
  `os.environ.get("LIS_DB_PASS", <literal>)`; the other 7 are unconditional hardcodes, so
  "just set an env var" does not fix them — and the literal-as-fallback shape is itself the
  trap, since it runs silently on the plaintext when the env is unset.
- **Merged: 0 / Promoted: 0 / Updated: 0.**
- **Cross-ticket review: not due.** Marker is 2026-08-14 (covered VP-17651, VP-17653, VP-17685,
  VP-17691, VP-17715). Zero STM reached `completed` since; threshold is 5.
- **Archived: 2.**
  - STM `VP-16416` → `archive/` (completed, `updated: 2026-07-05` = 42d, score 0.0947 < 0.1,
    category `technical`), `archived: '2026-08-16'` added per the existing convention.
  - Journal `2026-07-15-vp17421-stale-reminders` → `archive/journal/` (distilled, 32d).
- **Forgotten: 0.** Nothing is close. The forget rule needs age > 180d; the **oldest** file in
  the entire archive was created 2026-04-20, which is 118 days. Nothing can qualify before
  roughly late October, regardless of score.

### Lesson routing — 0 PRs, 2 inbox items

Neither candidate clears the cross-domain bar, so per the default (do-not-promote) both went to
`framework/proposals/` rather than a PR. Confirmed first that `ENGINEERING-LESSONS.md:75`
already carries 「關鍵字命中不等於覆蓋」 — so the obvious-looking third candidate was already
covered and was dropped, not re-proposed.

1. **Appended to the existing `2026-08-04-verification-criteria-fail-toward-pass.md`** rather
   than filing a new proposal, because the new case is the same lesson's mechanism: a coverage
   matcher normalised titles to `[a-z0-9]`, CJK titles normalised to the **empty string**, and
   the empty string is a substring of everything → it reported 36/36 covered, all matched to one
   unrelated lesson. Real number after a `len >= 8` guard: 16. It belongs there because the
   proposal's own rule 1 ("would this criterion pass if nothing had happened?") would have caught
   it outright, and because the failure direction is again biased toward pass — a normalisation
   that degenerates is silent, whereas one that produces garbage reports zero coverage and gets
   fixed immediately. Added the operational corollary: **calibrate a coverage tool by first
   feeding it a string you know is absent and confirming it reports "not covered."**
   I also recorded, without acting on it, that this case makes the domain-diversity question
   *arguable* — it comes from the agent's own tooling, not the employer's systems. That is
   exactly the judgment the inbox exists to hand to Leo, so I left it as his call.
2. **New `2026-08-16-never-500-for-caller-actionable-failures.md`** — the one entry the first
   journal explicitly flagged as a factory candidate and deliberately did not file. It passes
   the standalone and discipline bars cleanly (status code answers "who takes the next step?",
   zero domain nouns), and fails only on evidence: all four supporting cases are the same
   employer domain's same outward API. Noted what would complete it — one case from any other
   contract surface (CLI exit codes, queue retry vs dead-letter, SDK exception types).

## Score Statistics

```
Auto-linked: 46 new link entries
Scores and indexes rebuilt
  stm: count=147 highest=0.97 lowest=0.01 median=0.19
  ltm: count=11 highest=1.24 lowest=0.11 median=0.52
  archive: count=42 highest=0.76 lowest=0.03 median=0.08
```

STM 148 → 147 and archive 41 → 42 (the VP-16416 move). LTM 10 → **11**: `leo-working-rules.md`
entered the index for the first time — it was created during the 08-16 work session, after the
last dream ran. LTM median dropped 0.81 → 0.52 purely because a new unlinked file joined a
10-file set; it is not decay of existing entries. Top-of-index scores are unchanged.

## Notes

1. **Two consecutive nights with an empty STM working set, and the closeout audit agrees.**
   Zero Jira transitions, zero merges in six work repos, newest STM `updated:` still 2026-08-12.
   Three independent signals telling the same story is what makes the emptiness trustworthy —
   a single empty query would not have been. Worth keeping the contrast with 08-15 in mind: an
   empty working set is a finding, not a failure, but only once it has been corroborated.

2. **Tonight's near-miss inverts last night's.** Yesterday I nearly FLAGged a ticket by reading
   `updated_at` (a proxy) instead of the field value. Tonight I nearly dismissed a real
   credential exposure because my grep pattern assumed a shape the code does not use. Both are
   the same error — trusting the *instrument* rather than the *fact* — and they fail in opposite
   directions, one toward a false alarm and one toward a false all-clear. The second is worse,
   and it is exactly the failure mode described in the proposal I edited an hour later in this
   same run. Three nights, three instances of it. That recurrence is itself the argument that
   the lesson is load-bearing.

3. **The distillation work here was mostly deciding what *not* to write.** Both journals had
   already pushed their facts into LTM during the session that produced them, so the genuine
   risk tonight was re-copying content and recreating the three-copies-of-one-lesson problem
   that retired the native store in the first place. Checking `leo-working-rules.md` and
   `patterns.md:2014` before writing cost two greps and prevented exactly that.

4. **Open items for Leo, in priority order:**
   - **Cascades** — 2.5 months of non-delivery, two distinct error classes, unchanged for a
     second night. This is the one that needs a decision, not more observation.
   - **VP-17691** — still no live probe on day 4; two concrete open items already written up
     in its STM and ready to execute in a work session.
   - **Plaintext prod DB credential in 8 tracked files** — now recorded in `patterns.md` with
     the exact file list and the reason an env-var fix is not a one-liner. Already in git
     history, so rotation and history are separate decisions with different blast radii.
     Raised, not fixed.
   - **Two inbox proposals** awaiting triage, including the domain-diversity question on the
     fail-toward-pass one.
   - Still outstanding from prior runs, unchanged: the dream Phase 0.5 freshness check (an
     automation-behaviour change, so it needs a PR and Leo's threshold call), and the
     `~/personal/remote-agent/` instance with auto memory unset in both its repos.
