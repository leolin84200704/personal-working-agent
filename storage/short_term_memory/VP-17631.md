---
id: VP-17631
title: "EMR result OBR panel labels wrong — order-package API failure silently degrades to legacy first-match grouping"
status: in_review
category: emr_integration
created: 2026-08-06
updated: 2026-08-06
tickets: [VP-17631]
repos: [lis-backend-emr-v2]
pr: https://github.com/Vibrant-America/lis-backend-emr-v2/pull/326 (merged), https://github.com/Vibrant-America/lis-backend-emr-v2/pull/329 (merged)
jira: https://vibrantamerica.atlassian.net/browse/VP-17631
---

# VP-17631 — OBR panel labels wrong (Athena, accession 2606306595)

## Origin
Leo 轉來 Athena 診所（clinic 137932）抱怨：accession **2606306595**（patient SNEHAL PATEL, F, 1985-10-23,
sample_id 2587879, order 11436624, customer 41125）。診所只訂了 standard core panel，但 Athena 裡顯示
Healthspan Assessment Panel / Cardio Zoomer / Foundational Zoomer，點進 hyperlink 的 result 跟 panel 名稱不符，
診所懷疑是不是別的病人的結果。

## 診斷鏈（class A 變 class C/D）
1. `result_transmission_records` sample 2587879 只有一筆（`cmrqtbfmu001hz307n5fztrzu`，2026-07-18 20:21，
   TRANSMITTED、無 error）→ **投遞是健康的，錯的是內容的 OBR 標籤**。存下的 HL7 有 5 個 OBR：
   `VAREQUISTION10001^Healthspan Assessment Panel`(74 OBX) / `VAREQUISTION94^Other Markers`(1) /
   `VAREQUISTION460^Cardio Zoomer`(5) / `VAREQUISTION471^Foundation Zoomer`(2) / `VAPDF`。
2. `getOrderPackageAndTest?sample_id=2587879` 現在回 `package_id_list [97,51,103,59]`
   （CMP / Beta Cell Function / Lipids / CBC w-diff）+ 22 個 single test = 診所說的 core workup，
   完全沒有 Healthspan / Cardio Zoomer / Foundation Zoomer。
3. `TestPanelMappingService.groupTestsByPanels()` 把 primary path（order API）的**任何**失敗 catch 掉，
   改用 legacy `testIdToEmrCodeMap`：每個 test 指派給 `getPackage2TestMapping` 中**第一個**含它的 group。
   而 **group 10001（Healthspan, 117 members）就是該 API 的第一個 textual key** → 幾乎所有常規
   chemistry/CBC/hormone marker 都被蓋成 VAREQUISTION10001，剩下的 calc marker 落到 460/471。
4. **離線重放確認**：用 legacy first-match 算法跑這支 sample 的 82 個 marker，精確重現 7/18 的檔案
   （74 / 1 / 5 / 2）。→ 該檔確實由 fallback 產生。
5. 7/18 全天只有 2 筆有此 signature（20:21:30 與 20:21:36，同診所），其他當天投遞標籤都正確，
   且同 sample 今天 API 正常 → **transient 失敗**（retry 就會對，靜默 fallback 不會）。

## Prod 影響（下限）
`generated_hl7_content LIKE '%VAREQUISTION10001^Healthspan%'` 共 34 筆，逐筆對 order API 檢查
`10001 ∈ package_id_list`，**12 筆是誤標**（2026-01-05 → 07-18，跨 ATHENA/MDHQ/OPTIMANTRA/PF）：
samples 2400777, 2436807, 2220757, 2467814, 2482236, 2489991, 2501407, 2559894, 2500872, 2567900,
2597130, 2587879。這是**下限** — 同一 fallback 也會誤標到其他 superset package，此查詢抓不到。

## 已執行（whitelisted repush）+ post-verify
in-pod gRPC（`localhost:5000` `resultgeneration.ResultGenerationService/GenerateResultHl7`,
pod `lis-emr-v2-deployment-prod-9ff4b5f7d-ll45n`，container 名是 `lis-emr-v2-prod`）：
| sample | accession | new record | 結果 |
|---|---|---|---|
| 2587879 (SNEHAL PATEL, cust 41125) | 2606306595 | `cmsi25f340007yu07s2j8twh0` | TRANSMITTED 21:58:45，OBR 改為 97/51/103/59 + 22 VATEST + VAPDF |
| 2597130 (COURTNEY MILLS, cust 34544, 同 clinic 137932，同批次誤標) | 2607166072 | `cmsi2h2tq0009yu0776uuxj2m` | TRANSMITTED 22:07:48，同樣正確分組 |

SFTP 落地驗證（Athena `external.sftp.athena.io:/inboundintoathena`，從 pod 用 ssh2-sftp-client 列目錄）：
`2607166072.hl7` 492589 bytes（與 DB `file_size_bytes` 一致）仍在；`2606306595.hl7` 已被 Athena 取走
（早 9 分鐘上傳，資料夾只剩 1 檔）→ 兩檔都確認上傳成功且 vendor 正在輪詢。

## 附帶發現
1. **靜默丟資料**：primary path 只為 order API 回報的 package/single test 產生 OBR，
   result set 中未被歸屬的 marker 直接消失、零 log。重推後 2587879 從 82 → 71 marker，
   掉的 11 個是 Testosterone Total(14.3) / SHBG(44.6) / Free Testosterone(0.21) / HOMA-IR
   + 5 個 Cardio Zoomer ratio/score + 2 個 FZ Extra Score。前三個是真實 assay，屬 package 39001
   `Testo, Free [Incl. Total, Albumin, SHBG]`，order API 沒把它列給這支 sample。
   PDF report 仍含全部結果，所以 clinician 不是全盲，但 discrete OBX 少了。歸屬政策需 product 決定。
2. **prod dry-run 不可用**：`send_result=false` 走 `getClientConfiguration()` → 查
   `prisma.emrResultCustomer`，prod `lis_emr` **沒有 `emr_result_customers` 這張表** → 直接失敗。
   只有 send path 能跑（它從 ehr_integrations row 組 client config）。所以無法先預覽再投遞。

## 修復（PR #326 draft → staging）
https://github.com/Vibrant-America/lis-backend-emr-v2/pull/326
- order API transport/HTTP/timeout 失敗 → **throw**（`[OBR_GROUPING_FAILED]`），讓 job retry 而不是猜標籤
- `200` 但 0 package + 0 single test 也 throw（`[OBR_GROUPING_EMPTY]`）— 回空 panels 會產生無 OBX 的 ORU（LIS-7626）
- legacy grouping 保留在 `OBR_LEGACY_GROUPING_FALLBACK`（預設 off）逃生門後，ops 不必 deploy 就能恢復 best-effort 標籤
- 未歸屬 marker 改為 `[OBR_UNATTRIBUTED_RESULTS]` warn 出 test id
- 新 spec `test-panel-mapping.grouping-fallback.spec.ts`；`npx jest src/modules/result` 14 suites / 72 tests 全過；`npm run build` 乾淨

## 待 Leo 決定
1. 其餘 10 筆誤標 sample 是否 repush（不同 practice、無人抱怨；repush 會同時帶入上面第 1 點的 marker 缺漏）
2. 未歸屬 marker 的 OBR 歸屬政策（各自 package / Other Markers bucket / 設計上排除）
3. `send_result=false` dry-run 路徑是否修（現在 prod 無法預覽 HL7）
4. VP-17631 目前開為 Story，是否改判 Bug

## [2026-08-06 22:5x] Deploy 後全量修復（Leo 授權重推）

### 影響範圍最終數字（不再是下限）
第二種判別式（不依賴套組名稱）：legacy 路徑**從不產生 `VATEST` OBR**，且會把常規生化壓成極少數 OBR。
篩 `NOT LIKE '%|VATEST%'` + 含 `|CREAT^` + OBR 數 ≤6 → 2026 年 553 筆候選、逐筆對 order API 驗 OBR-1：
- **8 筆** OBR-1 不在訂購套組內，其中 1 筆（sample 2483762, `VAREQUISTION449`）是**誤判** —
  orderTypeId 449 不在 price mapping，primary path 用 `VAREQUISTION${orderTypeId}` 自行合成該代碼，標籤其實正確。
- 2025-10-23（此表最早紀錄）～2025 年底：13 筆候選、**0 筆誤標**。
→ 與 Healthspan 探針聯集後：**確認誤標 = 12 筆，全部在 2026-01-05 ～ 07-18，全部誤貼 Healthspan**。

### Deploy 驗證
prod pod `lis-emr-v2-deployment-prod-58f857fd9d-tkk4c`（image `4f012c2`，22:43 起）
`dist/modules/result/services/test-panel-mapping.service.js` grep 到
`OBR_GROUPING_FAILED` / `OBR_GROUPING_EMPTY` / `OBR_LEGACY_GROUPING_FALLBACK` ×4 / `OBR_UNATTRIBUTED_RESULTS` → 修復確實在跑。
**#329（dry-run 修復）尚未 build 到 prod**（main 已到 `4828871`），所以這批重推仍無法先預覽。

### 重推 9 筆（+ 先前 2 筆 = 11 筆）全部 verify
7 筆 cloud-location 從 AKS pod（in-pod gRPC `localhost:5000`，每筆間隔 10s）；
2 筆 onprem-location（ATHENA 18299 / PF 8093）改走 on-prem endpoint `192.168.60.6:31317`
（PF SFTP 是 IP 白名單 `45.24.217.150:2222`，從 cloud egress 送有風險）。

| sample | vendor | new record | OBR 標籤驗證 | 檔案落地 |
|---|---|---|---|---|
| 2436807 | MDHQ | cmsi3uw3f00010m07iwgfq9ix | PASS 35 OBR | /zeusmenshealth/results 534317 ✓ |
| 2400777 | MDHQ | cmsi3y3nw00030m0771m8eb67 | PASS 59 OBR | /rcode/results 7408927 ✓ |
| 2220757 | MDHQ | cmsi3z63000050m077i31lm9o | PASS 22 OBR | /kathleenthomsen/results 604733 ✓ |
| 2467814 | MDHQ | cmsi3zp6p00070m07gakmgj07 | PASS 7 OBR | /bliemr/results 675693 ✓ |
| 2489991 | MDHQ | cmsi406n800090m07ifke4b2i | PASS 31 OBR | /balancedhealthne/results 547808 ✓ |
| 2501407 | MDHQ | cmsi40qov000b0m076nb1fz9p | PASS 31 OBR | 535281 ✓ |
| 2559894 | MDHQ | cmsi41bax000d0m07ss4tcig9 | PASS 31 OBR | 539037 ✓ |
| 2500872 | ATHENA | cmsi422ef00anxx07n86qj01g | PASS 25 OBR | /inboundintoathena 485514 ✓ |
| 2567900 | PF | cmsi42euo00apxx07xf33hpfj | PASS 28 OBR | PF `/Prod/Archive/2605296027.hl7.1` 516626 已取走、不在 /Prod/Error ✓ |

驗證方法（`verify.py`）：每筆的 OBR code 集合 ⊆ {ordered package 的 uniqueemrcode} ∪ {VATEST\<single test id\>} ∪ {VAPDF}。
9/9 unexpected=0。**PF 資料夾空 ≠ 沒上傳** — PF 會即時取走並移到 `/Prod/Archive`（今天 8 筆投遞、資料夾全空），
直接在 Archive 找到我們的檔案才是證據。

### 保留未推
**sample 2482236**（Jennifer Horev, OPTIMANTRA, 2026-02-19）：customer 10344 現有 OPTIMANTRA + MDHQ
兩個 LIVE result_enabled integration（都 2025-12-24 建立），但當初只有 OPTIMANTRA 收到。
`findDistinctEligibleResultIntegrations` 依 (legacy_emr_service, sftp_result_path) 去重後會**送兩邊** →
MDHQ 會收到一份從未收過的 2 月報告。gRPC proto 無 `integration_id` 欄位（只有 sample_id/send_result/add_report），
無法只指定單一目的地。等 Leo 決定。

### 重推的代價（已量化，需 product 決定）
correct label 換來兩件事，**都是 primary path 原本的語意，不是修復造成**：
1. **未歸屬 marker 不再以 discrete OBX 送出**（仍在附件 PDF）。11 筆中 6 筆掉到真實 assay，不只計算值：
   - TESTO / SHGB / FAI（package 39001）：2436807, 2489991, 2501407, 2559894, 2587879, 2597130
   - **IRON / TIBC / TS / UIBCI（鐵代謝）**：2500872 ← 掉 14 個最多
   - FPSAP（free PSA）：2400777
   - 完全沒掉的：2467814、2567900
2. **重疊套組會重複 OBX**：診所同時訂 CMP + Electrolytes + Hepatic 時，Na/K/Cl/CO2/ALT/AST/ALBUM/ALK/TBILI/TPROT
   會在兩個 OBR 下各出現一次（2436807: 88 unique / 98 OBX）。舊 legacy 檔每個 marker 只有一份（因為 first-match
   一個 test 只會落在一個 panel）。

## [2026-08-06 23:1x] 未歸屬 marker 的歸屬規則 — 從 PDF 反推成功（Leo 提的方向）

### 取得 PDF 的路徑
DB 的 `generated_hl7_content` 把 PDF 剝成 `[PDF_CONTENT_REMOVED_FOR_DB_STORAGE]`，但**磁碟上的實體檔有**：
`result_transmission_records.file_path` → PV `emr-pv-storage:/EMR_storage/HL7Result_prod/{record_id}/{accession}.hl7`，
VAPDF OBX 的 field 5 = `^AP^^Base64^<data>`，base64 decode 即得 PDF；`pdftotext -layout` 轉文字看分節。
（順帶：prod 現在跑 `4828871`，#329 已上線，dry-run `send_result=false` 可用 —— 實測 success:true。
但 dry-run **會沿用既有 record 並更新 generation 欄位**（file_size_bytes/updated_at 被改），不是零副作用。）

### 報告的分節 = report team 的 catalogue
`LIS-Report/all_report_tests.json`（15,963 筆）每筆 = `{testId, productType, category, ...}`。
用 productType ∈ {Vibrant America, VA Internal}（= classic VA 報告）取 `category`，**與 PDF 實際渲染完全一致**
（sample 2500872 實測：Iron/UIBC/TIBC/TS 在 "Anemia" 節、Testo/SHBG/FAI 在 "Hormones (all)"、HOMA IR 在 "Insulin Resistance"）。

而這些 category 名稱**正好是 price mapping 裡 GROUP 的 name**，可直接換成既有 EMR code：

| marker | 報告 category | EMR code |
|---|---|---|
| Testosterone Total / SHBG / Free Testosterone | Hormones (all) | VAREQUISTION57 |
| Iron / TIBC / UIBC / Transferrin Saturation | Anemia | VAREQUISTION36 |
| HOMA IR | Insulin Resistance | VAREQUISTION69 |
| Free PSA (FPSAP) | Other Markers | VAREQUISTION94 |
| Non-HDL-C / TC-HDL / HDL-TG / TG-VLDL / Reynolds / Framingham / Extra Score 1&2 | 無 VA-report category（只存在於 Zoomer productType 內） | → VAREQUISTION94 Other Markers |

**否證過的替代規則**：「取最小的包含套組」不對 —— 它會把 Iron 群歸到 `TIBC [Incl. UIBC and Iron]`(39003)、
Testo 群歸到 `Testo, Free [Incl...]`(39001)，都與報告的 Anemia / Hormones (all) 不符。報告用的是自己策展的 category，
不是套組成員關係。

### 待決／待解的實作相依
emr-v2 需要拿到 `testId → (productType, category)`。repo 裡只是 JSON 快照，prod 要嘛 report team 開 API／給表，
要嘛比照 `order-mapping-cache.service.ts` 做定期刷新的快照。等 Leo 決定後實作。

## 上游（order API）失敗後的實際處理鏈（查證後）
- `ensureResultTransmissionRecord` 在 `generateHl7Content` **之前**（main line 205 vs 217）→ 我新加的 throw
  一定有 DB 痕跡：record 被標 `GENERATION_ERROR` + error_message + `retry_count++` + `next_retry_at`。
- BullMQ：`attempts: 5`、exponential backoff base 120s（≈ 2/4/8/16 分，總窗約 30 分）。processor catch → `markGenerationFailure` → re-throw → retry。
- 5 次用盡：`onFailed` → `markPermanentFailure` → `GENERATION_ERROR` + `TRANSMISSION_ERROR` +
  `PERMANENT FAILURE after max retries`，**`next_retry_at = null`**。
- **缺口**：`TIMEOUT_RETRY`（VP-17343）只在請求層 timeout 時觸發，**沒有掃 `next_retry_at` 的 cron**；
  emr-v2 的 @Cron 只有 order fetch / mapping cache / scheduled reports。agent 端 DailyJob 也只看 order 側（hl7_fail）。
  → 超過 30 分的上游中斷 = 報告不會送出、且**沒有人在看** ERROR row。修復把「靜默錯誤」換成「明確失敗但無人監看」。

## 可蒸餾到 LTM 的 pattern
- **「投遞成功」不等於「內容正確」**：result 類 triage 過去都停在 transmission_status；這次錯在 OBR 標籤，
  DB 一片綠。診斷 result 內容問題要把 `generated_hl7_content` 跟「order API 說訂了什麼」對撞。
- **fallback 只要語意上是「猜」，就必須 fail loud**：first-match grouping 對 superset package 天生會贏
  （10001 是第一個 key），degradation 產出的是合法但錯誤的臨床文件。與 patterns.md #5/#6/#9 同一族。
- **離線重放 legacy 演算法是便宜的 root-cause 證明**：用兩支 mapping API 的資料重跑舊算法，
  精確重現當時檔案的 OBX 分佈（74/1/5/2），不需要當時的 pod log。
