---
id: VP-17653
type: stm
category: repo_patterns
status: in_progress
base_weight: 0.8
created: 2026-08-10
updated: '2026-08-10'
links:
- VP-17291
- VP-17363
- VP-16463
---

# VP-17653 — Speed up emr-v2 CI/CD (~20 min builds)

Jira: https://vibrantamerica.atlassian.net/browse/VP-17653
PR (draft, target staging): https://github.com/Vibrant-America/lis-backend-emr-v2/pull/335
Branch: feature/leo/VP-17653 (worktree lis-backend-emr-v2.worktrees/VP-17653)

## Diagnosis

### [2026-08-10 — root causes of the 20-min build]
1. Sequential rollout waits: main build waits on-prem (2x `rollout status --timeout=300s`) THEN AKS (`--timeout=420s`); staging same shape. Deploys are independent once images are pushed.
2. Dead prepare-stage work: `npm install` + `npx prisma generate` + `npm run prisma:generate:test` on the agent — consumed by nothing since the test stage was deleted (commented out 2025-09-17 by zichen.z in "Fix AutoIntegration Auth Missing Issue", removed 2025-12-08 "remove commented code").
3. No .dockerignore: `COPY . .` shipped workspace node_modules/.git/kubectl binary/exported ConfigMap yamls into every Docker build context. `.env` must stay in context (Setup Env File stage injects it for the image).

Also noted (NOT in this ticket's scope):
- No test/lint guardrail at all in CI; feature/PR branches trigger nothing (only main/staging build). Leo wants to discuss re-adding tests after this lands.
- Registry credentials (internal + ACR) hardcoded in Jenkinsfile.
- Repo has 82 *.test.ts files + full jest scripts, unused by CI.

## Fix (4 commits on the PR)
1. .dockerignore (new)
2. prepare stage: keep GIT_* env exports, drop install/generate
3. main: ACR push moved before deploy fan-out (preserves VP-17291 pin-to-SHA invariant), ConfigMap export shared, then `parallel` on-prem + AKS
4. staging: ACR push up-front gated by DEPLOY_STAGING_TO_AKS, then `parallel` on-prem + AKS staging

## Gotchas hit
- Edit slip: dropped the `script{}` closing brace while wrapping the staging block — caught by brace-count check vs origin/staging (89/89 → must stay balanced). No local groovy linter; first staging build is the real syntax validation.
- pre-push hook failed in fresh worktree: no node_modules → `npx prisma generate` fetched Prisma 7.9.1 which rejects `url = env(...)` in schema (P1012). Fix: `npm ci` in the worktree so the pinned prisma@6 is used. Do NOT --no-verify.

## Deploy verification

### [2026-08-10 11:00] staging deploy verified (AKS side)
- PR #335 merged by Leo 2026-08-10 17:44:12Z (10:44 local), merge commit 2c0fdaf.
- AKS `lis-emr-v2-deployment-staging` image tag flipped to 2c0fdaf at ~10:55, rollout complete ~10:56, new pod 2/2 Running, old pod terminated.
- End-to-end merge→AKS-deployed ≈ **11-12 min** (was ~20). Syntax of the new parallel Jenkinsfile validated by the build itself.
- Pod log clean: Nest started, gRPC microservices up, Sentry env=staging, Kafka consumer disabled (expected for POD_ROLE=web).
- **Caveat**: on-prem staging branch NOT independently verified (no VPN; 192.168.60.11:31320 unreachable from local). With `parallel`, AKS success alone doesn't prove the on-prem branch passed — overall build SUCCESS/FAILURE lands in Slack lis-bot / portal-emr-bot. Verify on-prem image SHA next time on VPN: ssh 192.168.60.6 `kubectl get deployment lis-emr-v2-deployment -n default -o jsonpath='{.spec.template.spec.containers[0].image}'` should end in 2c0fdaf.
- Worktree removed after merge (branch + PR remain on origin).

## Main build post-merge analysis + cut 1

### [2026-08-10 11:30] main build 14+ min — root cause + PR #337
- Leo merged #335 and promotion #336 25s apart → staging & main builds ran concurrently.
- Main build external timeline: merge 17:44:37Z → images pushed ~17:50 → AKS prod deployed ~17:52 → build ended >14 min ⇒ on-prem parallel branch was the long pole (~8-9 min).
- **BUG FOUND**: `azure-lis-emr-v2-deployment.yaml` (on-prem STAGING deployment) pins image `lis-backend-emr-v2:latest` = the PROD image. Since VP-16463 every main build applied it → clobbered the staging SHA (on-prem staging ran prod code until next staging deploy), triggered a full staging rollout, waited ≤300s, and raced concurrent staging builds.
- Cut 1 (PR #337, draft, target staging): main no longer scp/apply/restart/wait the staging deployment; KEEPS staging ConfigMap apply-only sync (main is the sole Azure→on-prem ConfigMap path); prod flow unchanged. https://github.com/Vibrant-America/lis-backend-emr-v2/pull/337
- Jenkins found at **http://192.168.60.77:8080** (v2.492.2, Jetty; 60.10:8080 is some other 500-ing service). Login with Leo-provided leo/*** failed (loginError) both with/without trailing backtick — need corrected creds or API token for console-log timing analysis.
- On-prem NodePorts moved: 31318/31319/31320 serve on 60.5/60.6, NOT 60.11 (docs/VP-17363 say 60.11 — stale).

## Next
- Leo: review/merge PR #337 (staging → then promote main).
- Leo: correct Jenkins creds (or API token) → pull consoleText, measure per-stage timings, decide if docker build (~6 min incl. pushes) is worth a BuildKit/cache cut.
- After on-prem staging next staging build: verify its image is a staging SHA again (the clobber undone naturally).
- Item 1 (test guardrails) — separate discussion/ticket after this lands.
