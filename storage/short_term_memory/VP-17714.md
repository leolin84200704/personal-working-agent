---
id: VP-17714
type: stm
category: technical
status: active
base_weight: 0.9
created: 2026-08-14
updated: '2026-08-14'
links:
- VP-16520
- VP-16521
- VP-16523
- VP-16410
- VP-17065
tags:
- vp-17714
- calendar
- clinical-consult
- clinician-switch
- zoom
- external-url
summary: 'Clinician-switch reschedule copies the ORIGINAL clinician''s Zoom personal
  meeting room into the new event (event.service.ts `external_url: original.external_url`),
  so the transferred consult still points at the old educator''s room. Prod-confirmed
  on 3/3 Zoom switches; phone consults unaffected because external_url there is the
  patient''s phone number (copying is correct).'
jira_status: Dev To Do
---

# VP-17714 - Work Loop Record

> Created: 2026-08-14 UTC
> Status: active (Step 4 — awaiting Leo on approach)

---

## Ticket Analysis

### [2026-08-14] Basic info
- Bug | P2 - Medium | **Dev To Do** | Reporter Yuchen Wang | Assignee Leo | no components/labels/links
- Title tagged **[FE]** — evidence says the defect is **BE** (LIS-transformer-v2 calendar). FE angle only exists if we choose the "FE supplies the new link" option.
- Symptom: Zoom consult transferred from one Clinical Educator to another keeps the ORIGINAL educator's Zoom room. Example in ticket: consult moved off Dr. Jessy Dhanjal; the new educator and the provider both landed in Jessy's personal meeting room; consult started late. "A few other Clinical Educators have experienced the same issue." Phone consults unaffected.

### [2026-08-14] Root cause (code)
`LIS-transformer-v2` `src/calendar/models/event/event.service.ts` — `rescheduleClinicalConsult` (VP-16520), clinician-switch branch:

```
// F-8: new event clones the original's consult/order linkage; only clinician + time change.
const created = await tx.v2_event.create({ data: {
  ...
  external_url: original.external_url,   // <-- line ~4943
  location: original.location,
  ...
}});
```

`external_url` is the meeting-info column. For a **Zoom** consult it holds the *clinician's* personal meeting room URL → cloning it is wrong. For a **phone** consult it holds the *patient's* phone number → cloning it is correct. That asymmetry is exactly why the reporter sees Zoom break and phone not.

Same-clinician (in-place) branch never touches `external_url`, so it is correct by construction.

### [2026-08-14] Prod verification (calendar_prod, read-only)
- `external_url` per clinician confirms these are **personal meeting rooms (PMI)**, reused across dozens of events:
  Jessy Dhanjal `/j/2639232847` (70) · Brooke Ganev `/j/5067865963` (61) · Lillie Nguyen `/j/7840975748` (75) · Nour Amri `/j/6293226369` (76) · Suzette Garcia `/j/5613862727` (55) · Dana Filatova `/j/5114365088` (81).
- All clinician switches ever performed are identifiable by the accession audit fingerprint `reason = 'clinician switch reschedule, original event N'` in `v2_event_accession_audit_log` → **4 switches total** (2026-08-03 .. 2026-08-10), i.e. `rescheduleClinicalConsult` IS live in prod and the FE does call it.

| old ev | old clinician | new ev | new clinician | external_url on new event | verdict |
|---|---|---|---|---|---|
| 12405 | Nour Amri | 12406 | Jessy Dhanjal | `/j/6293226369` = Nour's room | WRONG |
| 12235 | Jessy Dhanjal | 12484 | Brooke Ganev | `/j/2639232847` = Jessy's room | WRONG — this is the ticket's incident (consult 2026-08-11 20:30Z) |
| 12455 | Lillie Nguyen | 12528 | Suzette Garcia | `/j/7840975748` = Lillie's room | WRONG — **still future-dated, 2026-08-21 17:30Z, live today** |
| 12614 | Dana Filatova | 12615 | Lillie Nguyen | `808-333-6747` (patient phone) | correct |

3/3 Zoom switches broken. **Event 12528 is an unfixed upcoming consult** → candidate for a data fix independent of the code fix.

### [2026-08-14] Blast radius of the stale link
`external_url` is read by: VI calendar UI (GraphQL), Google sync as the event `location` (`google.service.ts:522`), reminder emails (`reminder.service.ts:294` `location || zoom_event_id || external_url`), daily appointments report column `External_url` (VP-17065). Reschedule emails themselves do **not** carry the link (template model has no url/link key), so email is not an extra leak but the reminder is.

### [2026-08-14] Where a correct link could come from
- `v2_calendar.zoom_calendar_ids` is `{}` for **all 9** practice-150105 clinicians → no stored per-clinician link.
- `v2_third_party_integration` zoom rows exist for **8 of 9** clinicians (all except Mary Augustine) → the new clinician's PMI is resolvable server-side via their Zoom OAuth token: `GET /users/me` → `personal_meeting_url` (already used inside `ZoomService.generateLinkForProvider`, which additionally creates a scheduled meeting we would not want).
- `RescheduleClinicalConsultInput` has NO `external_url` field today → the FE currently *cannot* pass a corrected link even if it wanted to.

## Approaches Considered

### [2026-08-14] Options (pending Leo)
- **A — BE resolves the new clinician's Zoom room (recommended).** In the switch branch: if `original.external_url` looks like a Zoom URL, resolve the *new* clinician's PMI (new `ZoomService.getPersonalMeetingUrl(providerId, clinicId)` = token + `GET /users/me`, no meeting creation) and store that; otherwise copy as today. Fixes every caller, no FE dependency. Open: fallback when the new clinician has no Zoom integration or the API fails (leave null vs keep old link vs derive their most-used historical PMI).
- **B — FE supplies it.** Add optional `external_url` to `RescheduleClinicalConsultInput`; FE calls the existing `generateZoomLinkForProvider` for the new clinician and passes it. Matches the ticket's [FE] tag but needs an FE ticket and leaves non-FE callers broken.
- **C — Don't clone on switch.** Blank `external_url` when it is a Zoom URL, forcing manual re-entry. Safest, no Zoom API dependency, but a UX regression vs what the reporter asked for.
- A + B are combinable: B's input field as an override, A as the default.

## Decisions Made

### [2026-08-14] Leo: 走 A + optional external_url
- BE 預設自己解析新 clinician 的 Zoom PMI（新 `ZoomService.getPersonalMeetingUrl`，只 `GET /users/me`，不開排程會議），同時 `RescheduleClinicalConsultInput` 加 optional `external_url` 讓 FE 可覆寫。
- 只在 `original.external_url` 含 `zoom.us` 時改寫；其餘（電話 333 筆、空白 5870 筆、other-text 6 筆）維持照抄。practice 150105 的 external_url 只有這四種形狀，沒有 teams/gmeet/其他 URL，heuristic 乾淨。
- 12528 的資料修正 SQL 已產給 Leo 確認（未執行）。

### [2026-08-14] 已排除的疑慮
- 外部行事曆殘留：practice 150105 只有 8 筆 zoom integration，無 google/outlook，歷來 `google_event_id`/`outlook_event_id` 皆 0 → 不成立。
- reminder email / daily report / VI UI 都是當下讀 DB → code fix 後自動正確，無額外動作。

### [2026-08-14] 已知殘留（待 Leo 決定是否納入本 ticket）
- `updateEvent` / `updateEventByPatient` → `updateWholeEventRecord` → `replaceEventParticipants`（`event.service.ts:3633/3641` deleteMany+createMany）可以**原地換掉 clinician participant 而完全不碰 `external_url`** → 同一個 bug 的第二條路徑，且沒有 accession audit 指紋可追。
- 歷史上無法證明這條路徑被用來換過 clinician：`replaceEventParticipants` 每次帶 participants 的 update 都會重寫 `created_at`，所以 `participant.created_at > event.create_time` 這個訊號充滿雜訊（抽樣 30 筆，多數 external_url 與該 clinician 自己的 PMI 相符 = 不是 switch）。
- 目前 4 次 switch 全走 `rescheduleClinicalConsult`，所以 A 涵蓋現行 FE 行為；風險在「只換人不改時間」是否也走 reschedule mutation — 需 FE/QA 確認。

## Prod Data Fix

### [2026-08-14 19:21Z] event 12528 external_url 修正（Gate 7 走完）
- 反向稽核（比原本 switch-pair 更寬的條件：所有未來、未取消的 150105 事件，zoom.us 連結的 `/j/` 房號 ≠ 該 clinician 主要房號）撈到 2 筆：12528（真 bug）+ 9577（Emaline Brown，房號 `81595562495` 是 **11 碼一次性排程會議 ID**，非 10 碼 PMI，且不在任何 switch pair 中 → 正常單場會議，不動）。
- UPDATE 綁 `event_id=12528 AND is_canceled=false AND external_url=<Lillie 精確值>` → `UPDATE 1`，COMMIT。
- Before `https://vibrant-wellness.zoom.us/j/7840975748?pwd=RENGQUQ0MmpucC9jNnNuSHdGV0tHQT09`（Lillie）
  After `https://vibrant-wellness.zoom.us/j/5613862727?pwd=YjRWUTZ3OWVJdFdjaGZsZUNkVFgrdz09`（Suzette，69 筆佐證，最遠用到 2026-09-04）
- 修完重跑反向稽核：只剩 9577（預期內）→ 0 殘留。

## Code Changes

### [2026-08-14] Branch `bugfix/leo/VP-17714`（base origin/stage_test @ ead3fee），2 commits
- `9dcf3ff` — `ZoomService.getPersonalMeetingUrl(providerId, clinicId)`：唯讀，`getValidAccessToken` + `GET /users/me` → `personal_meeting_url ?? https://zoom.us/j/{pmi}`。**不建會議**（`generateLinkForProvider` 每次呼叫都會 `POST /users/me/meetings`，不能放在 reschedule 路徑上）。
- `4e16c77` — reschedule 接線 + 測試：
  - `RescheduleClinicalConsultInput` 加 optional `external_url`（FE override）
  - 新 private `resolveRescheduleExternalUrl(originalExternalUrl, override, newClinicianCalendarId, isClinicianSwitch)`，優先序：override → switch 且原值含 `zoom.us` 時解析新 clinician 的 PMI → 其餘照抄。解不出來（無 integration / API 失敗 / 查無 calendar）**寫 null 並 logWarn**，不留會導錯房間的連結。
  - **在 `$transaction` 之前呼叫** — 交易期間不得跨 outbound HTTP。
  - switch 分支 `external_url: original.external_url` → `resolvedExternalUrl`
  - in-place 分支只在 caller 明確給 `external_url` 時才寫入，否則完全不碰（維持原行為）
- 未動 `updateEvent`/`updateEventByPatient`（Leo 選 (i)，最小範圍）。
- `prisma2/generated/client2/*` 被 `prisma generate` 改寫成本機絕對路徑 → 已 `git checkout` 還原，未進 commit。

## Test Results

### [2026-08-14] 單元測試 + build
- `event.service.spec.ts`：**61/61 pass**（55 既有 + 6 新，全在 `meeting info on a clinician switch (VP-17714)` describe 下）：zoom 連結換成新 clinician 房間並驗證 `getPersonalMeetingUrl(30, 150105)` 被呼叫 / 電話原封不動且不呼叫 zoom / 解析失敗寫 null 且 logWarn / override 勝過 zoom 解析 / in-place override 生效 / in-place 無 override 時 update data 完全沒有 `external_url` key。
- `npm run build`：**0 error**。`tsc --noEmit`：我的 3 個檔 0 error（全репо 28 個錯全在我沒碰的 3 個 spec：schedule.integration 23 / provider-availability.service.spec 4 / clinic.e2e-spec 1）。
- **全套 baseline 對照**（用 worktree 跑 origin/stage_test @ ead3fee 再 diff 失敗清單）：base 17 fail / 382 pass / 399 total；branch 16 fail / **389 pass** / 405 total。逐 suite diff：唯一差異是 `auth.guard.spec.ts` base 掛、branch 過 → flaky（與本次無關），**零新增失敗**。其餘 pre-existing 失敗：clinic.service / event.practice-event-type.graphql / meeting-request.practice-event-type.graphql / mypractice x2 / utility.service。
- **未跑 e2e**（會打真 DB / 發信）→ 留 staging 驗證。

## User Feedback

（pending）

## Failures

（none yet）

## Retrospective

（pending）

## Lessons Learned

（pending）
