---
id: VP-17766
type: stm
category: technical
status: in_review
score: 0.00
base_weight: 0.9
created: 2026-09-04
updated: 2026-09-04
links: [VP-17765, VP-17759, QH-6719]
relations:
  unblocked_by: []
  blocks: []
  sibling: [VP-17765, VP-17759]
unblock_when: ""
tags: [vp-17766, consult-reminder, calendar-owner-email, multi-recipient, to-cc, transformer-v2, booking-form-email, per-event-recipient]
summary: "[BE] Clinical consult booking: plumb the booking-form email through as a per-event recipient (To + CC model) instead of the copy-once v2_calendar.calendar_owner_email snapshot; migrate comma/semicolon multi-address rows. Follow-up of VP-17765 / VP-17759. Cross-repo (LIS-transformer-v2 + consult booking FE)."
---
# VP-17766 - Work Loop Record

## Ticket Analysis

### [2026-09-04 09:30] Session start — L4 sync + prod sizing

- Jira (REST via repo .env; Atlassian MCP down this session): **Dev To Do**, P2, Task,
  assignee/reporter Leo, created 2026-08-18, last updated 2026-08-19. No human comments
  (only 2 Jira-automation "linked issue resolved" notes). Links: Relates VP-17765 (Done),
  Test QH-6719 (To Do). Description = the one written 2026-08-18 (see VP-17765 STM):
  multi-recipient To+CC hard requirement, optional contact_email on booking payload,
  per-event recipient preferred, migrate comma rows, FE must send the field, PM decision on
  semantics + email verification, backfill of 3,365 stale rows out of scope.
- Prior work: VP-17759 (diagnosis: typed email lives only in `v2_event.notes` `[Email: ...]`),
  VP-17765 (bounded data fix on calendar 35082 → comma 3-address list; comma fan-out
  live-tested via Postmark).

**Prod calendar_prod read-only sizing (2026-09-04):**
- Multi-address rows in `v2_calendar.calendar_owner_email`: **2**, not 1.
  - 35082 (comma, 3 addr, VP-17765 interim) — works today via Postmark comma fan-out.
  - **36760** (owner 4769, practice 150105, role patient, created 2026-02-25): **semicolon**
    4-address list, `length = 100` exactly → truncated (`...nurse@drkateth`). Owner's two
    clinicadmin calendars (9759 / 16878) hold only `healthcoach@drkatethomsen.com`.
    Semicolon list is NOT a valid Postmark To → any reminder for this row would fail/bounce.
    0 future consult events on 36760 right now.
- Whitespace: 442 rows with leading/trailing whitespace, 1 with inner whitespace (=36760).
- Empty / no `@`: 6,119 of 51,118 rows (patient 4,909 / clinicadmin 860 / provider 350).
- `v2_event` Clinical Consult: **1,880 total, 1,736 (92%) carry `[Email: ...]` in notes**.
  Last 90d: 1,474 consults, 1,384 with typed email; of those **392 (28%) differ** from the
  patient-participant calendar email (958 same, 34 no patient calendar, 8 typed value fails a
  basic email regex). → the typed email is the user's actual intent in ~1/4 of bookings.
- Schema: `v2_event` has no recipient column; `v2_event_participant(id, event_id,
  participant_calendar_id, role, status, created_at, updated_at)`;
  `v2_reminder_audit_log(id, event_id, reminder_type, recipient_email, template_id,
  idempotency_key, status, error_message, attempt_count, scheduled_for, sent_at, ...)`.
  `_prisma_migrations` exists (last: 20251208000001_add_strategic_indexes).
- Downstream: LIS-notification-center Kafka email contract already has `To`/`Cc`/`Bcc`
  (comma-separated strings, all required keys; `json.validator.ts:16-19`); Postmark path
  passes `Cc: job.data['Cc']` verbatim (`consumer.service.ts:107`), SendGrid path splits on
  ',' (L225-227); `email_send_out_request` has `cc`/`bcc` columns →
  **CC is supported end-to-end today; only transv2 needs to populate it.**

### [2026-09-04 10:45] FE located — va-portal (Explore result)

- Consult booking UI = **va-portal** `src/views/home/ScheduleMeeting/schedule-dialog/`:
  `ContactInfo.vue` (Name/Email/Phone/Meeting Type form, client-side `isValidEmail` regex from
  `src/utils/index.js:180`, trims only for the test — untrimmed value is stored) →
  `ScheduleAMeetingDialog.vue#finishFillInfo` (~L645-751) joins `[Accession Id ..] [Name: ..]
  [Phone: ..] [Email: ..] [Meeting Type: ..]` into `notes` and calls `createEventByPatient`
  (`src/graphql/mutations/clinician-schedule.js`, Apollo client `patientProfilePage` →
  `https://api.vibrant-america.com/v2/portal/trans-service/graphql`, **no intermediate hop**).
- Same client also calls `getLabClinicianAvailability`, `getRescheduleAvailability`,
  `rescheduleClinicalConsult`, `generateZoomLinkForProvider` (hardcoded clinicId 150105).
  Legacy Portal-Calendar REST (`api.vibrant-wellness.com/v1/portal/calendar/`) still used for
  some availability calls, but create path is fully GraphQL.
- Bracket convention originated server-side in Portal-Calendar `event.service.ts:244-246`.
- ehr-frontend has the host/provider half (generic `createEvent`), no patient booking form;
  wellness-portal / pns-portal have no consult flow.
- Side bugs seen (out of scope, note only): FE computes `contactObj.timezone` but never sends
  it; Telephone meetings put the phone number into `external_url`.
- Repo access: Leo has local clone `~/src/va-portal` (origin Vibrant-America/va-portal, main;
  prior branch bugfix/leo/VP-17868) → FE repo ownership question from the ticket is answered.
- Form prefill: `ScheduleMeetingDialogs.vue#fetchSettings` (L176-183) seeds
  name/email/phone from the account settings (`personal.practice_name`,
  `contact_info.primary_email`, `primary_phone`/`office_phone`). So the `[Email: ...]` value is
  the account's primary contact email unless the user edits it — a much better recipient
  signal than the copy-once calendar snapshot either way.

### [2026-09-04 11:00] transv2 code map (Explore result, origin/main)

- `CreateEventByPatientInput` event.model.ts:526-608 — 16 fields, **no email**; participant
  sub-input :655-670 (calendar_id/role/status only). `RescheduleClinicalConsultInput` has notes,
  no email. Public-booking DTO `appointment-request.dto.ts:49` `patient_email?` is the only
  per-request email in the module.
- Consult creation paths: `createEventByPatient` (resolver :247, service :1549-1888, pins
  practice 150105 in `buildEventData` :1732-1751, emails :1875 via
  `sendAppointmentScheduledEmailToNonPatients`); `createEvent` :465-698 (clinician-side,
  emails :677); `rescheduleClinicalConsult` :4882-5058 (emails :5197
  `sendRescheduleEmailsForClinicianEvent`, single `send()` choke point :5236-5262);
  meeting-request accept :1201-1219; `[Email:` string is built ONLY in FE, BE passes `notes`.
- `createPatientCalendarForCustomerIfAbsent` provider-availability.service.ts:658-800: copy-once
  from any provider/clinicadmin calendar (no practice filter), self-heals only when NULL;
  caller `getLabClinicianAvailability` meeting-request.resolver.ts:173-177.
- Readers of `calendar_owner_email`: event.service.ts 39 `to:` sites (scheduled :4437/:4488/
  :4614/:4671, updated :4792/:4848/:5769/:5825, reschedule :5243, canceled :5409/:5456/:5557/
  :5602/:5645, confirmed :5928/:5988 early-returns for 150105); reminder.service.ts recipients
  filter :170-174, per-participant send :236 + audit row, idempotency key
  `reminder:{event}:{type}:{epoch}:{calendar_id}` (:222, reminder.constants.ts:40-42);
  meeting-request.service.ts :319/:361/:761; daily-report :212; public-booking :499.
- Builders: `EmailTemplateConfigService.buildEmailMessage` email-template-config.service.ts:151-224
  already has `cc?/bcc?` params → `Cc: params.cc || ''` (:213); **zero callers pass cc**.
  `AppointmentNotificationEmailService` :75-157 accepts `string[]` To (';'-joined :141) + cc.
- `v2_reminder_audit_log` schema.prisma:778-797, `recipient_email VARCHAR(255) NOT NULL`
  (single), DDL prisma/manual-migrations/20260501_add_v2_reminder_audit_log.sql.
- **Schema management: Prisma (PG calendar + MySQL client2) but NO prisma migrate** — 5
  hand-written idempotent SQL files in `prisma/manual-migrations/` applied via psql; new column
  = SQL file + schema.prisma + `npx prisma generate` (both clients). Hot lesson "Schema
  migration before deploy" applies verbatim.
- GraphQL code-first `autoSchemaFile: true` (app.module.ts:55); checked-in `schema.gql` is stale
  (no createEventByPatient) — do not edit.
- OTP: public-booking.service.ts:683-786 (Redis key `transv2::public_booking::verification_code::{email}`,
  600s, templates 43460319/43460231) — REST on unauthenticated module, not shared; consult
  booking is behind AuthGuard.
- Tests: Jest, hand-rolled prisma mocks (`reminder.service.spec.ts` ~520 lines asserts
  `.data.recipient_email` / `.data.idempotency_key`; `provider-availability.service.spec.ts:620-770`
  VP-17825 pattern); GraphQL shape via `Test.createTestingModule` + autoSchemaFile
  (`event.practice-event-type.graphql.spec.ts:62`).

**Lesson injection (Step 1.4, 2 picked):**
1. *Schema migration before deploy* — non-ORM-managed prod DB: ALTER prod first, deploy second.
2. *Update 入口收的欄位要與 handler 消費清單對帳* — if the new recipient fields are added to
   any update/reschedule input, the handler must actually persist them or the input must not
   accept them; no silent no-op.

## Approaches Considered

### [2026-09-04 11:05] Draft (pre-debate) — see scratchpad vp17766-proposal.md
- **A (recommended)**: per-event `v2_event.contact_email` + `cc_emails text[]`, calendar-level
  default `v2_calendar.notification_cc_emails text[]` (home for migrated multi-address rows),
  audit `cc_emails text[]`; one pure `resolveConsultRecipients()` used by reminder + consult
  sendAppointment* patient-side sends; Cc rides on the same message (same idempotency key).
  API: optional `contact_email` + `cc_emails` on CreateEventByPatientInput, @IsEmail each,
  no OTP (authenticated). FE: send existing field + new optional CC input.
- **B**: child table `v2_event_email_recipient(event_id,email,kind)` — normalized, more joins,
  still needs calendar-level default home.
- **C**: mutate calendar row per booking — rejected by ticket (last-write-wins, shared field).
- **D**: To override only — fails hard requirement.

### [2026-09-04 11:40] Debate synthesis → adjusted design A'

Con findings verified in code (all true):
- Clinician-switch reschedule clones a NEW v2_event row with an explicit field list
  (event.service.ts:4943-4960) → new columns must be added to the clone (and to the
  THIS_AND_FUTURE series split ~:1996-2003) or every reschedule silently reverts to the stale
  calendar email.
- Every seeker send-site guards on the CALENDAR email (`:4653 if (!providerCal?.calendar_owner_email) continue`,
  `:5238 if (!toCalendar?.calendar_owner_email) return`, reminder :170-173/:220) → guards must
  test the RESOLVED To, not the calendar column.
- Actual consult update/cancel paths are `*ToNonPatients` (:4848 / :5456), reached from
  updateEventByPatient :2057-2062 and deleteEventByPatient :2215; :5557-5825 are generic paths.
- Seeker email is also template CONTENT for the clinician (`provider_email` :4586/:4763/:5265/:5292).
- meeting-request 150105 branch (:319/:761) also emails the seeker calendar.
- Seeker is a loop (`for providerSeeker of patientParticipants` :4648/:5449/:5528) → resolver
  is per (event, seekerCalendar), not per event.
- FE reconstructs reschedule email from `notes` (ScheduleAMeetingDialog.vue:579) → notes
  `[Email:]` stays as display source; `contact_email` is the authoritative send address.
- 36760's 4th address is unrecoverable from this DB (truncated upstream) → needs practice confirm.

Adjusted A' (kept calendar-level default CC — it is the only explicit home for the 2 migrated
multi-address rows; cost is one extra column in the reminder select since the calendar row is
already loaded at every send site):
- Columns: v2_event.contact_email text, v2_event.cc_emails text[] default '{}',
  v2_calendar.notification_cc_emails text[] default '{}', v2_reminder_audit_log.cc_emails text[] default '{}'.
- `resolveConsultRecipients(event, seekerCalendar)` → {to, cc}; To = event.contact_email ??
  cal.calendar_owner_email (trim/lower); Cc = dedupe(event.cc_emails ∪ cal.notification_cc_emails) − To.
- Inputs: CreateEventByPatientInput + RescheduleClinicalConsultInput get optional
  contact_email / cc_emails (@IsEmail each). UpdateEventInput NOT extended in v1 (lesson:
  contract fields must be persisted; update path is cancel-dominated).
- Send sites: :4437, :4671, :4848, :5456, :5243 choke point, :5557, :5769, meeting-request
  :319/:761, reminder :170-236 (+audit cc_emails), template provider_email x4, clone lists x2.

## Decisions Made
### [2026-09-04 12:00] Leo approved Step 5
- Leo: "a ok 進step 5, 2. 不做 3. 不管FE, 先改code" → design A' approved; NO OTP / email
  verification (validation = @IsEmail only); FE (va-portal) out of scope for now; backend
  (transv2) code first. Q4 (36760 interim fix) and Q5 (deploy order) not answered → no prod
  data write this session; deploy order stays as proposed pending confirmation.

### [2026-09-04 14:20] Leo: "給我commit + push PR" → committed + pushed + PR opened
- lis-prod-change-gate walked (Gate 0-8; no prod data write, no new env, schema is manual SQL
  with apply-before-deploy header).
- Commit `1f0fdd4` on `feature/leo/VP-17766` (LIS-transformer-v2), pushed.
- **PR #617 → `stage_test`** https://github.com/Vibrant-America/LIS-transformer-v2/pull/617
  (repo convention: separate `(stage_test)` PR first, main PR after staging verification).
- One-PR-one-head: no further pushes to this branch; fixes go to a new PR.
- Still pending Leo: 36760 interim fix, who runs the prod ALTER, FE follow-up ticket, main PR.

## Code Changes
### [2026-09-04 13:30] Step 5 — transv2 implementation on feature/leo/VP-17766 (from main 825786a)

Files:
- `prisma/manual-migrations/20260904_add_consult_recipient_overrides.sql` — idempotent
  ADD COLUMN x4: v2_event.contact_email TEXT, v2_event.cc_emails TEXT[] DEFAULT '{}',
  v2_calendar.notification_cc_emails TEXT[] DEFAULT '{}', v2_reminder_audit_log.cc_emails
  TEXT[] DEFAULT '{}'. Header says apply BEFORE deploy (Prisma selects every scalar).
- `prisma/schema.prisma` — same 4 fields; `npx prisma generate` run.
- NEW `src/calendar/shared/consult-recipients.util.ts` (+ spec, 18 tests): `normalizeEmail`,
  `normalizeEmailList`, `splitLegacyEmailList`, `resolveConsultRecipients(event, calendar)`,
  `resolveParticipantRecipients(event, calendar)` (override only for role==='patient').
  Legacy tolerance: comma/semicolon-joined calendar_owner_email → first=To, rest=Cc.
  Trim-only normalization; case-insensitive dedupe; To never in Cc.
- `event.model.ts` — `contact_email?` (@IsEmail) + `cc_emails?` (@IsArray @ArrayMaxSize(10)
  @IsEmail each) on CreateEventByPatientInput AND RescheduleClinicalConsultInput. NOT on
  CreateEventInput / UpdateEventInput (lesson: don't accept fields the handler won't persist).
- `event.service.ts` — persist in createEventByPatient buildEventData; clone on clinician-switch
  reschedule (input override wins, else original); in-place reschedule updates only when sent;
  THIS_AND_FUTURE series split carries fields; all 7 patient/seeker send loops + reschedule
  `send()` choke point resolve To/Cc via `resolveParticipantRecipients` and guard on resolved
  To (was: guard on calendar_owner_email); clinician-facing template `provider_email` x5 uses
  resolved seeker To. Lab-team / provider-side sends untouched.
- `reminder.service.ts` — select notification_cc_emails; recipient filter + loop use resolver;
  `cc` on buildEmailMessage; `claim()` gains ccEmails → audit `cc_emails`; idempotency key
  unchanged (CC rides on the same message).
- `meeting-request.service.ts` — sendMeetingRequestEmails (calendar-only resolve, no event yet),
  loader selects notification_cc_emails, sendAppointmentScheduledEmailForCreatedEvent seeker
  loop resolves To/Cc; types AppointmentEventCalendar/Record extended.
- `calendar.model.ts`, `schedule.model.ts` — GraphQL Calendar/V2Calendar classes implement
  v2_calendar → added `notification_cc_emails` @Field.
- Tests added: reminder.service.spec (+6), event.service.spec (+3 reschedule clone/in-place,
  +1 describe with 6 parametrized send methods + 4), event.recipients-input.spec (class-validator,
  8), consult-recipients.util.spec (18).

Verification: `tsc --noEmit` clean for touched files (pre-existing errors only in
schedule.integration.spec / clinic.e2e-spec / public-booking.service null checks).
Targeted jest: 178 pass; 3 failures in meeting-request.practice-event-type.graphql.spec are
PRE-EXISTING (Nest DI: TimezoneSettingService missing from that spec's providers since
VP-17190) — not touched.

### [2026-09-05 00:55Z] PR #617 merged to stage_test by Leo (20:27Z 09-04) → staging migration applied
- Merge commit 8d60a0e (head 1f0fdd4 confirmed = PR head). GitHub Actions
  `frontend-service-graphql-st` deployed st pod ~20:34Z; a later merge (#616) redeployed 21:25Z.
- st pod env: `DATABASE_URL_CALENDAR ...?schema=calendar_dev_new`, SERVER_ENVIRONMENT=stprod,
  platform_type=cloud (reminder cron runs on staging too). prod pods: schema=calendar_prod.
- **Applied** `20260904_add_consult_recipient_overrides.sql` to `calendar_dev_new` via psql
  (`SET search_path`), verified 4/4 columns present with defaults; calendar_prod verified
  untouched (0 columns). No prod write.

## Test Results
### [2026-09-04 14:00] Step 6 verification (staged on feature/leo/VP-17766, not committed yet)
- `npx tsc --noEmit`: no new errors (pre-existing only: schedule.integration.spec x23,
  clinic.e2e-spec x1, public-booking.service null-checks x3).
- Targeted jest (util / reminder / event.service / recipients-input / meeting-request /
  provider-availability / daily-report): **196 passed, 0 failed**.
- Full `npx jest`: 690 passed, 13 failed in 6 suites — ALL 6 suites fail identically on main
  (stash-baseline 12 failed on the same 5 suites + meeting-request graphql spec 3):
  event/meeting-request practice-event-type graphql specs (Nest DI missing
  TimezoneSettingService since VP-17190), clinic.service.spec, mypractice x2, daily-report
  (flaky in full run, passes alone). No regression attributable to VP-17766.
- eslint --fix: reverted 5 formatting-only hunks it applied to untouched code; remaining
  errors in touched files are pre-existing `no-unsafe-*` (baseline 18 → 11 on the 3 services).
- No CJK / em-dash in added code.
- Staged: 13 files, +965 / -68.

## User Feedback
## Failures
### [2026-09-05 00:50Z] Gate 3 tripped on STAGING (self-inflicted sequencing gap)
- Symptom: st pod logs every 2 min `PrismaClientKnownRequestError P2022: The column
  v2_event.contact_email does not exist` from `ReminderService.dispatchReminderType`
  (reminder.service.ts:131) — i.e. every v2_event read on staging failed from ~20:34Z until the
  ALTER at ~00:54Z (~4.3h). Staging only; prod pods run pre-change code.
- Root cause: the PR body / SQL header said "apply the SQL BEFORE deploying", but stage_test
  merge == auto-deploy, and nobody (me included) applied the staging ALTER before Leo merged.
  I had left "who runs the ALTER" as an open question instead of doing the staging half myself
  before opening the PR.
- Lesson (candidate for ENGINEERING-LESSONS / hot lessons refinement): for push-to-deploy
  branches, "schema before deploy" means the agent applies the STAGING schema change BEFORE
  opening the stage_test PR (staging schema is agent-safe, additive, idempotent), and the PR
  body states "staging ALTER already applied; prod ALTER must precede the main merge". Leaving
  it as a question to the human converts a mechanical step into a race.

## Retrospective
## Lessons Learned
