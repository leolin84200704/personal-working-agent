---
id: VP-17715
title: '[BE] EMR Push - Report-group push level: single push when all non-deferred
  reports are ready, final push at order completion'
status: completed
category: emr_integration
created: 2026-08-14
updated: '2026-08-14'
links:
- VP-17344
- VP-17493
- VP-17441
- emr-integration
tags:
- vp-17715
- result-push
- partial-push
- report-group
summary: 'PER_REPORT_GROUP push level: exactly-two-deliveries contract for cust 4953
  (push GROUP:MAIN when all non-deferred reports generated while deferred (GUT5)
  pending; existing report_finished whole-order push is the second delivery). PR
  #364 merged -> staging -> main (#365), deployed, DDL applied both envs, prod E2E
  PASS (replay + dedup), cust 4953 flipped LIVE 2026-08-14.'
jira_status: Done
---

# VP-17715 — PER_REPORT_GROUP push level (exactly two deliveries)

## Ticket Analysis

### [2026-08-14 11:00]
- Customer (Dr Prusmack cust 4953, via Terry) demanded: report comes through ONLY TWICE —
  once when everything except Gut Zoomer is resulted, once when GZ results IF it results.
- Current PER_SAMPLE_TYPE delivers 6-8 interim pushes + final (counted from their real
  orders); each interim can miss late-approved CALCULATION analytes (the +12 wellness
  scores/ratios found 2026-08-13) because `sample_type_all_finish` gates only on
  instrument-measured tests. Specimen type is also lab-internal (Urine vs Metal Free
  Urine = different collection containers; Serum vs SERUM = duplicate catalog entries).
- Filed by us after Leo decision ("我們要改過來"). Jira: VP-17715 (Story, Dev To Do).

## Key design decisions

### [2026-08-14 11:20]
- **Trigger = report readiness, not test approval**: `new_report_status_updated` addon
  carries `total_reports_short_names` (full expected set INCLUDING not-yet-generated,
  e.g. GUT5) + `generated_reports_short_names` — verified against ClickHouse
  kafka.general_sample_events for accession 2607166082 (10/11 generated, GUT5 pending).
  Report-ready also inherently includes calculated analytes.
- **Phase 2 = existing whole-order push**: report_finished fires when ALL reports incl.
  deferred are final; push levels never filter the final push. So NO new phase-2 code —
  main push condition requires `remaining ≠ ∅` so the final never duplicates it.
- Condition: `mainReports = total − deferred`; push GROUP:MAIN iff
  `mainReports ≠ ∅ ∧ mainReports ⊆ generated ∧ remaining ≠ ∅`.
- Config: `ehr_integrations.deferred_report_short_names` VARCHAR(500) JSON array
  (`["GUT5"]`); empty/unparseable → no main push (degrade to whole-order-only, fail-safe).
- Dedup: scope GROUP:MAIN, repushAfterMs null (delivered shields forever, undelivered 24h
  — same semantics as REPORT scope).
- Edge cases (all fall through to final push): deferred-only accession (Demos GZ-only
  barcode 2602196752), no deferred report ordered, deferred finished first (collapses to
  ONE complete final push — open question to PM, documented in ticket).

## Code Changes

### [2026-08-14 11:40] Branch feature/leo/VP-17715 (worktree off origin/staging f04db5f)
- `prisma/schema.prisma`: enum += PER_REPORT_GROUP; ehr_integrations += deferred_report_short_names String? @db.VarChar(500).
- `prisma/migrations/20260814_add_report_group_push_vp17715/migration.sql`: MODIFY enum + ADD COLUMN; header documents deploy order (VP-17344 pattern).
- `kafka-report-finished-listener.service.ts`: ResultIntegrationInfo += deferredReportShortNames (parsed via new parseDeferredReportShortNames, both eligibility selects); handleNewReportStatusUpdatedEvent restructured for dual levels (eligibility per level BEFORE the shouldSkipPartialWhenWholeFinal API call to preserve the no-integration fast path); new handleReportGroupPush.
- Spec: 6 new tests (happy path, main-incomplete, all-generated, unconfigured deferred, deferred-only accession, delivered-scope dedup). 26/26 listener; full suite 109 suites / 1287 passed / 0 failed.
- 3 commits (schema / wiring / tests) for revert boundaries. PR **#364 draft -> staging**: https://github.com/Vibrant-America/lis-backend-emr-v2/pull/364

## Deployment / Ops
- **DDL NOT yet applied** (prod lisportalprod2 + staging 192.168.60.11) — needs Leo go (DB schema change requires ask-first). Must be applied BEFORE deploy.
- Rollout: `UPDATE ehr_integrations SET result_push_level='PER_REPORT_GROUP', deferred_report_short_names='["GUT5"]' WHERE id='c0000CMYCWWC500T2I1A85BHS';` (60s cache). Rollback = back to PER_SAMPLE_TYPE.
- After flip: sample_type_all_finish events no longer match this integration (eligibility filters by exact level) → interim per-type pushes stop immediately.

## Deployment / closure (2026-08-14, same day)
- Leo merged #364 -> staging AND promote #365 -> main (18:42Z). DDL applied BY AGENT (Leo go) BEFORE pod roll:
  - staging 192.168.60.11 (via staging pod, NODE_PATH trick): DDL_OK, 46 rows untouched.
  - prod lisportalprod2 (local mysql2): DDL_OK, 1269 rows untouched (1266 WHOLE / 2 PST / 1 PR).
  - Gotcha: `status` enum on ehr_integrations is PENDING/APPROVED/LIVE/REJECTED — there is NO 'PAUSED'; to retire a test integration use `result_enabled=0` (eligibility hard-requires it).
- Deploy verified with two signals: pod image == main HEAD 52282e3 + `GROUP:MAIN` present in pod dist JS + PER_REPORT_GROUP in pod Prisma client.
- **Prod E2E PASS** (VP-17344 method adapted): created test integration `cvp17715e2etest000000001` (customer 4953, cloud, PER_REPORT_GROUP, deferred=["GUT5"], test SFTP /Test/Input/EMR_V2/VP17715 — push-level filtering guarantees the replay cannot touch the customer's real MDHQ folder even though it's the same customer). Replayed C Foote's real 10/11-generated event (GUT5 pending) to cloud Event Hub `general-sample-events` from the prod pod (SASL password via Key Vault `kafka-sas-connection-string`, DefaultAzureCredential works on-pod):
  - GROUP:MAIN record TRANSMITTED in 57s, 5077KB, FOOTE^CALLAN, 793 data OBX, oxalic acid present, PDF included.
  - Replay #2 → still exactly 1 record (DEDUP_OK).
  - Test row then retired via result_enabled=0.
- **Cust 4953 flipped**: c0000CMYCWWC500T2I1A85BHS -> PER_REPORT_GROUP + deferred=["GUT5"], verified. Interim per-specimen-type pushes stop within 60s (level cache).
- Jira transitioned Done (closure chain: merge -> deploy(2-signal) -> DDL -> E2E -> flip, all verified).

## Open items
1. PM/customer sign-off on GZ-first collapse-to-one-push edge (ticket open question 1) — Leo said no re-confirm; reply sent stating config live.
2. lis-report behavior when a main report is permanently cancelled: does total_reports shrink? (ticket open question 2)
3. Report short-name versioning (GUT5 → future) needs ops runbook note when flipping other customers.
4. Residual watch: first ORGANIC main-group push for a real 4953 order (next order whose non-GZ set completes) — worth a dream-closeout check.

## Failures
- Worktree node_modules cloned from a stale branch checkout missed staging's newer deps (@azure/identity, @sentry/node) → 5 TS2307 build errors; `npm install` in the worktree fixed it. Lesson: after cloning node_modules into a worktree, run npm install before trusting the build.
- `npx jest <full path>` matched 0 tests (testRegex vs path mismatch in this repo) — use a name pattern (`npx jest kafka-report-finished-listener`).
