---
id: VP-17591
type: stm
category: emr_integration
status: in_review
score: 0.00
base_weight: 1.0
created: 2026-08-04
updated: 2026-08-04
links: [VP-17544, VP-16832, VP-17117, VP-17480]
tags: [vp-17591, address, pid-11, ny-routing, gz, patient-completeness]
summary: "EMR patient address: PR #316 merged+deployed (NY now inbound-only per Leo; new-patient address + type fix). CORE SYMPTOM UNRESOLVED — emr-v2 never sends address in place-order, so the empty postOrderStr address comes from a downstream service not in our repos. is_primary_address is de-facto abandoned (77% zero)."
---
# VP-17591 - Work Loop Record

## Ticket Analysis

Bug, P2, `jira_escalated`. Reporter Mingxi Li, assignee Leo. Order 2607316527,
patient JONATHAN HAFT (patient_id 3160709), customer 35353 / clinic 128087.
Symptom: EMR order file contains the patient address, but the payload sent to
Order service has every address field blank.

Leo asked to look at this together with VP-17544 — correctly, they are the same
family: upstream's patient-completeness check is **DOB + gender + address**, and
VP-17544 covered the first two while address was silently broken.

### Evidence chain (all three verified)

| | |
|---|---|
| HL7 file | `PID\|1\|11078.1914\|\|\|HAFT^JONATHAN^\|\|19771007\|M\|\|\|89 Vagneur Lane^^Basalt^CO^81621\|\|970-948-3168\|...` — address complete |
| Patient record (`GetPatient` 3160709) | `{"address_type":"shipping","street_address":"89 Vagneur Ln","city":"Basalt","state":"CO","zipcode":"81621-9100","address_confirmed":true,"is_primary_address":false}` |
| Sent to order service | `{"zipcode":"","country":"238","city":"","street":"","state":""}` |

`country:"238"` is the only populated field because it is hardcoded
(`patient_country_id = 238`) — a tell that the address came from neither source.

Reading the file needed appserver04 (`/mnt/storage/EMR_storage/...`, password auth
via expect + ControlMaster); the AKS pod's PV holds only the single folder that
pod owns.

### Root cause — three independent decisions stacking

1. **`findOrCreatePatient` existing-patient branch discarded the file's address
   unconditionally** (`patientDetails = grpcPatient` replaced the whole object).
   It never even checked whether the record had an address, so "DB empty + file
   populated" was equally broken.
2. **`populateFromExisting` selected the record address with
   `if (addr.is_primary_address)`.** Reads as "pick the main one out of several"
   — correct with several, but when the only row is not flagged primary the same
   line stops being a *selection* and becomes an *exclusion*.
   **741,520 of 1,045,219 patients with an address (71%) have no primary row**, so
   the majority case sits on a branch written for the minority.
3. **order-management asks the same question differently**:
   `CorePatientService.CheckIfPatientHasAddress` keys on
   `address_type == "shipping"` and ignores `is_primary_address` → it considers
   these patients complete and never flags the empty address we send. Two
   definitions of "has an address", no error on either side.

**Root enabler for (2)**: `address_type` was **absent from the
`PatientWithDetails` address type** although `GetPatient` always returned it —
so selecting by type would not compile, leaving `is_primary_address` as the only
selectable field.

**Confirmed the data does reach us** —病灶 100% 在 emr-v2，上游不需要改。

## Decisions Made

Leo: **file wins; if absent use DB** — then narrowed for NY:
> 有 Gut Zoomer（或任何需要判別 NY 的測試）一律只能以 HL7 為標準，沒有就連 DB 都不看直接拒單。
> 多收錢的問題一定要 customer 提供完整訊息，否則因搬家等原因會有財務問題，不能到我們這裡。

So two uses, two fields:
| field | source | used for |
|---|---|---|
| `patient_state` | file, else record | shipping the kit |
| `inbound_patient_state` | **file only** | NY routing + $80 fee |

`inbound_patient_state` set unconditionally (including undefined) so "the order
did not tell us" reaches `decideGzNy` as empty → existing
`NY_ADDRESS_REQUIRED` reject path fires.

**Scope discipline**: only `parse()` (HL7) switched. `assembleWithNyRouting`
serves the **API** intake path where the caller sends only `patient_id` and never
an address — inbound-only there would reject every GZ order; that path is guarded
by eligibility-check, which validates address itself. Verified the two paths share
no call site.

## Code Changes

Branch `bugfix/leo/VP-17591` (base `origin/staging`), **PR #316 draft**
https://github.com/Vibrant-America/lis-backend-emr-v2/pull/316
- `ec42fc3` file-wins address + record selection by shipping/primary/any +
  `address_type` added to the type
- `9fef7b0` NY decisions read `inbound_patient_state` only

## Test Results

Full suite **95 suites / 1083 passed**, `tsc --noEmit` clean, pre-push build OK.
13 regression tests from the order's **real PID line**.

**A failure that earned its keep**: my first test version hand-counted PID fields
and put the address in PID.10 (one blank short) — 4 tests failed, which is how I
caught it. Rewrote them to parse the verbatim PID string through the same helper
the decoder uses, so offsets cannot drift from reality.

## Failures

- Hand-counted HL7 field offsets instead of parsing a real segment → wrong index,
  4 failing tests. **Lesson: for positional formats, build fixtures from a real
  record parsed by production code, never by counting delimiters.**

### [2026-08-04 18:35] 診斷修正：emr-v2 根本不送 address，PR #316 沒解決核心症狀

**我錯在哪**：我驗證了「emr-v2 內部拿不到 address」（那是對的），卻**沒有驗證那個值有沒有離開
emr-v2**。實際上：

- `PlaceOrderRequest` DTO（`dto/place-order.dto.ts`）**沒有任何 address 欄位** —— grep
  `address|street|city|state|zipcode` 零命中。
- `toPlaceOrderRequest`（`order-request.mapper.ts:154`）只送 `patient_id` / `customer_id` /
  `clinic_id` / line_items / deliver_method / payments / notification。
- `OrderFrontend` 型別同樣沒有 address 欄位。
- `patient_address` 在整個 hl7-order-processing 只出現在三處用途：
  `createPatientV2`（**只有新病人**）、`updatePatientInfo` 的 request 型別（**但
  `updateContactIfChanged` 只更新 phone/email**）、以及 NY email 的 `{{patient_state}}`。

→ ticket 那個 `postOrderStr` 的空 address 是**下游從 patient DB 補的**，不是 emr-v2 送的。

**PR #316（已 merge + 已 deploy prod）的實際價值**：
| 改動 | 有效 |
|---|---|
| NY 判定改讀 `inbound_patient_state`（Leo 規則） | ✅ 真的在 emr-v2，decideGzNy 直接讀 |
| 新病人 address 寫入（createPatientV2） | ✅ |
| `address_type` 補進型別 | ✅ |
| 既有病人 `patientDetails.address` | ❌ **不進訂單 payload，對 ticket 症狀無效** |

**已排除的下游嫌疑（都有 fallback，不是元兇）**：
- `LIS-backend-v2-order-management/tasks/message.go:618` 用 `IsPrimaryAddress` 挑，
  但 `if primaryAddress == nil { primaryAddress = patient.PatientAddress[0] }` 有 fallback。
  且那是 Kafka addonColumn，不是 postOrderStr。（該處註解自己寫
  `// TODO: Need to verify which address used here`。）
- `LIS-backend-billing/.../OrderServiceImpl.java:553` 用 `address_type=="shipping"` 挑，
  找不到還退到 provider 的 office address。且那是 concierge coverage 用途。
- **billing 是 postOrderStr 的收方**（`metaDataMap.get("postOrderStr")`），不是產生者。
  全 `~/src` 只有 billing 的 script/test 提到 `completeOrderInformationSelected` 格式
  → **產生者不在我 clone 到的 repo 裡**（可能是 legacy LIS）。已停止盲追第五層，改問 Leo。

**`is_primary_address` 對病人地址已實質廢棄（強證據）**：
| type | primary | 筆數 |
|---|---|---|
| shipping | 0 | **833,404（77%）** |
| shipping | 1 | 251,608（23%） |
| `Hello` / 空字串 | 1 | 2 筆髒資料 |
最近建立的 5 筆全是 `primary=0` → 不是「忘了打勾」，是現行寫入流程根本不寫它；23% 有值的是歷史資料。
且病人地址**一律** `address_type='shipping'`（只有 2 筆例外）→「以 shipping 為主」的挑選
實務上等於「拿任何一筆」。

**Deploy 驗證（prod, main@32ea60e, pod 844b49cc7f-xgmtc）**：
CI success、2/2 Running、restarts=0、舊 pod 已終止、零 DI/unhandled-rejection、
AlertingModule + Hl7OrderProcessingModule 正常初始化、Sentry 仍 not-configured（預期）。
→ **只證明無 regression**，不能證明症狀修好（那個值不在我們送出的 payload 裡）。

**Jira 狀態：不轉 Done。** merged + deployed + 零 regression，但核心症狀未解。

### [2026-08-04 18:50] 逐層排除，元兇定位到 billing（但無法定案）

**產生 `postOrderStr` 的是 billing 自己**：
`LIS-backend-billing/.../OrderServiceImpl.java:3915` →
`buildCompletePatientInfoMap(lisCorePatient, newYorkPatient)` → `:4030`
`asyncServices.putOrderList(wholeBodyStr, ...)`。
（`SampleServiceImpl:303` 那個 `metaDataMap.get("postOrderStr")` 是另一條 path，收既有字串。）

**address 填入邏輯（`:4433-4465`）本身是對的**：
```java
addressMap.put("country", "238");   // ← 預設值，與 ticket payload 完全一致
for (PatientAddress a : patient.getPatient_address())
    if (a.getAddress_type().compareTo("shipping") == 0) { ...填入... }
```
用 `address_type=="shipping"`，**完全不看 `is_primary_address`**。而該病人的 address 正是
`shipping` → **應該填得進去**。payload 的 `country:"238"` 是這裡的預設值，
證明那個迴圈**根本沒進去** → billing 手上的 `patient.getPatient_address()` 是空的。

**逐層排除（每一層的邏輯都正確）**：
| 層 | 判斷 | 結論 |
|---|---|---|
| emr-v2 place-order | `PlaceOrderRequest` DTO 無 address 欄位 | 不送，非元兇 |
| order-management `message.go:618` | `IsPrimaryAddress` 但有 `[0]` fallback；且是 Kafka addon 非 postOrderStr | 非元兇 |
| billing concierge `OrderServiceImpl:553` | `shipping` + 退到 provider office address | 非元兇 |
| **billing `buildCompletePatientInfoMap`** | 邏輯正確（shipping，不看 primary） | **輸入為空** |
| coreSamples `GET /api/patient/get-patient-by-id` → `readPatient` | **有** `include: { patient_address: true }` | 應回 address |
| 時序 | address_id 4686438 與 patient 3160709 連號；`patient_create_time` **2025-12-09**，訂單 2026-07-31 | **早 8 個月，排除** |

→ **每一層的邏輯都對，結果卻是空的** ⇒ 問題在執行期而非邏輯：
候選為 (a) billing 的 `PatientAddress` model 反序列化失敗（欄位名/型別不匹配 → `getAddress_type()`
回 null → `compareTo` NPE 被上層吞掉，addressMap 保持預設）、(b) prod 跑的 coreSamples/billing
版本與 repo 不同、(c) billing 呼叫的 `CoreService` 指向另一個服務。

**定案需要 billing 的 prod log** —— 它有
`log.info(GsonUtil.nestJsonMap2Json("Patient Map", compeletePatientInfoMap))` (`:3919`)
和 `log.info("Retrieving patient with id: ...")`，一看就知道它拿到什麼。
但 billing 的 pod **不在我的 kubeconfig 範圍**（只有 AKS `lisportalprod` + minikube；
`bkkeeping` ns 只有 `lis-accounting`）→ 交給 Leo。

**不自行開票**：元兇在 billing，屬別的服務
（[[feedback_defect_found_must_be_ticketed]] 的 OTHER-team 分支：交診斷、不自己開）。

### [2026-08-04 19:16] Deploy 最終驗證通過（無 regression）

`id=6788 vendor=THM pod=lis-emr-v2-deployment-prod-777c956c9b-xs52b finished=1
sample=2609199 retry=5 err="" code=""`

- pod hash `777c956c9b` ≠ AKS 的 `844b49cc7f` → **on-prem pod 也已更新**。
  （DB 的 `last_update_pod_name` 是唯一能證明 on-prem 已更新的途徑 —— 那個 cluster
  不在本機 kubeconfig。同一手法在 VP-17544 用過，當時 hash 是 `546b6869b8`。）
- `retry=5` 未扣 → 一次成功；`err=""` / `code=""` → 無 error、無誤攔截、無 NY_ADDRESS_REQUIRED。
- 加上先前的 boot 驗證（零 DI/unhandled-rejection、AlertingModule +
  Hl7OrderProcessingModule 正常、cron 18:45 執行）→ **無 regression 成立**。

**但這不證明 ticket 症狀修好** —— address 不在 emr-v2 送出的 payload 裡，元兇在
billing（見上一節）。這是本次驗證的範圍上限，已在 PR #316 與此處明確標示。

## Retrospective

Pending Leo's review of PR #316.

## Lessons Learned

- **A conditional that selects among many becomes a filter when there is one.**
  `if (isPrimary)` was written for "choose among several addresses"; with a single
  non-primary row it silently produced *no* address. When a predicate picks a
  winner, ask what it does when there is exactly one candidate that fails it.
- **Two services keying on different fields for the same concept = silent data
  loss.** Neither side errors: the strict one produces empty, the lenient one sees
  "complete" and does not complain.
- **A missing optional field in a type can steer behaviour.** `address_type`
  absent from the interface made the correct selector uncompilable, so the wrong
  one was the only option available.
- **Stale data is not a smaller version of missing data — for money decisions it
  is worse.** Leo's rule: never infer a billable attribute from stored state the
  current request did not assert. Missing → reject and make the sender supply it.
