---
id: VP-17493
title: '[BE] EMR partial push (PER_SAMPLE_TYPE): include report PDF in sample_type_all_finish pushes'
status: in_review
category: emr_integration
created: 2026-07-24
updated: '2026-07-24'
links:
- VP-17344
- emr-integration
tags:
- vp-17493
- result-push
- partial-push
- pdf
summary: 'PER_SAMPLE_TYPE partial pushes now resolve add_report from report_option
  (PDF attached) instead of forced 0; generation degrades to data-only on PDF
  failure (single attempt, no placeholder, no job failure). PR #288 draft -> staging.
  Origin: Dr Prusmack (cust 4953) complaint; report team confirmed PDF availability
  at sample_type_all_finish time 2026-07-24.'
jira_status: Dev To Do
---

# VP-17493 — PER_SAMPLE_TYPE partial pushes include the report PDF

## Ticket Analysis
### [2026-07-24 11:00]
- Filed by us same day (triage on VP-17344 STM 2026-07-24 12:00 entry has full evidence chain: 21/44 samples PDF-less since the 07-16 VIP flip; 9 manual repushes 07-23/24).
- Leo decision after report team confirmed PDF exists by sample_type_all_finish time.

## Outcome
- Branch `feature/leo/VP-17493` (worktree, off origin/staging b4a599b), PR **#288 draft -> staging**: https://github.com/Vibrant-America/lis-backend-emr-v2/pull/288
- 3 commits: a402b1a degrade guard (service) -> 63e2d58 listener flip -> 032c57a tests.

## Key decisions / findings
### [2026-07-24 11:30]
- Data flow: listener enqueue `add_report_override` -> processor (`undefined` => getReportOption maps report_option CLASSIC->'1'/PERSONALIZED->'2') -> `GenerateResultHl7Input.add_report` -> clientConfig.add_report -> `generateHl7Content` PDF block -> encoder emits PDF OBX only when `enabled && base64Data` (hl7-encoder.service.ts:206).
- **generatePdfReport NEVER threw before** — total download failure returned a hardcoded placeholder PDF (result-generation.service.ts ~:2285, the patterns.md:1046 anti-pattern). New `throwOnFailure` option throws instead; partial path catches -> WARN + pdfBase64 undefined (data-only). Whole-order path untouched (still placeholder — pre-existing, out of scope).
- `downloadPdfFromApi` gained `maxRetries` param; partial uses 0 (single attempt) to avoid the 10/20/40s backoff spiral inside BullMQ jobs.
- Plumb: `SampleResultData.pushScopeKey?` set from `input.push_scope_key` at the generateHl7Content call site (no prepareResultGenerationData change).
- `enqueuePartialPush` parameterized: SAMPLE_TYPE passes `undefined`, REPORT passes `'0'` (PER_REPORT stays data-only, out of scope).
- Tests: new `result-generation.partial-pdf.spec.ts` (direct-construction harness copied from pdf-retry.spec; generateHl7Content tested with spied privates + mocked hl7Service/testPanelMapping). Full suite 922 passed / 0 failed.

## Open items
1. Leo review + undraft PR #288; staging deploy; then staging/prod verify per PR rollout plan (replay sample_type_all_finish on cvp17344e2etest0000000001; confirm PDF OBX present; second replay with broken PDF URL to prove degrade).
2. Promote staging -> main after verify; first organic PDF-carrying partial for cust 4953 = customer comms trigger.
3. Blast radius today = only integration c0000CMYCWWC500T2I1A85BHS (cust 4953) is PER_SAMPLE_TYPE.
4. Pre-existing debt noted, NOT fixed here: whole-order placeholder-PDF fallback (ships junk PDF on download failure) — candidate follow-up ticket.
