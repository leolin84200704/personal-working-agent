---
id: VP-18034
type: stm
category: emr_integration
status: active
score: 1.0375
base_weight: 1.0
created: 2026-09-03
updated: '2026-09-03'
links:
- BETA-E2E-20260729
- BIOINSIGHTS-onboarding
- HL7-NPI-PRACTICE-MATCH-20260820
- HL7FAIL-20260722-MDHQ
- HL7FAIL-20260729-PLESSEN
- HL7FAIL-20260730-TURNPAUGH
- HL7FAIL-20260903-EVERSPAN
- INCIDENT-2604156666
- LBS-1541
- LBS-1656
- LBS-1773
- LIS-7716
- PH-847
- QH-1660
- QH-2257
- QH-2577
- QH-4350
- QH-4608
- RESULTCHECK-20260819-RCODE-2608186060
- VEJO-DELETION-20260804
- VP-14787
- VP-15279
- VP-15952
- VP-16166
- VP-16175
- VP-16193
- VP-16251
- VP-16271
- VP-16280
- VP-16329
- VP-16685
- VP-16734
- VP-16765
- VP-16766
- VP-16832
- VP-16881
- VP-16885
- VP-16934
- VP-17076
- VP-17117
- VP-17120
- VP-17136
- VP-17283
- VP-17286
- VP-17344
- VP-17474
- VP-17475
- VP-17497
- VP-17499
- VP-17524
- VP-17538
- VP-17539
- VP-17544
- VP-17584
- VP-17589
- VP-17591
- VP-17628
- VP-17686
- VP-17691
- VP-17734
- VP-17752
- VP-17760
- VP-17810
- VP-17812
- VP-17827
- VP-17914
- VP-18030
- VP-18055
- VP-18066
- VP-18080
- VP-18086
- emr-integration
- fhir-api
relations:
  unblocked_by:
  - VP-18031
  - VP-18032
  - VP-18089
  blocks: []
  sibling:
  - VP-18032
  - PH-886
unblock_when: ''
tags:
- vp-18034
- vp-18032
- ph-886
- partner-api
- order-intake
- charge-indicator-t
- platform-payment
- rs256
- stax-mit
summary: 'Portal API: chargeIndicator T (third-party platform pays) on POST /orders
  — emr-v2 half. Accept T + platformId + X-Payment-Authorization (platform-signed
  RS256 JWT, verified per VP-18032), charge the platform''s Stax card via charging
  (account_type=platform, VP-18089), fail-closed like C. Blocked-by chain: VP-18089
  Dev Complete 09-03; VP-18032 (mine) + VP-18031 (Rui, platform record + public key)
  still Dev To Do.'
jira_status: Dev Blocked
---

# VP-18034 - Work Loop Record

## Ticket Analysis

### [2026-09-03 12:20] Intake (L4 verified via Atlassian MCP)

- VP-18034 `[BE] chargeIndicator "T" on POST /orders — platform pays`. Task under epic VP-17290
  (Vibrant Wellness API — Partner Platform). Reporter Xiaoye Li, assignee Leo, P2. Status **Dev
  Blocked** (automation comment 186568 on 09-02 asks Leo for blocker details — unanswered).
  Blocks-links: is blocked by VP-18032 (Dev To Do, assignee Leo) and VP-18089 (Dev Complete
  2026-09-03 11:58, Fangyuan). QA twin QH-6928. Relates PH-886.
- PH-886 (Nan Wu, Dev In Progress) = requirement. Attachment `third-party-payment-flow.md` v2
  (2026-09-01) downloaded to scratchpad; Xiaoye's 09-01 meeting-outcome comment 186250 narrows
  scope to items 1, 2, 4: platform record + public key (VP-18031, Rui Chen, Dev To Do), signed
  assertion verification (VP-18032), chargeIndicator T (VP-18034 + VP-18089). Stax tokenization,
  card-management endpoints, charge webhook are OUT (VP-18033/18035/18036 resolved 09-01).
- Leo's trigger 2026-09-03: "PH-886.m4a VP-18034 應該可以做了" — the trigger is VP-18089 landing;
  a 41-min meeting recording (PH-886.m4a) accompanies it. Transcribed locally with mlx-whisper
  large-v3-turbo (whisper not installed on this machine; installed via pipx).

### Spec (third-party-payment-flow.md v2, the acceptance reference)

- Part 2 — platform JWT: header `{alg: RS256, kid}`, claims `iss=sub=platformId`,
  `aud=https://api.vibrant-america.com/v1`, `exp` 60 s out, `jti` random. Server: resolve key by
  platformId → verify RS256 → check aud + exp (small skew) → reject reused jti (shared short-lived
  store across instances).
- Part 3 — POST /orders: `Authorization: Bearer <provider token>` + `X-Payment-Authorization:
  Bearer <platform JWT>` + body `chargeIndicator: "T"`, `platformId`. Server: verify provider
  token → resolve platform record (key + card) → verify platform JWT → charge card → place order.
  platformId is a lookup hint, NOT authorization. Stax charge must carry
  `meta.transaction_initiation_type = MIT` + `meta.transaction_schedule_type = unscheduled`.
  Charge failure or no card on file ⇒ no order (same as C). VP-18032 additionally suggests binding
  the assertion to the order it authorizes.

### Current emr-v2 state (origin/main 582c00d, worktree `lis-backend-emr-v2.worktrees/vp18034`, branch feature/leo/VP-18034)

- `order-intake.controller.ts`: `CHARGE_INDICATORS = ['C','P','I','O']`, anything else → 400
  INVALID_CHARGE_INDICATOR (VP-17687). `order-intake-mapper.service.ts`
  `chargeMethodFromIndicator`: 'C' → CUSTOMER_PAY else PATIENT_PAY_LATER.
- Charge path lives in `order-finalizer.service.ts` (charge-first): builds `chargeUserPayload`
  (customer_id/clinic_id, role clinic) → `ChargeClientService.getPaymentMethodCandidates`
  (GET allSharedPaymentMethods?customer_id&clinic_id → customer_payment_methods +
  clinic_payment_methods) → walks candidates calling `transactionPay` (POST transaction/pay with
  account_type 'customer', TRANSACTION_PAY_DEFAULTS stax/card/emr/new_sample). API path sets
  `failOnCustomerPayFailure` so no payment_id ⇒ `paymentFailed` ⇒ intake row 'rejected',
  reason payment_failed (422). Completed charge persisted on order_intake
  (payment_id/sample_id_payment/julien_barcode) BEFORE placement for replay (VP-17497).
- `toPlaceOrderRequest` sends `charge_method` (PlaceOrderChargeMethod has no third-party value)
  and `payments[{id: payment_id, payment_type card, payment_platform stax}]`.
- Auth: `JwtStrategy` accepts HS256 (JWT_SECRET) + RS256 (FHIR_JWT_RS256_PUBLIC_KEY, single
  OAuth key). `jsonwebtoken` already a dependency (fhir-access.guard uses jwt.verify RS256).
  Redis available via ioredis (`RedlockService`, REDIS_HOST/PORT/PASSWORD/DB) — candidate jti
  store.
- No platform-record table exists anywhere in emr-v2 (prisma has no platform model; no remote
  branch for VP-18031/18032). OAuth repo (Rust) also has no platform branch.

### Charging side (LIS-backend-v2-charging main 661ce1e = PR #360 feat/platform-payment, VP-18089)

- 46f2930: `account_type` enum gains `platform` on customer_info + transaction.
- 9b45c75: GET /paymentMethod/allSharedPaymentMethods accepts `account_type=platform&account_id=`
  → response key `account_payment_method` (Confluence 620396707 §5 updated 09-03; Fangyuan's
  comment 186677 points Leo there).
- **GAP**: no code sets `transaction_initiation_type`/`transaction_schedule_type` on the Stax
  charge (`model.ChargePaymentMethodMeta` has tax/poNumber/shippingAmount/subtotal/lineItems/memo
  only; grep for MIT/unscheduled across repo = 0 hits). Spec Part 3 + VP-18089 AC #2 require it.
- transaction/pay with account_type=platform would flow through `payWithCardViaStax`:
  `IsBatchPayment` applies to any non-patient account (≥4 successful charges on the same token in
  the 12-h window ⇒ "pending batch" record, charged later by BatchCharge, and that branch posts
  the accounting Payment with `AccountType: "customer"` hard-coded). `pay()` also reads
  customer_id/clinic_id from our HS256 token (`GetCustomerClinicIdFromJWT`) — for a platform
  charge those would be the PROVIDER's ids unless we decide otherwise.

### [2026-09-03 12:30] Meeting recording PH-886.m4a (2026-09-01 design meeting, 41 min, Mandarin)

First pass with `--language en` hallucinated the first ~15 min ("It's the same" loop) — the
meeting is Mandarin; re-ran with `--language zh --condition-on-previous-text False`. Decisions
audible in the Mandarin portion (Chris = spec author, Leo, Fangyuan, "Ray" = Rui):
- Two platforms in scope: **Junction** and **Get Healthy**. No UI for them; card tokenization
  is an emailed Stax payment link, done manually. Expect "at most one or two" platforms.
- Chain per platform: platform name ⇄ Stax payment token ⇄ emailed RS256 public key. **Rui stores
  the public key under the platform record** ("那个是 Ray 要存"); Leo "拿 platformId 去跟 Ray 拿
  public key" then verifies the header signature; then Leo fetches the platform's card from
  Fangyuan's charging (new account_type `platform` + account_id) and charges it; "剩下的流程跟
  provider pay 一样" — post-payment flow follows customerPay; refunds go back to that card.
- `platformId` in the body was Leo's suggestion in the meeting ("加一个栏位直接告诉我们是哪一个人的");
  Chris accepted and put it in spec v2 (so no try-every-key).
- One card per platform in this phase. GET/DELETE payment-method endpoints deferred (revoke =
  our-side delete; Stax-side revoke discussed as nicer but not required). Multi-card ordering /
  card selector = later.
- Receipts: phase 1 unchanged (still to the provider); phase 2 maybe a platform receipt email on
  the record. Charge webhook event = phase 2.
- Fangyuan: charging needs to distinguish a third payer role beyond provider/patient.
- Tail of the recording is PH-858 (quote response for free add-ons / discount bundles) — Rui's
  pricing ticket, unrelated to this one.

### Lesson injection (ENGINEERING-LESSONS, sparse)

- "Idempotency key 不可被 terminal failure 消耗 … 若 pipeline 是 charge-first，副作用完成的當下就要把憑證
  持久化到該 row，retry 時 replay 而非重做" — the T path must reuse the VP-17497 replay so a
  platform is never charged twice on a resend.
- Hot lesson "Schema migration before deploy" applies if a platform table / order_intake column
  lands in emr-v2 (non-ORM-managed prod DB → manual ALTER first).

## Approaches Considered

### [2026-09-03 12:45] Proposed design (emr-v2, VP-18032 + VP-18034 together) + debate synthesis

Design as presented to Leo (Step 4):
1. Controller: add 'T'; when T require `platformId` (400 MISSING_PLATFORM_ID) and
   `X-Payment-Authorization` (401 MISSING_PAYMENT_AUTHORIZATION); verify BEFORE the intake row
   is written; failures 401 INVALID_PAYMENT_AUTHORIZATION + details.reason; unknown platform 400
   INVALID_PLATFORM_ID. `platformId` added to the PH-861 type-check loop.
2. New `PlatformAssertionService` (VP-18032): jsonwebtoken RS256 pinned (`algorithms:['RS256']`),
   key from injected `PlatformKeyDirectory` (impl = client to Rui's VP-18031 record, URL via env;
   key looked up by the CLAIM iss, then iss==sub==body.platformId enforced; kid must be one of
   that platform's registered keys), jti replay via injected `JtiStore` (Redis SET NX EX, TTL ≥
   exp + skew; in-memory impl for tests). Redis down ⇒ fail-closed for T.
3. Mapper: T → ChargeMethod.CUSTOMER_PAY + `OrderFrontend.thirdPartyPayer = { platformId }`.
4. Finalizer: payer set ⇒ candidates from new `getPlatformPaymentMethods(platformId)`
   (`account_type=platform&account_id=`, response key `account_payment_method`), transactionPay
   with account_type 'platform' / account_id platformId. Guard/replay/indeterminate-stop unchanged
   (PRO verified: only finalizer :90 and :113-114 know the payer).
5. Two PRs: PR-A VP-18032 (service + tests, no wiring), PR-B VP-18034 (wiring).

PRO (verified in code): replay branch :74-81, failOnCustomerPayFailure guard :192-206,
indeterminate stop :169-177, VP-17497 persistence intake.service :288-297 are all payer-agnostic;
fhir-access.guard.spec already signs real RS256 JWTs with generateKeyPairSync — same pattern for
the new spec. PlaceOrderChargeMethod has no third-party value so customerPay is the only legal
mapping today.

CON (verified in charging code, ranked):
- **Critical — batch path**: `IsBatchPayment` (transaction_service.go:276-297) = ≥4 successful
  record_ids on the SAME payment_token in the 12-h UTC window ⇒ batch. A platform card pays for
  many providers, so the 5th order of the half-day WILL hit it; that branch (payment.go:1041-1084)
  posts the accounting Payment with `AccountType:"customer"` + `AccountID = JWT customer_id`
  (the PROVIDER), creates a status=2 pending transaction and returns
  `payment_transaction_id = "charging_v2_pending_batch_…"`, which emr-v2 (finalizer :129) treats
  as a completed charge. Also `AdjustChargeAmount` (stax_service.go:786-811) shaves $0.01 per
  same-amount charge on the same token within 5 min ⇒ charged ≠ `total` sent to Place Order.
- **High — no authz in charging**: `pay()` and `getAllSharedPaymentMethods` never call VerifyID;
  emr-v2's assertion check is the ONLY thing preventing charging an arbitrary platform. So the
  assertion must be bound to the request (VP-18032 "consider binding" ⇒ required), not optional.
- **High — jti vs placerId**: jti consumed in controller before the dedup ⇒ a retry replaying the
  same assertion gets 401 not `duplicate`; must be documented (new assertion per attempt). dryrun
  consumes jti but never touches charging.
- **Medium — semantics**: charge_method customerPay ⇒ order-management thinks provider paid
  (refunds/receipts). `GetStaxKey(orderCustomerId)` picks sandbox vs prod by the PROVIDER's id.
- **Medium — schema**: no payer column ⇒ reconciliation parses request_json; recommend nullable
  `order_intake.payer_platform_id` (manual ALTER before deploy).
- MIT/unscheduled meta absent in charging (VP-18089 AC #2 unmet).

Synthesis adopted into the proposal: (a) bind assertion to request (claim `placer_id` + `amount`
or body hash — needs Chris/PM since partners must sign it); (b) emr-v2 T path treats a
`charging_v2_pending_batch_` id as "charge did not complete" (fail-closed) until charging skips
batching for platform; (c) add `payer_platform_id` column; (d) charging-side go-live
prerequisites list for Fangyuan (skip IsBatchPayment for platform, fix batch AccountType, add MIT
meta, decide GetStaxKey for platform, accept provider HS256 token with account_type=platform).

## Decisions Made
## Code Changes
## Test Results
## User Feedback
## Failures
## Retrospective
## Lessons Learned
